# Future Crates

Items extracted from completed plans. See [index](../deferred-items.md).

## Deferred

| Item | Source | Status | Tracked |
|------|--------|--------|---------|
| Examples for geometry-events, widgets, paint-style \| those crates not yet implemented | [examples-crate spec](../plans/done/2026-05-02-examples-crate.spec.md) | | #45 (closed), #46 (closed), #47 (closed) |
| Facade `style-dispatch` feature in `quartzite/Cargo.toml` for re-exporting the bridge crate. | [renderer-style-dispatch design](../plans/done/2026-05-13-renderer-style-dispatch.design.md) |  | #393 (closed) |
| `BrushKind::LinearGradient` / `RadialGradient` rendering — needs gradient-stop API + peniko `Gradient` wiring | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #281 (closed) |
| `Image` source-rect cropping — trait surface lacks a source rect; would require a `Painter` method addition | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #291 |
| Per-test perceptual-diff tolerance tuning — calibration once real pixels exist (mentioned in `gpu-snapshot-tests-ci` open questions) | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #286 |
| `Container` content clipping (clip-rect to `geometry()` so overflow children are cut) — requires renderer-side dispatch decisions outside `Style::draw_widget`'s contract; tracked under #312. | [container-lineedit-rendering spec](../plans/done/2026-05-15-container-lineedit-rendering.spec.md) | #312 (closed) |
| Automated `scripts/check-unsafe-index.sh` parity with any future `scripts/check-panic-index.sh` — mechanical CI gate enforcement. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| Per-entry severity tagging (e.g. "structurally unavoidable" vs "removable with refactor") — revisit if the index grows beyond ~20 entries. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| Flipping `continue-on-error: false` — tracked as a deferred follow-up keyed to N consecutive Miri-clean master runs. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Editing AGENTS.md § Build & Test for a Miri local-reproduction recipe — not required by ACs; deferred follow-up if developers ask. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Stacked Borrows secondary job (cross-check against TB) — follow-up if TB lets a known-UB site slip through. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Cross-target Miri (Windows/macOS/big-endian/32-bit) — opened after v1 is hard-failure on Linux. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| v3 aggressive variant: `quartzite-macros/tests/via_facade.rs` Miri-runnability via `--no-default-features` if facade's renderer dep is feature-gated. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| v2-aware FFI-free audit recipe: replace naive `grep -nE "winit\|wgpu\|vello" Cargo.toml` with `cargo metadata --no-deps` filter to catch dev-dep vs prod-dep distinction automatically. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| A `scripts/check-miri-tags.sh` audit script that intersects `rg '#\[test\]'` against Miri's actual reported run-list and flags untagged failures. Nice-to-have; the manual recipe in spec Scope §7 (and `ai-docs/miri-policy.md`'s § Audit recipe) suffices for v3. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Rewriting cross-references in `ai-docs/learnings.md` / past spec docs that mention "poisoned mutex recovery" wording. Historical artefact, not actionable; Corrections-Log Boundary rule 1 (append-only) preserves history. | [migrate-std-locks-to-parking-lot spec](../plans/done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | — |
| A future PR introducing named `#[serial]`-style serialisation groups (if/when tests legitimately need distinct lock domains). v1 has no demonstrated need; file a fresh issue if/when a named-group requirement materialises. | [replace-serial-test-with-mutex-helper spec](../plans/done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | — |
| Wiring `spec-writer` / `triage-runner` / other non-visual `.claude/agents/*` to the design system — none of those currently make visual decisions; file a follow-up the first time a real gap surfaces. | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Per-PR review automation that checks visual-rule deviations programmatically — candidate follow-up issue once AC4 has stabilised in practice (design-review now flags deviations as `major` manually). | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Adding `line_edit_read_only.png` as a new golden snapshot — unit tests in `default_style_tests.rs` already pin the `LineEdit` read-only brush colour; image golden is redundant for this fix's verification. Deferred for a follow-up if visual-regression coverage of the `LineEdit` read-only state becomes desirable. | [make-read-only-overlay-visible spec](../plans/done/2026-05-18-make-read-only-overlay-visible.spec.md) | — |
| Stale `/design` slash-command label in `ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md:242` (the cell reads "Items the design agent (`/design`) will still own — …"). Phrasing was semantically wrong even pre-rename (agents are not slash-invokable; only skills are); caught during Step 10 self-review of #470 but outside this spec's § Scope enumeration. Deferred as a one-line cleanup follow-up. | [rename-design-skill-to-ui-design spec](../plans/done/2026-05-18-rename-design-skill-to-ui-design.spec.md) | — |
| Windows / macOS snapshot golden re-bootstrapping — wrong platform for this PR's author; lanes stay `continue-on-error` | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | #192 (closed) |
| Promoting non-flagged fns to `const fn` opportunistically (fns with branches the lint can't yet handle but that *could* become `const fn` with restructuring). Separate issue: No — falls under future const-fn work if ever desired. | [fix-missing-const-for-fn spec](../plans/done/2026-05-19-fix-missing-const-for-fn.spec.md) | — |
| OS-dark-mode auto-detection (e.g. via `winit` window theme events) — separate concern from theme definition itself; needs platform matrix. Separate issue needed: yes. | [dark-style-design-system spec](../plans/done/2026-05-19-dark-style-design-system.spec.md) | — |
| Per-widget `Style` overrides / multi-style cascade — the current registry holds a single `&'static dyn Style`; a multi-style model is a larger architecture change. Separate issue needed: yes. | [dark-style-design-system spec](../plans/done/2026-05-19-dark-style-design-system.spec.md) | — |
| Auto-switching `Palette::default` based on env var or `prefers-color-scheme` equivalent — would change the meaning of `Palette::default`, which the snapshot suite and existing tests pin to the light defaults. Separate issue needed: yes. | [dark-style-design-system spec](../plans/done/2026-05-19-dark-style-design-system.spec.md) | — |
| Dark-mode goldens for `quartzite-widgets/tests/snapshots/` — today those tests render empty chrome because `WidgetExt::paint` is a no-op; revisit when widget-side painting lands. Separate issue needed: probably. | [dark-style-design-system spec](../plans/done/2026-05-19-dark-style-design-system.spec.md) | — |

## Out of scope

| Item | Source | Status | Tracked |
|------|--------|--------|---------|
| Proc macros (separate crate) | [core-types spec](../plans/done/2026-05-01-core-types.spec.md) | ✅ done | |
| Widget hierarchy (quartzite-widgets) | [core-types spec](../plans/done/2026-05-01-core-types.spec.md) | | #46 (closed) |
| Event system (quartzite-events) | [core-types spec](../plans/done/2026-05-01-core-types.spec.md) | | #45 (closed) |
| Widget rendering (quartzite-widgets) | [runtime spec](../plans/done/2026-05-01-runtime.spec.md) | | #46 (closed) |
| Event model types (quartzite-events) | [runtime spec](../plans/done/2026-05-01-runtime.spec.md) | | #45 (closed) |
| Examples for unimplemented crates (geometry, events, widgets, paint, style) | [examples-crate spec](../plans/done/2026-05-02-examples-crate.spec.md) | | #45 (closed), #46 (closed), #47 (closed) |
| `quartzite-widgets` (not yet implemented) | [public-api-docs spec](../plans/done/2026-05-02-public-api-docs.spec.md) | | #46 (closed) |
| Built-in image decoders — `Image` is still consumed as the raw RGBA8 buffer set up by `paint-style`. File / byte-stream decoding stays tracked under #282. | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #282 |
| `BrushKind::LinearGradient` / `RadialGradient` rendering — variant already exists on `BrushKind` as `#[non_exhaustive]`; backend support tracked under #281. | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #281 (closed) |
| `Image` source-rect cropping — tracked under #291. | ai-docs/plans/done/2026-05-12-renderer-painter-impls.spec.md | #291 |
| Per-platform variants (macOS-flavoured, Windows-flavoured) — the source paint-style spec defers these and tracks the work via #284. | ai-docs/plans/done/2026-05-13-default-style-content.spec.md | #284 |
| Per-crate coverage targets in `codecov.yml` — tracked in issue #268; explicitly deferred from this task. | [increase-project-coverage spec](../plans/done/2026-05-15-increase-project-coverage.spec.md) | #268 |
| Tightening the Codecov drop threshold below 1% or flipping `informational: true` to a hard gate — tracked in issue #267; explicitly deferred until coverage stabilises. | [increase-project-coverage spec](../plans/done/2026-05-15-increase-project-coverage.spec.md) | #267 |
| Refactoring or rewriting either of the 2 production unsafe blocks. The index records them; hardening is the "preferred fix" of each entry, not the unsafe-index creation task itself. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| Tracking test-only unsafe (any `unsafe` block in `tests/`, `#[cfg(test)]` modules, `benches/`, or `examples/`). Index records production sites only, mirroring panic-index. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| `scripts/check-unsafe-index.sh` enforcement script. Sync gate is enforced by review-findings + self-review, not by a CI script. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| CI workflow changes. Sync enforced at PR-review time, not at GitHub-Actions time. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| Merging unsafe-index and panic-index into a single "invariant-index" file. User explicitly asked for a parallel file. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| Cross-target Miri runs (Windows, macOS, big-endian, 32-bit targets) — defer until the Linux baseline is green. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Stacked Borrows secondary job alongside Tree Borrows — only justified if TB lets a real UB pattern slip through; speculative in v1. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Miri-instrumented benchmarks (criterion harness compatibility + runtime cost). | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Miri against doctests (`cargo miri test --doc` harness behaviour varies). | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Auto-bridging Miri findings into `ai-docs/unsafe-index.md` — Miri findings remain a CI artefact; index updates stay manual. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| PR-side Miri trigger — master-only in v1; per-PR latency (10–30× native cargo test) is prohibitive. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| Re-routing master Miri failures into `/master-ci-failed` — existing flow already covers any red master CI signal; no special-case wiring required. | [miri-master-push-job spec](../plans/done/2026-05-17-miri-master-push-job.spec.md) | — |
| `quartzite-renderer` — permanently excluded from Miri (its library uses wgpu/winit/vello directly; no `--lib`/`--test` filtering can side-step that). | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| `quartzite-widgets/tests/snapshots.rs` — by design uses GPU; not added to Miri subset. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| `quartzite-style/tests/snapshots.rs` (if it exists post-#374) — same as above. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| `quartzite-macros/tests/via_facade.rs` — uses the facade crate which depends on renderer; excluded via `--test` allow-list. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| Increasing `MIRIFLAGS` further — v2 unchanged from v1 cumulative `-Zmiri-tree-borrows -Zmiri-ignore-leaks -Zmiri-disable-isolation`. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| Flipping `continue-on-error: true` → `false` — separate deferred follow-up to #422. | [miri-subset-widening spec](../plans/done/2026-05-17-miri-subset-widening.spec.md) | — |
| Flipping `continue-on-error: true` → `false` on the Miri job — separately-tracked deferred follow-up to #422; v3 inversion does not advance it. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Adding or removing `MIRIFLAGS`. v3 inherits the cumulative v2 set verbatim (`-Zmiri-tree-borrows -Zmiri-ignore-leaks -Zmiri-disable-isolation`). New failure classes surfaced post-merge follow the v1/v2 AC8 envelope in a follow-up PR, not this spec. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Switching Tree Borrows → Stacked Borrows. Out of scope; TB rationale already lives in #422 spec. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Running Miri per-PR. Master-only-comment AXIOM in `miri.yml` still applies (10–30× latency cost vs `cargo test`). | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Cross-target Miri (Windows / macOS / big-endian / 32-bit). Already deferred per #424. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | #424 |
| Doctest Miri coverage — `cargo miri test` does not Miri-execute doctests by default; v3 does NOT change that. Doctests stay covered by the standard `cargo test --workspace --doc` gate. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Bench / example Miri coverage — benches (`harness = false` criterion binaries) and examples are not `#[test]` targets; Miri does not run them and v3 does not change that. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| `quartzite-renderer` test re-inclusion — permanently excluded at workflow level under v3, same as v1 / v2. The whole rendering surface is FFI by construction. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Auto-generated `[workspace.metadata.miri]` or `cargo-miri` config-file approach. v3 uses CLI flags only; config-file forms are not introduced. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Renaming or moving the existing `#![cfg(not(miri))]` opt-outs in `quartzite-widgets/tests/no_style_dep.rs` and `quartzite-runtime/tests/timer.rs`. They stay as-is and become the documented fallback recipe. | [miri-deny-list spec](../plans/done/2026-05-17-miri-deny-list.spec.md) | — |
| Migrating `std::sync::Once` / `std::sync::OnceLock`. Not mutexes; no parking_lot equivalent that improves on std. | [migrate-std-locks-to-parking-lot spec](../plans/done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | — |
| Migrating `std::sync::Arc` / `std::sync::Weak` / `std::sync::atomic::*` / `std::sync::mpsc`. Out of scope; only `Mutex`/`RwLock`/`MutexGuard`/`RwLockReadGuard`/`RwLockWriteGuard` move. | [migrate-std-locks-to-parking-lot spec](../plans/done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | — |
| Replacing the `quartzite_core::Mutex` re-export at `quartzite-core/src/lib.rs:76` — it already re-exports `parking_lot::Mutex`. | [migrate-std-locks-to-parking-lot spec](../plans/done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | — |
| Performance benchmarking the parking_lot migration. Motivation is consistency, correctness (no poisoning), and idiom simplification; any perf delta is a side-effect. | [migrate-std-locks-to-parking-lot spec](../plans/done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | — |
| Splitting the single bare-default `#[serial]` group into multiple named groups (no named groups exist in the codebase today; v1 preserves single-group semantics). | [replace-serial-test-with-mutex-helper spec](../plans/done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | — |
| Cross-binary test isolation (`serial_test` did not provide this either; nothing changes). | [replace-serial-test-with-mutex-helper spec](../plans/done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | — |
| Migrating production code from `std::sync::Mutex` to `parking_lot::Mutex` — #442 already standardised this and landed independently (PR #456). | [replace-serial-test-with-mutex-helper spec](../plans/done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | #442 (closed) |
| Resolving #427 directly. This issue is the *implementation track* for option (f) of #427's proposed paths; verifying that the post-merge Miri run is green is a release-gate observation. | [replace-serial-test-with-mutex-helper spec](../plans/done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | #427 (closed) |
| Editing `CLAUDE.md` — auto-import (`@<file>`) is the wrong tool for conditional context (would unconditionally load ~21 KB on every session including non-visual tasks). | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Editing `design-system/` contents themselves — source of truth is already correct; this task only wires the agent prompts to read it. | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Changing the YAML frontmatter of `design-system/SKILL.md` (the `user-invocable: true` flag stays); the new `.claude/skills/ui-design/SKILL.md` is a separate, complementary slash-discoverable pointer. | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Inlining the 19 KB `design-system/README.md` into `AGENTS.md`, `CLAUDE.md`, or any agent prompt; every touchpoint references the design-system folder by path. | [wire-design-system-context spec](../plans/done/2026-05-18-wire-design-system-context.spec.md) | — |
| Updating `design-system/README.md` § *Transparency & blur* and § *Dark theme* "Read-only" row to describe the new `WindowText.with_alpha(0.10)` overlay rule (currently describes the old `Window.with_alpha(0.5)` overlay). Out of scope per spec; the design-system proposal at `design-system/proposals/text-edit-read-only-overlay.md` tracks the follow-up. | [make-read-only-overlay-visible spec](../plans/done/2026-05-18-make-read-only-overlay-visible.spec.md) | — |
| Renaming the `design` agent at `.claude/agents/design.md` (frontmatter `name:` field set to the old slug) to free up the slug entirely. Out of scope from the skill rename (issue #470 explicitly contrasted the skill against the existing agent and asked only the skill to move); future work could decide to rename the agent as well, eliminating the cross-namespace `design` collision completely. | [rename-design-skill-to-ui-design spec](../plans/done/2026-05-18-rename-design-skill-to-ui-design.spec.md) | — |
| Renaming the canonical `design-system/SKILL.md` (frontmatter `name: quartzite-design`, `user-invocable: true`). Out of scope from the skill rename — it is the Read-target of the (renamed) `ui-design` pointer skill and keeps its name. Filed for visibility only; no actionable follow-up unless the design-system folder itself is restructured. | [rename-design-skill-to-ui-design spec](../plans/done/2026-05-18-rename-design-skill-to-ui-design.spec.md) | — |
| `bincode 2 → 3` — placeholder release (`compile_error!`); documented exception in `ai-docs/plans/done/2026-05-10-object-property-serialization-layer.design.md § Open questions`. | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | — |
| `rstest 0.25 → 0.26` — addressed in #438. | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | #438 |
| Re-bootstrapping Windows / macOS snapshot goldens. Per the #192 follow-up policy, those lanes remain `continue-on-error: true` until contributors with those platforms regenerate locally. This task does **not** flip that flag. | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | #192 (closed) |
| Workspace-wide `peniko` constraint changes. Verified via crates.io `index.crates.io/ve/ll/vello`: vello 0.9 requires `peniko ^0.6.1`; our existing `peniko = "0.6"` constraint (in `quartzite-renderer`, `quartzite-style`, `quartzite-widgets`, `quartzite-paint`, `quartzite-paint-api`) accepts that under cargo caret-semantics. No workspace peniko bump required. | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | — |
| Miri exclusion-list changes. Per issue #439's coordination note, the renderer crate is already excluded from Miri at the workflow level (`--exclude quartzite-renderer`), so #436's deny-list inversion is unaffected by this bump. | [bump-vello-wgpu-major spec](../plans/done/2026-05-18-bump-vello-wgpu-major.spec.md) | #436 (closed) |

## Open questions

Rows sorted here by `/triage` from `_inbox.md` (Source-anchored). 3-column schema `Item | Source | Tracked` — the heading encodes what was the `Section` column.

| Item | Source | Tracked |
|------|--------|---------|
| AGENTS.md § Agent Docs symmetry — add rows for BOTH `ai-docs/panic-index.md` AND `ai-docs/unsafe-index.md`, or leave both out symmetrically. Current state: neither has a row. Spec conditioned the unsafe-index row on the panic-index row's presence; consistent treatment (add or leave-out) is the follow-up. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
| AGENTS.md § Propagation Rule symmetry — confirm the existing Procedure's grep step covers "unsafe-index" naturally (default assumption); add a new sync-group row if grep misses the unsafe-index in practice. | [unsafe-index spec](../plans/done/2026-05-16-unsafe-index.spec.md) | — |
