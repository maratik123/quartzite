# CI, Docs & Workflow

Items extracted from completed plans. See [index](../deferred-items.md).

## Deferred

| Item | Source | Status | Tracked |
|------|--------|--------|---------|
| Badge links in README \| will be added later by user | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #60 (closed) |
| Contributing guide, roadmap \| user will add later | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #60 (closed) |
| Additional facade features \| will be added based on needs as the project grows | [docs-and-facade spec](../plans/done/2026-05-02-docs-and-facade.spec.md) | | #60 (closed) |
| Multi-version docs (post first `cargo publish`) — After docs.rs takes over for releases | [cargo-doc-pages spec](../plans/done/2026-05-07-cargo-doc-pages.spec.md) |  | #266 |
| Threshold tightening (below 1%) — after project matures | [coverage-ci spec](../plans/done/2026-05-07-coverage-ci.spec.md) |  | #267 |
| Per-crate coverage targets — not needed until more crates land | [coverage-ci spec](../plans/done/2026-05-07-coverage-ci.spec.md) |  | #268 |
| `.github/workflows/fork_pr_benchmarks_closed.yml` (PR branch archival in Bencher) — not needed at first land | [criterion-benchmarks spec](../plans/done/2026-05-07-criterion-benchmarks.spec.md) |  | #269 |
| `--no-default-features` cross-OS coverage — not in this issue | [multi-platform-ci spec](../plans/done/2026-05-07-multi-platform-ci.spec.md) |  | untracked |
| `Swatinem/rust-cache@v2` migration — smarter cargo-side caching with better eviction | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| Coverage workflow sccache integration — needs separate validation that sccache doesn't perturb coverage instrumentation | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | #270 |
| Self-hosted sccache backend — only worthwhile at much higher CI volume than this project has | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| **crates.io / docs.rs badges** — both URLs require first publish | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | #271 |
| **`lib.rs` versioning-policy section** — text changes post-publish | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | #272 |
| **Facade conveniences (re-exports / helpers)** — on-demand as new crates land | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Force-rerun escape hatch — deferred to Open questions; separate issue needed only if the team wants explicit override semantics. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| Migration of *other* `.claude/skills/**` and `.claude/agents/**` files that are also approaching their own perf thresholds — track separately when measured. — independent perf chore; not blocking this one | [shrink-agents-md spec](../plans/done/2026-05-13-shrink-agents-md.spec.md) |  | #378 |
| A `scripts/check-agents-md-size.sh` precommit / CI gate that would mechanically prevent regression past the threshold. — nice-to-have automation, not required by this task | [shrink-agents-md spec](../plans/done/2026-05-13-shrink-agents-md.spec.md) |  | #379 (closed) |
| CI gate for "no orphaned `.progress.md` on master" — useful safety net but requires a separate design discussion and a separate workflow file | [cleanup-progress-issue-derive spec](../plans/done/2026-05-13-cleanup-progress-issue-derive.spec.md) |  | #380 |
| Bash unit-test harness for `.claude/skills/**/scripts/*.sh` — useful for future script changes but a workspace-wide tooling decision | [cleanup-progress-issue-derive spec](../plans/done/2026-05-13-cleanup-progress-issue-derive.spec.md) |  | #381 |
| Migration of *other* `ai-docs/{code-style,doc-convention,agent-writing-style,corrections-log}.md` files that the AGENTS.md size AXIOM also targets but which have not yet crossed the 35k speed bump. — independent perf chore; not blocking this one | [shrink-context-md spec](../plans/done/2026-05-13-shrink-context-md.spec.md) |  | #382 (closed) |
| A `scripts/check-instruction-file-sizes.sh` precommit / CI gate that would mechanically prevent regression across all instruction files at once. — nice-to-have automation, named in the AGENTS.md AXIOM as "Until ... lands"; not required by this task | [shrink-context-md spec](../plans/done/2026-05-13-shrink-context-md.spec.md) |  | #383 (closed) |
| Pinning `actions-rust-lang/setup-rust-toolchain` to a specific minor (e.g. `@v1.16.1`) — currently using major-floating per project convention; revisit if a future minor release ships a regression. | [macos-cargo-path-pin spec](../plans/done/2026-05-14-macos-cargo-path-pin.spec.md) |  | #384 (closed) |
| Auditing per-job `RUSTFLAGS` strictness uniformly across the workspace — post-merge follow-up if any latent-warning fallout from this PR's swap is non-trivial. | [macos-cargo-path-pin spec](../plans/done/2026-05-14-macos-cargo-path-pin.spec.md) |  | #385 (closed) |
| Long-form anti-patterns catalogue for compaction-related failure modes (what NOT to do after re-entry) — depends on observation post-merge; likely separate `/improve` cycle. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | #386 (closed) |
| Generalising the "compaction recovery" callout into a reusable include / template once a second project adopts the pattern — premature now; separate issue if a second project shares this repo's skill layout. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Token-budget telemetry / measurement scripts to confirm post-merge that `/task` SKILL.md stays under the 5,000-token target as it evolves — useful follow-up; not blocking; separate `tooling/` issue. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| 95%+ stretch coverage goal — requires closing `quartzite-runtime` concurrency gaps and additional `VelloPainter` GPU-paint paths beyond what is feasible in one task; follow-up issue filed as part of this task's AC6 closure. | [increase-project-coverage spec](../plans/done/2026-05-15-increase-project-coverage.spec.md) |  | untracked |
| `quartzite-renderer::vello_painter::Segment::_ => {}` wildcard coverage — `#[non_exhaustive]` upstream-extension catch-all on `quartzite_paint_api::Segment`; same shape as the AC4 BrushKind restructure but not in this task's scope; separate follow-up. | [increase-project-coverage spec](../plans/done/2026-05-15-increase-project-coverage.spec.md) |  | untracked |
| Citation-footer fan-out across the 6 SKILL.md compaction-recovery callouts (`> Per ai-docs/agent-writing-style.md § Pattern 7, variant X`) — low leverage, AC5 of #358 explicitly bars callout-body edits in this PR; revisit if ever wanted. | [propagate-callout-to-style-guide spec](../plans/done/2026-05-15-propagate-callout-to-style-guide.spec.md) |  | untracked |
| CI / pre-commit gate that runs the AGENTS.md `## Propagation Rule` grep automatically — engineering concern parallel to `scripts/check-instruction-file-sizes.sh`; scope both gates together when either lands. | [propagate-callout-to-style-guide spec](../plans/done/2026-05-15-propagate-callout-to-style-guide.spec.md) |  | untracked |
| Generalising the three callout variants into a reusable include template — premature; wait until the variant taxonomy stabilises through ≥1 more skill onboarding (e.g. `/pr-failed` per #357). PR #349's *Risks* table flagged this as a long-term mitigation for variant drift. | [propagate-callout-to-style-guide spec](../plans/done/2026-05-15-propagate-callout-to-style-guide.spec.md) |  | untracked |
| Auto-invocation of `/master-ci-failed` from `/pr-merged` when the merge commit's CI subsequently turns red — keeps the two skills loosely coupled in v1; revisit once `/master-ci-failed` lands and we observe how often merge commits turn master CI red. | [ci-failed-skills spec](../plans/done/2026-05-15-ci-failed-skills.spec.md) |  | #387 (closed) |
| A `watch` mode for `/pr-ci-failed` that loops polling `gh pr checks` until green (Step 9 currently "optional") — out of scope for v1; user can re-invoke `/pr-ci-failed` after the run completes. | [ci-failed-skills spec](../plans/done/2026-05-15-ci-failed-skills.spec.md) |  | #388 (closed) |
| A sister `ci-classifier` Opus agent that owns log triage when the inline classifier in the skill body is ambiguous — defer until we observe whether misclassification is frequent enough to justify the cost; for now both skills carry the inline classifier. | [ci-failed-skills spec](../plans/done/2026-05-15-ci-failed-skills.spec.md) |  | #389 (closed) |
| Self-reference exception design refinement for `/ai-audit` Checklist M — AC5 demonstrator showed that the audit recipe (`ai-audit/SKILL.md`) AND the style guide (`agent-writing-style.md`) self-match sub-checks 2 / 6 / 7 / 8 because they naturally contain fail-loud verbs / variant phrases / anti-pattern row names. A future `/improve` cycle can either exclude these two files from those sub-checks specifically, or add a per-sub-check false-positive rate threshold (Pattern 6's > 50% FP fallback model). Not blocking AC5. | [ai-audit-checklist-m design](../plans/done/2026-05-16-ai-audit-checklist-m.design.md) |  | untracked |
| Variant-B body drift in `.claude/skills/bugfix/SKILL.md` AND `.claude/skills/interview/SKILL.md` — both contain the `Compaction recovery check` callout (style guide table classifies them Variant B) but show TOTAL=0 hits against all 3 distinguishing phrases on the AC5 demonstrator run. Likely real drift from the locked Variant-B body. Investigate post-merge via a focused /task or /improve. | [ai-audit-checklist-m progress (AC5 demonstrator)](../plans/done/2026-05-16-ai-audit-checklist-m.spec.md) |  | untracked |
| Tighten Pattern 6 heuristic further if AC5 demonstrator's false-positive rate exceeds 50% in practice — the spec's documented fallback path; recorded as a noise-management option for the first /improve cycle after this PR. | [ai-audit-checklist-m design](../plans/done/2026-05-16-ai-audit-checklist-m.design.md) |  | untracked |
| Promoting the grouping rule into a project-wide AXIOM in AGENTS.md — additional propagation surface; the design / design-review agent files are sufficient surface for now. | [design-handoff-groups spec](../plans/done/2026-05-16-design-handoff-groups.spec.md) |  | #390 (closed) |
| Tooling that lints a `*.design.md` for grouping compliance outside the design-review agent — second enforcement layer; review-agent enforcement is sufficient at the gate's current failure rate. | [design-handoff-groups spec](../plans/done/2026-05-16-design-handoff-groups.spec.md) |  | untracked |
| Auto-detecting whether an orchestrator's *own* context is approaching compaction and proactively spinning up a fresh top-level orchestrator session — not blocking; deeper redesign, needs telemetry. | [task-step-8-group-handoff spec](../plans/done/2026-05-16-task-step-8-group-handoff.spec.md) |  | untracked |
| Generalising the every-group handoff pattern to `/project-review`'s phase loop and `/pr-commented`'s round loop — scope-creep into other skills; revisit when the /task version is proven. | [task-step-8-group-handoff spec](../plans/done/2026-05-16-task-step-8-group-handoff.spec.md) |  | #391 (closed) |
| Moving Steps 9–11 into dedicated handoff subagents (the user's original phrasing implied this; round-1 narrowed it). Revisit if Steps 9–11 are observed to trigger compaction in practice. | [task-step-8-group-handoff spec](../plans/done/2026-05-16-task-step-8-group-handoff.spec.md) |  | #392 (closed) |
| `scripts/check-instruction-file-sizes.sh` pre-commit / CI gate — long-term mechanical enforcement complements the audit-side back-stop. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | #383 |
| Generalising Sub-check 10's heading-derived coverage map to other reference style guides (`code-style.md`, `doc-convention.md`, etc.) — reduces audit drift across multiple style references. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Extraction of `AGENTS.md § Workflow` — second-highest extraction value, second-highest context risk (multiple downstream skills depend on rule placement). Revisit if a future edit pushes AGENTS.md back into the 35k early-warning band. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Second-round extraction analysis after this PR lands — defer to the next PR that hits the early-warning band; the AXIOM itself prescribes the action. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Per-crate allow-list refinement — v1 workspace-level uniformity, only split if a crate consistently violates a healthy-elsewhere lint | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Threshold tightening below 512 KiB defaults (e.g., 64 KiB for `quartzite-paint`) — first-run data needed to identify candidate crates | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Removing now-redundant per-crate `#![warn(clippy::...)]` attributes superseded by `pedantic = warn` — cleanup; behaviour-neutral | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Tightening to `level = "deny"` directly in `[workspace.lints.clippy]` — retains feature-branch `cargo clippy --workspace` (without `-D warnings`) workflow | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Enabling `clippy::restriction` subset (e.g., `clippy::unwrap_used`, `clippy::dbg_macro`) — overlaps with `panic-index.md` discipline; separate decision | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Per-pedantic-lint promotion audit (Option B) — higher effort, per-lint judgement, not blocking the narrow win this spec captures | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Auditing project-level group settings (`pedantic = warn`, `nursery = warn`) for whether they should escalate to `deny`. Different decision class (group-level lint posture) than per-lint allow audit. Out-of-scope per #423's framing; revisit if needed in a separate spec. | [audit-workspace-clippy-allows spec](../plans/done/2026-05-18-audit-workspace-clippy-allows.spec.md) | — |
| Bulk retro-tagging of remaining historical `learnings.md` entries with `Kind: correction` — low-leverage churn vs. the default-when-omitted rule already covers them | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| A central `ai-docs/patterns.md` cross-skill index — rejected by Q3 (Inline Patterns chosen); revisit only if cross-skill reuse of carrot patterns proves to be a real need | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| `scripts/check-instruction-file-sizes.sh` pre-commit / CI gate that would mechanically prevent regression past the 40k cap or the 35k early-warning band — nice-to-have automation referenced by the AXIOM itself ("Until `scripts/check-instruction-file-sizes.sh` lands as a pre-commit / CI gate, …"); not required by this task. | [squeeze-agents-context-size spec](../plans/done/2026-05-20-squeeze-agents-context-size.spec.md) | — |
| Shrinking the 7 oversized `.claude/skills/*/SKILL.md` files (`/ai-audit` Sub-check K1) — independent perf chore; needs per-skill design review since each contains workflow content. | [squeeze-agents-context-size spec](../plans/done/2026-05-20-squeeze-agents-context-size.spec.md) | #498 |
| `scripts/check-instruction-file-sizes.sh` gate landing — needs its own design pass for soft/hard thresholds, CI placement, allow-list format | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| AGENTS.md § Agent Docs symmetry for `panic-index.md` + `unsafe-index.md` (already tracked as an open question in `ai-docs/deferred/_inbox.md`, unsafe-index source). This spec adds a row for `skill-size-exemptions.md` only; the symmetry question stays open. — cohesion with the new index might motivate adding panic / unsafe rows in a follow-up — separate issue not needed yet (already in the inbox). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Generalising the audit-script-versus-instruction-surface tension (e.g., any future "audited list of source files" should live in `ai-docs/` rather than as per-file inline comments). — design-pattern claim worth recording if a third index lands — separate issue likely no. | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Removing or further rewriting the `palette-state-groups.proposal.md` cross-references that other Rust files might gain once #402 lands. None exist in the current tree — re-evaluate when #402 ships. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| **Pre-commit hook for the rustdoc-internal-refs regression gate.** Adds setup cost on every contributor's first commit; CI gate alone covers the regression risk for the published surface. Revisit if doc-comment leakage recurs after the CI gate lands. | [rustdoc-strip-internal-refs spec](../plans/done/2026-05-21-rustdoc-strip-internal-refs.spec.md) | — |
| **Broader audit of `//` non-doc comments for internal references.** Non-doc comments don't reach the published surface; this issue is scoped to rustdoc only. | [rustdoc-strip-internal-refs spec](../plans/done/2026-05-21-rustdoc-strip-internal-refs.spec.md) | — |
| Mechanising the rule via a regression script — semantic classification cannot be reliably encoded in regex per issue body | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| Broader audit of inline comments inside fence content beyond `// …`-prefixed lines (e.g. `let x = 3; // comment` trailing comments) — not flagged by the issue's `rg` sweep; would require a different pattern and a fresh classification pass | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| `scripts/check-instruction-file-sizes.sh` (#383) — not landed yet; must NOT include `.claude/rules/` in scan list (Q-A semantics) | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| Cross-rule-file propagation conventions (when 2+ rule files exist and one changes a shared term) — premature — only one rule file exists today | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| Snapshot coverage for state combinations beyond single-flag goldens (e.g. `text_edit_focused_read_only.png`, `text_edit_disabled_focused.png`) — Button spec capped at single-state goldens too; combinations not enumerated unless a regression surfaces | [hover-focused-label-textedit-scroll spec](../plans/done/2026-05-21-hover-focused-label-textedit-scroll.spec.md) | — |
| Re-paint trigger on state transition (calling `update()` after `set_hovered` / `set_pressed` / `set_focused`) — input-plumbing pass owns the event-loop side and will set state + call `update()` together | [hover-focused-label-textedit-scroll spec](../plans/done/2026-05-21-hover-focused-label-textedit-scroll.spec.md) | — |
| Dark-theme `hovered` / `pressed` snapshot coverage on Label / TextEdit / ScrollArea — only `dark_*_focused.png` ships in v1; the dark `Window`/`Base` Hover/Pressed cells derive a small luminance shift toward `MERCURY` (0.06 / 0.16 blend), visually subtle relative to the Highlight-driven `Pressed` cells | [hover-focused-label-textedit-scroll spec](../plans/done/2026-05-21-hover-focused-label-textedit-scroll.spec.md) | — |
| Dark-theme `hovered` / `pressed` snapshot coverage for LineEdit — mirrors the #403 spec's deferral on Label/TextEdit/ScrollArea; only `dark_line_edit_idle.png` + `dark_line_edit_focused.png` ship in v1; the dark `Base` Hover/Pressed cells derive a small luminance shift that would produce near-identical PNGs to idle/focused | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Combined-state snapshot goldens beyond single-flag coverage for LineEdit (e.g. `line_edit_focused_read_only.png`, `line_edit_disabled_focused.png`, `line_edit_pressed_placeholder.png`) — Button / #403 specs capped at single-state goldens too; combinations are recording-painter-tested rather than snapshot-tested | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Re-paint trigger on state transition for LineEdit (calling `update()` after `set_hovered` / `set_pressed` / `set_focused`) — input-plumbing pass owns the event-loop side and will set state + call `update()` together; this spec only changes paint output | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| `scripts/check-instruction-file-sizes.sh` precommit/CI gate — why: superseded by user direction — `/ai-audit` Sub-check 9 is the only enforcement surface | [reframe-size-cap-axiom spec](../plans/done/2026-05-22-reframe-size-cap-axiom.spec.md) | — |
| Convenience facade fn `quartzite::paint_widget_tree(...)` that wraps `dispatch_paint` + a default palette + a `StyleRegistry::try_style()`-aware no-op shape — new issue if the four-arg signature proves clunky for typical callers | [facade-style-dispatch-feature spec](../plans/done/2026-05-22-facade-style-dispatch-feature.spec.md) | — |
| Adding `WidgetResolver` to `quartzite::prelude` — name collision with `quartzite_widgets::layout::WidgetResolver` requires a rename; new issue when one of the two traits becomes prelude-worthy | [facade-style-dispatch-feature spec](../plans/done/2026-05-22-facade-style-dispatch-feature.spec.md) | — |
| End-to-end example under `examples/style_dispatch.rs` showing facade-level usage — requires a running painter backend; new issue when a `quartzite-renderer`-using example lands | [facade-style-dispatch-feature spec](../plans/done/2026-05-22-facade-style-dispatch-feature.spec.md) | — |
| A second bridge-crate caller's actual translate-pattern needs (e.g. nested clip + translate, or scale + translate) — re-evaluate when a second bridge crate is filed; speculative additional combinators without a real consumer risk YAGNI | [raii-guard-painter-transform spec](../plans/done/2026-05-22-raii-guard-painter-transform.spec.md) | — |
| Closure-passing convenience helper (`Painter::with_translate(…)` or `quartzite_paint_util::with_translate(painter, origin, \|p\| …)`) — separate ergonomics task; new issue if it ever lands | [raii-guard-painter-transform spec](../plans/done/2026-05-22-raii-guard-painter-transform.spec.md) | — |
| Wiring the new reference doc into `/task` / `/project-review` / `/context-reset` / `spec-writer` / `triage-runner` skill / agent files as a load-on-demand reference for `instruction file rewrite` triggers — low priority; methodology is only consumed when a contributor opts in via the source plan's Phase 2 follow-up | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Promoting the guard to a workspace-wide instruction-file size-check sibling (e.g. running it alongside `scripts/check-instruction-file-sizes.sh` once that lands per #383) — parallel CI-gate tooling, separate concern | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Rolling the `ui-design` filter into a shared "non-actionable-by-Claude" filter alongside `blocked` — refactor into a single helper section once a third such label appears; re-evaluate when the next out-of-harness label lands. | [next-ui-designer-queue spec](../plans/done/2026-05-23-next-ui-designer-queue.spec.md) | — |
| Retroactive sweep over the 277 existing `_inbox.md` rows to classify and link design-work entries — bounded one-time backfill but expensive (~277 prompts); future `/triage --backfill-design-link` one-shot once the forward-going gate has shipped and stabilised. | [triage-ui-design-blocker spec](../plans/done/2026-05-23-triage-ui-design-blocker.spec.md) | — |
| Renaming on-disk filenames under `.claude/agents/` to `*.subagent.md` — filename rename has broad Propagation-Rule + `.claude/settings.json` + plugin-metadata blast radius. Round-1 "Prose only" answer locks this out; only re-open if a future need emerges. | [harness-taxonomy-unify spec](../plans/done/2026-05-23-harness-taxonomy-unify.spec.md) | — |
| Adding a CI lint that enforces the four naming axes (`grep -E '(the (Bash\|Read\|…) tool\|slash command\|sub-agent)\b'` returning empty in PR diff) — mechanical enforcement is a force-multiplier but distinct from this one-shot rewrite. | [harness-taxonomy-unify spec](../plans/done/2026-05-23-harness-taxonomy-unify.spec.md) | — |
| Adding a CI lint that enforces clash-rename policy (Checklist O mechanically, in pre-commit) — `/ai-audit` runs on demand; a pre-commit guard catches it earlier. Re-open after at least one Checklist O false-positive / true-positive cycle informs the regex. | [harness-taxonomy-unify spec](../plans/done/2026-05-23-harness-taxonomy-unify.spec.md) | — |
| Renaming `ai-docs/agent-writing-style.md` → `ai-docs/subagent-writing-style.md` — the file documents writing-style rules for both Skills and Subagents; rename has Propagation-Rule blast radius. Only if future maintenance burden justifies it. | [harness-taxonomy-unify spec](../plans/done/2026-05-23-harness-taxonomy-unify.spec.md) | — |
| Behavioural rewrite of any Class-E 🟡 candidate Subagent — Class E currently lists zero 🟡 candidates (all six landed ✅ keep or ⛔ inline mandatory). If a future audit reclassifies any to 🟡, the rewrite is its own PR per the round-3 promotion directive ("audit-only is acceptable for this PR"). | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| `LineEdit` caret + selection rendering (#405) — shares the `StyleClock` + `Painter` text-measurement cursor seams + selection-model shape introduced by this spec; ships as a near-identical follow-up. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | #405 (closed) |
| IME composition underline — needs a `ColorRole` decision and shaping integration; open question in the design proposal. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | — |
| Block-cursor / overwrite mode — needs a `mode` field on `TextEdit`; deferred until input handling lands. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | — |
| Scroll-offset rendering on `TextEdit` (content overflow) — needs a viewport-offset model; deferred until text editing's scroll-policy lands. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | — |
| Word / line selection on multi-click — input-handling concern; folded into the input-handling pass. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | — |
| Per-widget caret-blink phase deviation (e.g. different blink rates per widget) — not in the design contract; follow-up if a real use case appears. | [textedit-caret-selection spec](../plans/done/2026-05-23-textedit-caret-selection.spec.md) | — |
| Password / masked input (`echo_mode`) on `LineEdit` — needs a `mode` enum field; caret position exposure on masked input is an open design question. | [lineedit-caret-selection spec](../plans/done/2026-05-24-lineedit-caret-selection.spec.md) | — |
| IME composition underline on `LineEdit` — needs a `ColorRole` decision and shaping integration; shared with TextEdit's deferred. | [lineedit-caret-selection spec](../plans/done/2026-05-24-lineedit-caret-selection.spec.md) | — |
| Block-cursor / overwrite mode on `LineEdit` — needs a `mode` field; same boundary as TextEdit. | [lineedit-caret-selection spec](../plans/done/2026-05-24-lineedit-caret-selection.spec.md) | — |
| Horizontal scroll-offset rendering on `LineEdit` (content overflow) — needs a horizontal viewport-offset model. | [lineedit-caret-selection spec](../plans/done/2026-05-24-lineedit-caret-selection.spec.md) | — |
| Word / line selection on multi-click in `LineEdit` — input-handling concern; folded into the input-handling pass. | [lineedit-caret-selection spec](../plans/done/2026-05-24-lineedit-caret-selection.spec.md) | — |
| Padding / min-size tokens (G6) — widget geometry is fully caller-driven; introducing built-in `min_size_hint` defaults is a layout-system concern, not a paint-rule concern. Follow-up spec needed. | [design-system-conformance spec](../plans/done/2026-05-24-design-system-conformance.spec.md) | — |
| Refresh `design-system/preview/*.html` mocks where mock simplification differs from the implemented rule (e.g. read-only overlay, `comp-label.html` top-anchored vs K3 vertical-centring). Documentation-only drift cleanup. | [design-system-conformance spec](../plans/done/2026-05-24-design-system-conformance.spec.md) | — |
| Automated AC# rustdoc leak guard (LLM-based, CI script, or doc-test bash/Python) — prevents future `AC<digit>` tokens leaking into `cargo doc` output; design needs its own discussion. | [fix-ac10-public-docs spec](../plans/done/2026-05-24-fix-ac10-public-docs.spec.md) | #559 (closed) |
| github-actions ecosystem coverage (`dependabot/github_actions/*` branches, `actionlint` gate, `workflow-only` diff classification) — v2 enhancement; v1 is cargo-only per KD-2 | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Auto-bisect of a grouped Dependabot PR to identify the offending sub-bump — requires test harness against partial-revert lockfile states | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Renovate / other bot support — scope creep for v1 | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Slack / GitHub notification routing when the skill bails to user — out of repo-local skill scope | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Per-dep allow-list of "always auto-merge" names (e.g., `serde` patches) — requires running data on pause-for-user frequency before deciding | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| LLM-based semantic guard for arbitrary internal requirement IDs (`KD<N>`, `Q<N>`, etc.) — broader scope; defer until a second leak class actually appears (no non-`AC` internal token has leaked yet). | [ac-doc-leak-guard spec](../plans/done/2026-05-27-ac-doc-leak-guard.spec.md) | — |
| Pre-commit hook variant of the AC# rustdoc leak check — the CI gate is sufficient and easier to maintain centrally; no separate issue planned. | [ac-doc-leak-guard spec](../plans/done/2026-05-27-ac-doc-leak-guard.spec.md) | — |
| Per-widget content-rect-aware dispatch — pass parent clip rect into child's paint as a layout hint for auto-fitting content to the viewport. Clip-only behaviour is sufficient for v1; auto-fit is a layout concern. | [per-widget-clip-rect spec](../plans/done/2026-05-27-per-widget-clip-rect.spec.md) | untracked |
| Generalise `content_rect()` to a richer frame model (separate accessors for inner-rect, border-rect, header-rect, etc.). Today's single `content_rect` suffices for clipping; richer decomposition is style-system work. | [per-widget-clip-rect spec](../plans/done/2026-05-27-per-widget-clip-rect.spec.md) | untracked |
| Hit-testing clip-rect awareness — clicks outside a `ScrollArea`'s clip rect should not reach `content_widget`. Belongs with the hit-testing pass tracked under #395. | [per-widget-clip-rect spec](../plans/done/2026-05-27-per-widget-clip-rect.spec.md) | #395 |
| Convert the 8 doc-example (`///`) `assert!(matches!(...))` occurrences (quartzite-paint-api path/brush/image, quartzite-core snapshot/tree) to `assert_matches!` — deferred from the 1.96 assert_matches adoption; needs a per-example `# use core/std::assert_matches;` hidden preamble; low value vs user-facing-doc churn; revisit only on a doc-style pass. | [bump-msrv-rust-1-96 spec](../plans/done/2026-05-29-bump-msrv-rust-1-96.spec.md) | untracked |
| Macro-doc-deny hard-error class (51 errors) + `--all-targets` extension of the Feature-matrix clippy step — `--all-features` enables BOTH the `#[object_impl]` macro's `undocumented-allow` and `undocumented-deny` features and deny wins → 51 undocumented test/example/bench fixtures across quartzite / quartzite-macros / quartzite-style become `compile_error!` hard errors under `--all-targets --all-features` (also fails `cargo build --workspace --all-targets --all-features`, confirming not clippy-specific). Resolving the class AND extending the new clippy CI step to `--all-targets` (to lint the serde-gated `#[cfg(test)]` sites on the all-features leg) are both deferred. | [all-features-clippy-debt spec](../plans/done/2026-05-30-all-features-clippy-debt.spec.md) | #587 (closed) |
| Event-dispatch loop routing `MouseEvent` to the hit widget's `on_mouse_press` / `on_mouse_release` / focus transitions — needs an event-routing design + mutable tree access (`&mut dyn AsWidget`) | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Per-widget custom (non-rectangular) hit shapes via a `WidgetExt::contains_point` override — needs a hit-shape abstraction + audit of every widget type | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Mouse-capture / pointer-grab so a pressed widget keeps receiving moves until release — needs the event-dispatch loop first | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |

## Out of scope

| Item | Source | Status | Tracked |
|------|--------|--------|---------|
| Windows / macOS CI runners | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #133 (closed) |
| Auto-merge | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #214 |
| Code coverage (`cargo-llvm-cov` + Codecov) | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #134 (closed) |
| Benchmarks (`criterion` + PR regression comments) | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #135 (closed) |
| Release workflow (cargo publish on tagged versions) | [github-workflow spec](../plans/done/2026-05-01-github-workflow.spec.md) | | #136 |
| Removing `rstest` from `quartzite-core` — it is genuinely used in `quartzite-core/src/value.rs` | [code-quality-cleanup spec](../plans/done/2026-05-02-code-quality-cleanup.spec.md) | | #215 |
| Any other code-quality changes not listed above | [code-quality-cleanup spec](../plans/done/2026-05-02-code-quality-cleanup.spec.md) | | untracked |
| `std` feature on `quartzite-runtime` (would gate nothing today) | [docs-and-facade spec](../plans/done/2026-05-02-docs-and-facade.spec.md) | | #216 |
| Full wildcard re-exports beyond `prelude` | [docs-and-facade spec](../plans/done/2026-05-02-docs-and-facade.spec.md) | | #217 |
| Future features (`extension`, `8k_pages`, etc.) | [docs-and-facade spec](../plans/done/2026-05-02-docs-and-facade.spec.md) | | untracked |
| `cargo doc` publishing / CI integration | [docs-and-facade spec](../plans/done/2026-05-02-docs-and-facade.spec.md) | | #137 (closed) |
| Unit tests inside example files | [examples-crate spec](../plans/done/2026-05-02-examples-crate.spec.md) | | #218 |
| Private/internal items | [public-api-docs spec](../plans/done/2026-05-02-public-api-docs.spec.md) | | #219 |
| Separate EXAMPLES.md or README additions beyond existing content | [public-api-docs spec](../plans/done/2026-05-02-public-api-docs.spec.md) | | #220 |
| Enabling GitHub Pages in Settings → Pages (user action — must be done by the repo owner after the first workflow run) | [cargo-doc-pages spec](../plans/done/2026-05-07-cargo-doc-pages.spec.md) |  | untracked |
| Per-PR docs preview | [cargo-doc-pages spec](../plans/done/2026-05-07-cargo-doc-pages.spec.md) |  | untracked |
| `--document-private-items` | [cargo-doc-pages spec](../plans/done/2026-05-07-cargo-doc-pages.spec.md) |  | untracked |
| Versioned/multi-version docs | [cargo-doc-pages spec](../plans/done/2026-05-07-cargo-doc-pages.spec.md) |  | untracked |
| Multi-OS coverage (Windows/macOS) — duplicate work, ubuntu-only is sufficient | [coverage-ci spec](../plans/done/2026-05-07-coverage-ci.spec.md) |  | untracked |
| Auto-fail on coverage drop — comment-only, informational only | [coverage-ci spec](../plans/done/2026-05-07-coverage-ci.spec.md) |  | untracked |
| Special proc-macro crate handling — `cargo-llvm-cov` v0.5+ handles proc-macros automatically | [coverage-ci spec](../plans/done/2026-05-07-coverage-ci.spec.md) |  | untracked |
| `quartzite-runtime/benches/` — event-loop timing dominated by OS scheduler noise on shared CI runners | [criterion-benchmarks spec](../plans/done/2026-05-07-criterion-benchmarks.spec.md) |  | untracked |
| Multi-platform benchmarks — statistically incoherent across OS/allocator/scheduler differences | [criterion-benchmarks spec](../plans/done/2026-05-07-criterion-benchmarks.spec.md) |  | untracked |
| Self-hosted runners | [criterion-benchmarks spec](../plans/done/2026-05-07-criterion-benchmarks.spec.md) |  | untracked |
| Object-tree lookup benchmarks (already in `quartzite-runtime/benches/object_tree.rs`) | [macro-object-bench spec](../plans/done/2026-05-07-macro-object-bench.spec.md) |  | untracked |
| Benchmarking the proc-macro compilation step itself | [macro-object-bench spec](../plans/done/2026-05-07-macro-object-bench.spec.md) |  | untracked |
| Workflow file changes (Bencher CI picks up `cargo bench --workspace` automatically) | [macro-object-bench spec](../plans/done/2026-05-07-macro-object-bench.spec.md) |  | untracked |
| `docs` and `features` jobs — stay on `ubuntu-latest` only (not in issue scope) | [multi-platform-ci spec](../plans/done/2026-05-07-multi-platform-ci.spec.md) |  | untracked |
| Rust toolchain matrix (stable only; toolchain matrix is a separate concern) | [multi-platform-ci spec](../plans/done/2026-05-07-multi-platform-ci.spec.md) |  | untracked |
| Windows path-length / line-ending issues (file follow-up if they surface at runtime) | [multi-platform-ci spec](../plans/done/2026-05-07-multi-platform-ci.spec.md) |  | untracked |
| Changing PR #182's `RUSTC_WRAPPER` / `SCCACHE_GHA_ENABLED` env vars. | [ci-rust-cache-migration spec](../plans/done/2026-05-08-ci-rust-cache-migration.spec.md) |  | untracked |
| Workflow-level `env:` block migration (samply variant). | [ci-rust-cache-migration spec](../plans/done/2026-05-08-ci-rust-cache-migration.spec.md) |  | untracked |
| Authoring an internal composite action (Mozilla pattern). | [ci-rust-cache-migration spec](../plans/done/2026-05-08-ci-rust-cache-migration.spec.md) |  | untracked |
| Removing sccache (Mozilla "rust-cache only" pattern). | [ci-rust-cache-migration spec](../plans/done/2026-05-08-ci-rust-cache-migration.spec.md) |  | untracked |
| Touching non-compile jobs: `format`, `roadmap-sync`, and the `*-pass` aggregator jobs (`build-pass`, `test-pass`, `clippy-pass`, `features-pass`, `roadmap-sync-pass`). | [ci-rust-cache-migration spec](../plans/done/2026-05-08-ci-rust-cache-migration.spec.md) |  | untracked |
| `format` job in ci.yml — runs `cargo fmt --check` only; no compilation, sccache wouldn't help | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| `*-pass` aggregator jobs (`build-pass`, `test-pass`, `clippy-pass`, `features-pass`, `roadmap-sync-pass`) — pure `needs:`-wait jobs, no compile | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| `roadmap-sync` — runs the gen-roadmap script, no Rust compile | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| `coverage.yml` — does not gate merge; coverage instrumentation may interact with sccache in ways that need separate evaluation | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| `docs.yml` (Pages-deploy workflow) — does not gate merge; rebuilds docs only on master push | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| `base_benchmarks.yml`, `fork_pr_benchmarks_run.yml`, `fork_pr_benchmarks_track.yml` — don't gate merge; benchmark wall-time should be measured on a stable build, not a sccache-affected one | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| Self-hosted sccache backend (S3 / Azure) — overkill at this CI volume | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| Replacing `actions/cache@v5` with `Swatinem/rust-cache@v2` — separate concern, file as follow-up if desired | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| **Additional facade conveniences (re-exports, helper functions)** — added on demand as new crates land; no spec required. | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| **API stability discussion** — covered by AGENTS.md `## API Stability` (no shims pre-crates.io). | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Path filtering for the separate workflows: `coverage.yml`, `docs.yml`, `base_benchmarks.yml`, `fork_pr_benchmarks_run.yml`, `fork_pr_benchmarks_track.yml`. Each is its own file with its own trigger; address separately if/when they become noisy. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| Removing `format` or `roadmap-sync` from instruction-only PRs. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| Adding a `Cargo.toml`-only fast-path. `Cargo.toml` changes can affect feature flags, dependency resolution, and workspace topology — safer to run the full matrix. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| Changing branch-protection settings on `origin`. The existing required contexts (`Format`, `Build`, `Test`, `Clippy`, `Docs`, `Feature matrix`) are preserved by aggregator naming. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| A `[force-ci]` / `workflow_dispatch` escape hatch for forcing the full matrix on a doc-only PR (see § Open questions). | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| Audit `spec-writer` agent: heredoc `cat > … <<EOF` writes vs. harness-native `Write` / `Edit` tools — inspect whether the omission of `Write`/`Edit` from `.claude/agents/spec-writer.md`'s `tools:` line was intentional or oversight; same question for any other agent whose tool list deliberately omits Write/Edit. | [ai-docs/learnings.md 2026-05-14 entry](../learnings.md) |  | #345 (closed) |
| Implementation of `scripts/check-instruction-file-sizes.sh` (the pre-commit / CI mechanical gate). Already tracked separately in #383. This task's audit-side check is a back-stop, not a replacement. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Rewriting the AXIOM body in `AGENTS.md § Build & Test`. The AXIOM stays where it is; `agent-writing-style.md` Pattern 8 references it as the source-of-truth via `AGENTS.md § Build & Test`. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Editing `AGENTS.md` for this task. The Round-1 decision to land the AXIOM as Pattern 8 means no new propagation-rule row is needed in AGENTS.md (the existing `agent-writing-style.md` propagation row covers fan-out via `## Propagation rule for new patterns` in the style guide). | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Changing the severity bands (35k / 40k) or the covered file set — both inherited verbatim from the AGENTS.md AXIOM. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Generalising Sub-check 10's heading-derived coverage map to other style references (e.g., `code-style.md`, `doc-convention.md`). | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Adding a Rule-5 substring blacklist entry to `.claude/agents/spec-writer.md`. The char-cap rule is a structural property of files, not a question-time pre-resolved rule. | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Mechanical pre-commit / CI gate for instruction-file size enforcement — tracked separately in #383. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | #383 |
| Substantive rewriting of extracted content — extraction is a move + stub, not a content edit; binding semantics preserved verbatim. Editorial micro-tightening permitted only when meaning-preserving. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Further extraction of `AGENTS.md § Code Style` — already largely cross-referenced to `ai-docs/code-style.md`; diminishing returns. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Further extraction of `AGENTS.md § Propagation Rule` — PR #376 already extracted the agent-writing-style.md row; remaining rows are mostly one-liners. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Extraction of `AGENTS.md § Build & Test` — the 40k AXIOM itself lives in that section; recursive extraction would be confusing. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Edits to `.claude/skills/**` or `.claude/agents/**` files — they have their own per-file budgets, audited independently. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Substantive Rust source changes — none expected; this PR is markdown-only. | [agents-md-extraction spec](../plans/done/2026-05-16-agents-md-extraction.spec.md) | — |
| Promoting the new lints to `level = "deny"` directly in `[workspace.lints.clippy]`. The existing `cargo clippy --workspace -- -D warnings` CLI gate continues to be the single point of escalation. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Per-crate allow-list refinement (vs workspace-level uniformity). v1 picks workspace-level for every allow; per-crate split is a follow-up only if a single crate consistently violates a lint that's healthy elsewhere. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| The `clippy::restriction` lint group. Project-style restrictions already live in `panic-index.md` discipline + AGENTS.md *Library safety idioms*. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| New CI workflow file. The existing `ci.yml` clippy job picks up `[workspace.lints]` automatically — no workflow edit needed. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Rust MSRV change. The workspace MSRV is already `1.95` (verified in root `Cargo.toml`), well above the `[workspace.lints]` requirement of Cargo 1.74. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Tightening the size-aware thresholds below the 512 KiB clippy default. v1 keeps defaults; threshold tuning is a deferred follow-up if first-run data warrants. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Cleaning up pre-existing per-crate `#![warn(clippy::missing_errors_doc)]` / `#![warn(clippy::missing_panics_doc)]` / `#![warn(clippy::doc_markdown)]` / `#![warn(clippy::undocumented_unsafe_blocks)]` attributes in `lib.rs` files that become redundant under `pedantic = warn`. They become no-ops but are harmless; removal is a separate follow-up. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Enforcement of the new lints on the `cargo build -p quartzite --no-default-features --features libm` derive-free path. AGENTS.md *Build & Test* only requires `cargo clippy --workspace -- -D warnings` on the default-feature build; the no-default-features path remains a build-only gate. | [tighten-clippy-pedantic-nursery spec](../plans/done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | — |
| Widening clippy to other workflow files (`base_benchmarks.yml`, `docs.yml`, `coverage.yml`, `miri.yml`). The Docs and benchmark workflows already cover their target kinds. | [ci-clippy-bench-targets spec](../plans/done/2026-05-18-ci-clippy-bench-targets.spec.md) | — |
| Adding `--all-features` to the clippy CI step. Orthogonal — feature-gated code paths are not currently failing. | [ci-clippy-bench-targets spec](../plans/done/2026-05-18-ci-clippy-bench-targets.spec.md) | — |
| Adding new workspace lints or changing `[workspace.lints.*]` beyond what this task introduced. | [ci-clippy-bench-targets spec](../plans/done/2026-05-18-ci-clippy-bench-targets.spec.md) | — |
| Adding new lints to `[workspace.lints.clippy]`. This is severity-only escalation of three already-declared entries. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Touching the 16-entry workspace `allow`-list. Allows stay allows. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Removing `-D warnings` from the CI clippy invocation. The CLI gate stays as belt-and-braces regardless of declaration-level severity. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Escalating the `pedantic` or `nursery` lint groups. Issue #464 classifies these as "Weak" and "Don't" respectively; `nursery` in particular is a footgun across toolchain bumps. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Per-pedantic-lint promotion (Option B in the issue). Higher effort, separate judgement call, deferred. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| Touching `[workspace.lints.rust]` / `[workspace.lints.rustdoc]` — both already at `deny`. | [escalate-clippy-warns-deny spec](../plans/done/2026-05-18-escalate-clippy-warns-deny.spec.md) | — |
| `bitflags!` / `enumflags2` collapse of `WidgetBase` bool flags into `BitFlags<WidgetState>`. Issue #316 already tracks the design call; out of scope from `struct_excessive_bools` audit which only decides Keep/Narrow/Fix on the allow. | [audit-workspace-clippy-allows spec](../plans/done/2026-05-18-audit-workspace-clippy-allows.spec.md) | #316 |
| Code changes to `quartzite-*` crates — this issue only touches the instruction surface and agent / skill files. | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| Schema migration of existing `learnings.md` entries — they remain `Kind: correction` implicitly per the default-when-omitted rule. No backfill commit. (The worked-example carve-out is the single named exception.) | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| Auto-memory write-back from project agents to `~/.claude/projects/.../memory/*` — Phase 5 only reads auto-memory as a companion signal; no writes back to the user-local layer. | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| A separate `ai-docs/patterns.md` index file — Q3 resolved to Inline Patterns in the most-local file; no central index is created. | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| Heuristic prose-based legacy-entry detection in `self-improve.md` — Q3's Option B (Option 2 from the worked-example carve-out fallback list) is rejected in favour of the surgical one-off retro-add. | [carrot-pass-improve-ai-audit spec](../plans/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | — |
| The `scripts/check-instruction-file-sizes.sh` pre-commit / CI gate (AGENTS.md mentions this as future work; tracked separately). | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| AGENTS.md + `ai-docs/context.md` size squeeze — tracked in #497. | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| Any change to skill workflow behaviour. Pure refactor; no semantic deltas. | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| Re-extracting `.claude/agents/*.md` files (issue scope is `.claude/skills/*/SKILL.md` only). | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| Adding new content to any SKILL.md — net-additive edits land in separate PRs. | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| Landing `scripts/check-instruction-file-sizes.sh` itself (deferred, tracked in #383). This task only updates the pre-merge design's reference target so the future gate reads the new index file. | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | #383 |
| Auditing `.claude/agents/*.md` files for the same size pattern (separately deferred via #498 spec § Out of scope). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Re-extracting more content from any SKILL.md (independent extraction work; #501 closed that work for now). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Renaming, restructuring, or moving any of the six exempted SKILL.md files. | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Adding or removing entries from `ai-docs/panic-index.md` or `ai-docs/unsafe-index.md` (they are precedent, not part of this task). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| Refactoring any of the 5 lock-acquire-and-match patterns to release the guard before the body runs. The workspace comment at root `Cargo.toml:47` records the original intent ("deliberately held for atomicity"); the design phase reaffirms it but does not rewrite the locking discipline. | [clippy-narrow-significant-drop-allows spec](../plans/done/2026-05-20-clippy-narrow-significant-drop-allows.spec.md) | — |
| Touching any other workspace allow in `[workspace.lints.clippy]`. The remaining 5 entries (`must_use_candidate`, `redundant_pub_crate`, `return_self_not_must_use`, `type_complexity`, etc.) are unrelated. | [clippy-narrow-significant-drop-allows spec](../plans/done/2026-05-20-clippy-narrow-significant-drop-allows.spec.md) | — |
| Changing the workspace lint level for `significant_drop_in_scrutinee` (it inherits from `clippy::nursery = warn` once the explicit `allow` is dropped, escalated to deny by the CI `-D warnings` invocation — exactly the desired posture). | [clippy-narrow-significant-drop-allows spec](../plans/done/2026-05-20-clippy-narrow-significant-drop-allows.spec.md) | — |
| Refactoring the test fixtures themselves (e.g., replacing the multi-tuple returns with builder structs that own the channels). The fixtures are short-lived test helpers; the lint is the only concern. | [clippy-narrow-type-complexity spec](../plans/done/2026-05-20-clippy-narrow-type-complexity.spec.md) | — |
| Touching any other workspace allow in `[workspace.lints.clippy]`. The remaining 3 entries (`must_use_candidate`, `redundant_pub_crate`, `return_self_not_must_use`) are unrelated and out of scope here. | [clippy-narrow-type-complexity spec](../plans/done/2026-05-20-clippy-narrow-type-complexity.spec.md) | — |
| Changing the workspace lint level for `clippy::type_complexity` (it inherits from `clippy::all = warn` once the explicit `allow` is dropped, escalated to deny by CI `-D warnings` — the desired posture). | [clippy-narrow-type-complexity spec](../plans/done/2026-05-20-clippy-narrow-type-complexity.spec.md) | — |
| Extending the `object_tree.rs` type alias to the `wrapped_handler.rs` sites — different concrete `Mutex<…>` inner types (`Vec<(String, Vec<Value>)>` vs `Vec<Size>`/`u32`); sharing would force an over-generic alias. | [clippy-narrow-type-complexity spec](../plans/done/2026-05-20-clippy-narrow-type-complexity.spec.md) | — |
| Resolving the issue body's wording that lumps all 5 sites under "Sites" without distinguishing the runtime trio (shared shape) from the renderer pair (one-offs). The spec records the distinction in § Scope; no follow-up needed. | [clippy-narrow-type-complexity spec](../plans/done/2026-05-20-clippy-narrow-type-complexity.spec.md) | — |
| Sharing the ast-index DB via git or any other transport — DB is host-local by design. | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| A long-lived `ast-index watch` daemon as the primary sync mechanism (issue explicitly rejects this). | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| Changes to `ast-index` itself or to the `ast-index:ast-index` skill. | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| Touching `.claude/settings.local.json` entries that are NOT specific-argument `rg` / `grep` invocations (other tools like `python3 -c`, `curl`, `xargs`, etc. remain as they are — out of scope for this PR even though they are similarly redundant). | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| `**No change**` to `design-system/SKILL.md` (`name: quartzite-design`) at the repo root. It remains the canonical pointer-only manifest, non-slash-discoverable. | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| `**No change**` to the body of design-system content (`design-system/README.md`, `design-system/colors_and_type.css`, `design-system/preview/`, `design-system/ui_kits/widgets/`). | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| `**No change**` to other skills' `disable-model-invocation` values. | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| `**No change**` to `.claude/agents/design.md` / `.claude/agents/design-review.md` Read-before-designing bullets (they already point at `design-system/SKILL.md` + `README.md` directly; they do not depend on `/ui-design` being model-invocable). | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| `**No change**` to `CLAUDE.md` § *Design system* — its trigger list is the same content as AGENTS.md (CLAUDE.md `@AGENTS.md`-imports AGENTS.md), so the AGENTS.md edit propagates implicitly. | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| Issue #402 (palette state groups, `FocusRing` role, derived `Hover` / `Pressed` cells, `ColorGroup` axis, `Palette::color(role, group)` signature change, `with_role_all_groups`, `DefaultStyle::draw_button` refactor to use the matrix, snapshot regeneration of `button_hovered.png` / `button_pressed.png` / dark siblings, doctests for the new signatures). Per the round-1 answer the user will implement #402 in its own task session. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Adding new `Color` constants to `quartzite-paint-api/src/color.rs`. The eight dark-theme constants (`MINE_SHAFT`, `MERCURY`, `ECLIPSE`, `NERO`, `DODGER_BLUE`, `LIGHT_DODGER_BLUE`, `CHAROITE`, `PASTEL_RED`) are already present (lines 67–89). No new constants are required by any present design-system value. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Adding documentary state-cell hex constants (`HOVER_WHITE` = `#F0F0F0`, `PRESSED_SKY_BLUE` = `#006CD6`, etc.) to `Color`. These belong to the #402 derivation machinery; the design-system explicitly notes (`palette-state-groups.proposal.md` § *Naming convention*) that they are documentation-only and computed by the `Palette` constructor. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Changing the `Color::blend` derivation factors (6 % / 16 %). The design-system narrates these but the derivation only fires once #402 lands. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Regenerating any snapshot PNGs. No paint-time value changes here → no snapshot drift. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Editing any file under `design-system/`. The design-system folder is the source of truth; this spec syncs code to it, never the reverse. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Refactoring `DefaultStyle::paint::<Button>`'s 25 %-blend hover (`default_style.rs:92`) or its direct `ColorRole::Highlight` focus-ring usage. Both belong to #402. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| Updating the `text-edit-read-only-overlay` proposal's alphas. Already implemented and matches the proposal exactly. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| **Mechanical regression gate.** The issue body explicitly excludes this: the classification is semantic ("does this help a reader?") and a regex either over-matches (every inline `//` becomes a finding) or under-matches (no token reliably identifies "scene-setting"). Human review is the gate; the script gate from #336 (PR #516) already catches the prose-side leak, which IS mechanisable. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **Re-opening the prose-side rule from #336.** The Pattern A / Pattern B audits from #336 cover doc-comment **prose** (text outside code fences). They are settled in PR #516 and remain unchanged by this issue. This issue strictly extends scope to code-fence inline comments. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **Non-doc `//` comments.** `//` comments that are not inside any doc-delivery surface (neither a `///` / `//!` line-prefixed block nor the body of a `#[doc = ...]` / `#![doc = ...]` attribute) do not reach rustdoc and are not in scope. Inline `//` comments inside raw-string / string-literal doc attributes ARE in scope — see Scope §1 Pass B and Key Decisions. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **`tests/` integration files, `benches/`, `#[cfg(test)]` modules, `quartzite-test-helpers/src/**`.** Same surface exclusions as #336 — not part of the published rustdoc. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **Fence content other than `// …` comments.** Use statements, function bodies, struct definitions, asserts, etc. inside code fences are not in scope — the sweep specifically targets lines that begin (after `///` / `//!` and optional whitespace) with `//`. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **Renaming or restructuring code-fence examples.** Only the in-fence inline `//` comments change; surrounding API-use code stays as-is unless an inline-comment edit makes a structural rewrite trivially better (judgement call by design). | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| Adopting additional rule files (this PR is purely the integration pass; new rule content lands in its own PR). | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| Landing `scripts/check-instruction-file-sizes.sh` (#383) itself. | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| Auto-loading rule files into the per-invocation prompt — would invalidate Q-A's rationale; explicit non-goal. | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| Folding this work into PR #521 (rationale: blast-radius isolation + each PR keeps its ACs verifiable; the issue body's "Why not folded into PR #521" section captures this). | [integrate-claude-rules-dir spec](../plans/done/2026-05-21-integrate-claude-rules-dir.spec.md) | — |
| `Container` hover / pressed / focused rendering — `Container` is a grouping widget with no inherent interactive surface; defer to a separate issue if interactive-container semantics ever land | [hover-focused-label-textedit-scroll spec](../plans/done/2026-05-21-hover-focused-label-textedit-scroll.spec.md) | — |
| Adding new `ColorGroup` variants beyond `Normal` / `Hover` / `Pressed` — `Disabled` and `Focused` remain orthogonal modifiers per #402 | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Adding new `ColorRole` variants — `Base` / `Text` / `Highlight` / `HighlightedText` / `FocusRing` are all that the spec needs | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Event plumbing (renderer → widget tree) — hit-testing, `MouseEnter`/`MouseLeave` synthesis, focus traversal, per-widget event routing are owned by the prior input-plumbing pass | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Cursor-shape changes on hover — tracked under #404 | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| LineEdit caret / selection / scroll-on-overflow rendering — current impl paints the raw `text` string; caret and selection are not yet plumbed | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| `Container` hover / pressed / focused rendering — `Container` is a grouping widget with no inherent interactive surface; not within this issue's scope | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| `Style` / `Paint` trait surface redesign — per the #403 precedent, per-widget state-awareness reduces to a one-line `state_group` selector plus two-axis palette lookups inside the `Paint<W>` impl; if a future design phase finds a trait-level pain point it returns NO-GO with a redesign proposal | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Re-aligning Button / Label / TextEdit / ScrollArea paint impls — those already ship the state-aware idiom this spec mirrors | [lineedit-hover-pressed-focused spec](../plans/done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | — |
| Adding any new shell scripts or CI gates (explicitly rejected by user direction 2026-05-22). | [reframe-size-cap-axiom spec](../plans/done/2026-05-22-reframe-size-cap-axiom.spec.md) | — |
| Actually shrinking files currently above 35k / 40k chars — that is `/ai-audit`'s job once its widened scan starts firing against the new file set. | [reframe-size-cap-axiom spec](../plans/done/2026-05-22-reframe-size-cap-axiom.spec.md) | — |
| Refactoring the AXIOM into a non-AXIOM (the blockquote / table format stays; only the severity wording and covered-set change). | [reframe-size-cap-axiom spec](../plans/done/2026-05-22-reframe-size-cap-axiom.spec.md) | — |
| Repointing the rule-of-truth from `ai-docs/agent-writing-style.md § 8` to a new location. | [reframe-size-cap-axiom spec](../plans/done/2026-05-22-reframe-size-cap-axiom.spec.md) | — |
| Phase 2 of the source rewrite plan itself (rewriting procedural skills under the methodology) — explicit deferral remains. | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Editing skills / agents that could later cite the new reference doc (`task`, `project-review`, `context-reset`, `spec-writer`, `triage-runner`). Listed as future candidates only. | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Re-running v5 validation on Phase 1 files — handled by `/improve` or `/ai-audit` if they naturally fire. | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Adding an `ai-docs/plans/INDEX.md` row — meta-plans don't get INDEX rows (precedent: `process-improvements.md`). | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Rewriting `done/**` artefacts to update the rename spec's `--exclude=` list — `done/` is archival. | [extract-instruction-rewrite-methodology spec](../plans/done/2026-05-22-extract-instruction-rewrite-methodology.spec.md) | — |
| Switching toolchain action / cache action — orthogonal; the swap to `setup-rust-toolchain@v1` is the upstream cause of today's uniformity. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Adding `-D <new-lint>` flags beyond the `-D warnings` baseline — that's a separate code-quality discussion, not a strictness-uniformity audit. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Changing branch-protection / required-status-checks. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Substantive code changes to fix latent warnings — none surfaced; nothing to fix in production code. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Removing the `clippy` job's explicit `-- -D warnings` lint flag — it remains belt-and-braces; the guard does not require either parity direction for the lint flag. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Migrating the guard to a Rust binary, pre-commit hook, or `.claude/rules/` rule file — bash script with PR-CI wiring matches the precedent (`check-rustdoc-internal-refs.sh`) and is sufficient. | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Renaming any OTHER currently-clashing project-defined name beyond `verify` — the AC16 AXIOM is forward-looking; `verify` is the only incidental fix this PR applies. If `/ai-audit` Checklist O surfaces additional clashes after merge, they get their own task. | [harness-taxonomy-unify spec](../plans/done/2026-05-23-harness-taxonomy-unify.spec.md) | — |
| Renaming any project-defined Subagent / Skill / Hook — Embedded-name clash AXIOM in `AGENTS.md § Propagation Rule` already covers naming conflicts; this task is the inverse direction (adopting embedded primitives where project ones duplicate them). | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Replacing `ast-index` usage with anything — `ast-index` is a marketplace plugin §3b, not a simulation candidate; it remains the project's preferred search primitive. | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Behavioural rewrite of any Class-E-audited Subagent (`design`, `self-review`, `review-findings`, `triage-runner`, `learnings-escalation-audit`, `self-improve`) — Class E is audit-only per the round-3 promotion; Class E findings recorded zero 🟡 candidates (5 ✅ keep, 1 ⛔ inline mandatory for `self-improve`'s missing `Agent` primitive). | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Re-architecture of any Subagent's overall purpose (migrating to embedded `Plan` / `general-purpose` wholesale) — re-architecture is not propagation; out of scope by user's "Docs+search+gap" answer to Q1. | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Replacing `triage-runner` Phase 4.5's bounded one-file `Read` with `Explore` — too narrow for the spawn-cost trade-off; same rationale that keeps `Read` on `design-review` / `spec-writer` in Class C; reaffirmed by Class E entry E4. | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Touching `.claude/settings.local.json` — user-local state per `/ai-audit` Step 2.2. | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| Re-fetching live `code.claude.com` content during this task to verify URLs — the three URLs in Class A1 are the ones currently embedded in `/ai-audit`; if any 404 in practice, `claude-code-guide` surfaces that and the failure is treated as a docs-publisher change, not a regression of this task. | [propagate-embedded-tools spec](../plans/done/2026-05-23-propagate-embedded-tools.spec.md) | — |
| An event-dispatch loop / routing of `MouseEvent` to `WidgetExt::on_mouse_press` etc. This spec covers the **hit-test traversal primitive only**; wiring it into `WidgetRoot::on_mouse_press` / `quartzite-renderer`'s window dispatch is a separate follow-up (the "event-dispatch design beyond what `WidgetRoot::on_mouse_press` does today" the source spec § Deferred names). New issue when the dispatch loop lands. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Mouse-capture / grab semantics (a widget capturing all subsequent events until release). Future input-plumbing concern. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Hover / enter / leave tracking and cursor-shape changes. Tracked separately (#404 cursor-shape, hover-state items in the backlog). | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Z-order overrides / `raise()` / `lower()` / explicit stacking beyond child-iteration order. v1 reverse-z-order is purely the inverse of paint iteration order; no per-widget z-index field exists. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Overlapping-sibling tie-breaking beyond "later-painted sibling wins." Containers lay children out without overlap today; the reverse-iteration rule is the complete tie-break. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Per-widget custom hit shapes (non-rectangular hit regions, e.g. circular buttons). v1 hit-tests against the rectangular `geometry()`; a `WidgetExt::contains_point`-style override is a future concern. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Transforming the result into screen/global coordinates. The function works in root-local space; callers convert if they need global coordinates. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| Multi-window hit-testing. Each call hit-tests one tree, exactly like `dispatch_paint` walks one tree. | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |

## Open questions

| Item | Source | Status | Tracked |
|------|--------|--------|---------|
| **Bench split**: accepted — tree-lookup benches live in `quartzite-runtime/benches/` to avoid circular dep. | [criterion-benchmarks design](../plans/done/2026-05-07-criterion-benchmarks.design.md) |  | untracked |
| **`find_by_path`**: bench `find_by_name` + `find_by_name_in` as-is; no new API needed. | [criterion-benchmarks design](../plans/done/2026-05-07-criterion-benchmarks.design.md) |  | untracked |
| Are there any branch-protection required-status-check rules referencing the bare job names `build`, `test`, or `clippy`? If so, they must be updated to the matrix-suffixed names (e.g. `build (ubuntu-latest)`) after this PR merges. Check the repository settings before merging. | [multi-platform-ci design](../plans/done/2026-05-07-multi-platform-ci.design.md) |  | untracked |
| **Cache contention with existing 10 GB GHA repo cache** — current cargo cache + sccache cache + benchmarks cache + coverage cache may collectively exceed the GHA repo limit, causing eviction churn that erodes the win. Worth observing post-merge: if cache hit rates show high churn, tune `SCCACHE_CACHE_SIZE` per OS. | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| **Does sccache benefit `cargo clippy`?** — clippy runs the full type-check / borrow-check pipeline; sccache caches up to and including codegen. Clippy may not actually drive codegen, in which case sccache hit rate on the clippy job will be lower than on build/test. Worth observing in stats. | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| **`cargo doc` interaction** — `cargo doc` invokes `rustdoc`, not `rustc` directly. sccache's wrapping is on `rustc` via `RUSTC_WRAPPER`. doc may bypass sccache entirely. Acceptable if so — `docs` job is shorter than build/test anyway. | [ci-sccache spec](../plans/done/2026-05-08-ci-sccache.spec.md) |  | untracked |
| Exact prose wording inside the `lib.rs` overview / per-concept-section bodies, provided each section has the structure specified (short prose + small example + sub-crate deep-link). | [project-docs design](../plans/done/2026-05-08-project-docs.design.md) |  | untracked |
| The exact 5 per-concept section heading names — reasonable variants (`# Signals` vs `# Signals and slots`, `# Object tree` vs `# Object hierarchy`) are interchangeable; pick the form that reads cleanest. | [project-docs design](../plans/done/2026-05-08-project-docs.design.md) |  | untracked |
| Per-section deep-link targets — sub-crate landing pages (`[`quartzite_core::signal`]`) vs. specific item pages (`[`quartzite_core::Signal`]`) — implementer picks based on what produces the most useful rustdoc landing for a reader who clicks through. | [project-docs design](../plans/done/2026-05-08-project-docs.design.md) |  | untracked |
| The exact `actions/checkout` major version pin in the new `roadmap-sync` job — query the registry per AGENTS.md `## Dependency Versions` rule. | [project-docs design](../plans/done/2026-05-08-project-docs.design.md) |  | untracked |
| Exact wording / structure of the project-description block (3–5 paragraphs vs. tight tagline + bullet list). | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Exact `lib.rs` quickstart content shape — likely a counter-style example with one signal/slot pair, or a config-style example with one property. Either fits `no_run` and the conceptual focus. | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Whether the CI sync-gate runs on all OS matrix entries or `ubuntu-latest` only (depends on bash-on-Windows behavior of the generator's `awk`/`sed` invocations). | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Exact set of AGENTS.md bullets to excerpt into `CONTRIBUTING.md` (a "high-leverage" judgment call per the standard-depth choice). | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| Generator format choices for `ROADMAP.md`: how literally to mirror `INDEX.md` vs. how much to distill / re-narrate. | [project-docs spec](../plans/done/2026-05-08-project-docs.spec.md) |  | untracked |
| **Force-rerun escape hatch** (carried from spec § Open questions). Default position is "no" — contributors who want to validate the matrix against an instruction change can include a trivial whitespace edit to a `.rs` file. If this proves friction-creating in practice, open a follow-up issue to add a `[force-ci]` PR-title token or a `workflow_dispatch` trigger; not part of this design. | [ci-skip-rust-matrix design](../plans/done/2026-05-09-ci-skip-rust-matrix.design.md) |  | untracked |
| **Filter contract documentation.** Should the path-filter list carry an inline YAML comment naming the contract ("any future Rust artefact must be added here in the same PR that introduces it")? Recommendation: yes — one-line comment above the filter is cheap insurance against silent drift. Defer to implementer / reviewer if they prefer to keep the YAML uncommented. | [ci-skip-rust-matrix design](../plans/done/2026-05-09-ci-skip-rust-matrix.design.md) |  | untracked |
| **Force-rerun escape hatch**: should there be an explicit way to force the full matrix on a doc-only PR (e.g. `[force-ci]` PR title token, or `workflow_dispatch`)? Default position: no — if a contributor wants to validate the matrix against an instruction change, they can include a trivial whitespace edit to a `.rs` file (which would also serve as a clear signal in the diff). Revisit if this proves friction-creating in practice. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| **`actionlint` validation of the `if:` expression syntax**: confirmed expressible as `if: needs.changes.outputs.rust == 'true'` per GHA docs; design phase will verify against the live `actionlint` rule set. | [ci-skip-rust-matrix spec](../plans/done/2026-05-09-ci-skip-rust-matrix.spec.md) |  | untracked |
| **Should the sanity-net stderr warning also fire when `${PR_NUM}` IS empty but a `.progress.md` whose slug matches the branch exists?** The current `${PR_NUM}` empty path prints `pr-merged: no merged PR found for <branch>; skipping progress-file cleanup.` and exits 0. Extending the orphan-check to that path would catch the "branch merged outside `gh`" case as well. Design's call — recorded here as a candidate refinement, not an AC. | [cleanup-progress-issue-derive spec](../plans/done/2026-05-13-cleanup-progress-issue-derive.spec.md) |  | untracked |
| **Should the issue body keyword list include the conjugated forms (`Closed`, `Fixed`, `Resolved`)?** GitHub's auto-close parser accepts them, but `/task` PR bodies in this repo consistently use the bare imperative (`Closes #N`). Design may choose to over-cover (cheap) or stay minimal (simpler regex). | [cleanup-progress-issue-derive spec](../plans/done/2026-05-13-cleanup-progress-issue-derive.spec.md) |  | untracked |
| Whether `coverage.yml` needs an explicit `components: llvm-tools-preview` after the swap — settled by design (added explicitly). | [macos-cargo-path-pin spec](../plans/done/2026-05-14-macos-cargo-path-pin.spec.md) |  | untracked |
| Concrete small-fix LOC ceiling for latent-warning fallout (Scope item 5) — settled by design at 30 LOC. | [macos-cargo-path-pin spec](../plans/done/2026-05-14-macos-cargo-path-pin.spec.md) |  | untracked |
| Whether the bundled cache's segment-based restore behaviour fully reproduces today's `restore-keys:` partial-prefix semantics — settled by design (yes, natively; verified against `Swatinem/rust-cache/src/{config,restore}.ts`). | [macos-cargo-path-pin spec](../plans/done/2026-05-14-macos-cargo-path-pin.spec.md) |  | untracked |
| Exact wording of the "Compaction recovery check" callout — design drafted; revisit if a user-visible phrase becomes load-bearing post-merge. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Whether `decisions_log` lines should also be echoed to the user at each step boundary (drift-spotting) or stay write-only — design picked the lighter touch first. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Whether `last_passed_gate` is one field or one per gate (build / clippy / fmt / doc) — design defaulted to the single-field "most recent passed gate" formulation. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Anti-patterns content for the new `.claude/skills/task/reference.md` — design owns the concrete list; not blocking spec. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Section heading style for the `/bugfix` trace-file extensions (h2 vs inline `**Field:**`) — design picked the shape consistent with the existing trace template. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| `/context-reset` heading style for the two triggers (single SKILL.md with two sub-headings vs front-matter dispatch) — design picked two-sub-heading variant. | [sonnet-skill-reentry-protocol spec](../plans/done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) |  | untracked |
| Representation of the handoff-grouping plan in `design.md`'s artifact — three candidate shapes (separate `## Handoff plan` h2, new `Group` column on the Decomposition table, in-table header-row separators). Design (Step 6) picked Option 1; recorded as a resolved-at-design open question. | [design-handoff-groups spec](../plans/done/2026-05-16-design-handoff-groups.spec.md) |  | untracked |
| Should Pattern 8 cross-reference the deferred `scripts/check-instruction-file-sizes.sh` (issue #383)? — Resolved at design phase: adopted the spec's default (one-line forward reference: "Mechanical pre-commit gate planned in #383; this audit-side back-stop fires in the meantime."). | [ai-audit-charcap-axiom spec](../plans/done/2026-05-16-ai-audit-charcap-axiom.spec.md) | — |
| Whether `interview/SKILL.md` (227 lines, 12,127 chars) and `bugfix/SKILL.md` (257 lines, 14,095 chars) genuinely benefit from extraction or are best left intact with documented exemptions. Both are already under the 14k-char marker and only marginally over the 200-line target; design agent decides per-skill whether net-positive readability is achievable. | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| Whether the `ai-audit/SKILL.md` Phase 2 checklist (the largest single section at 27,878 chars) can be cleanly extracted to `reference.md` while keeping Phase 2 entry navigable from `SKILL.md`. The checklist IS the audit, but each sub-check (A–N + sub-checks 9–10) is independently referenceable. Design agent assesses. | [extract-oversized-skill-references spec](../plans/done/2026-05-20-extract-oversized-skill-references.spec.md) | — |
| **`interview/SKILL.md` extractability** — answered: NO extraction (category map: cat-(2) State file YAML template + cat-(3) Steps 1–4 + Round/question caps; no cat-(1) content). Documented exemption inline (subtask 5). 27 lines over the 200-line target and 12,127 chars (well under the 14k marker the spec cites). | [extract-oversized-skill-references design](../plans/done/2026-05-20-extract-oversized-skill-references.design.md) | — |
| **`bugfix/SKILL.md` extractability** — answered: NO extraction (category map: cat-(2) Step 1 trace-file template + cat-(3) Steps 2–7 + cat-(3) Step 6.5 self-review prompt block; no cat-(1) content). Documented exemption inline (subtask 5). 57 lines over the 200-line target and 14,095 chars (just over the 14k marker). | [extract-oversized-skill-references design](../plans/done/2026-05-20-extract-oversized-skill-references.design.md) | — |
| **`ai-audit/SKILL.md` Phase 2 Checklist A–N extractability** — answered: YES, extract (category map: cat-(1) Checklist A–N detail bodies — Sub-checks 9/10 are nested inside Checklist M, no separate range — + Step 2.6 sub-step 4 bash all move; cat-(3) Step 2.3 collapsed table + workflow narrative stays inline). SKILL retains a collapsed table of {letter, one-line purpose, anchor pointer} so Step 2.3 navigability survives the move. The SKILL → reference handoff is a single hop (AC4-conformant). Estimated post-extraction line count: ~171; defensive exemption added only if Step 2.3 collapsed-table inflates the body past 200 lines. | [extract-oversized-skill-references design](../plans/done/2026-05-20-extract-oversized-skill-references.design.md) | — |
| **AGENTS.md § Agent Docs symmetry with `panic-index.md` / `unsafe-index.md`.** Adding only `skill-size-exemptions.md` (and not the older two) leaves the symmetry question that `ai-docs/deferred/_inbox.md` already tracks (unsafe-index spec, open-question row). This spec does not resolve that — the symmetry follow-up either adds both older rows in a later edit or removes the new row to keep all three consistent. Default chosen here: add the new row since the index is actively consumed by Checklist K (panic-index / unsafe-index are passive documentation). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| **Drift-finding output format.** The exact wording of the audit finding (e.g., does it print both numbers, suggest the fix, link to the SKILL?) is a presentation choice the design agent decides. The spec only fixes severity (`minor`) and trigger (any line-count mismatch). | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| **Should the index also track `wc -c` drift?** The 40k-char-cap AXIOM is the actual budgetary concern; line count is a proxy. Spec records both numbers per entry (Key decisions row 6) but Checklist K item 1's drift detection compares line count only. Char drift detection is a possible follow-up if it adds signal; the chars value in the entry serves as audit-time documentation regardless. | [extract-skill-size-exemption-index spec](../plans/done/2026-05-20-extract-skill-size-exemption-index.spec.md) | — |
| (Non-blocking, design-only) Confirm `Bash(ast-index *)` space-form is accepted by the current Claude Code permissions schema before commit. The issue Notes flags this as a verification step, not a spec question. Fallback to the colon-form `Bash(ast-index:*)` if the schema rejects space-form; if the fallback fires, apply the same form to `grep` and `rg` wildcards too for consistency. | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| (Non-blocking, design-only) Whether `scripts/update-ast-index.sh` should additionally prune stale entries for files removed since the last `update` — current `ast-index update` semantics already handle this; flagged here only to confirm during design. | [ast-index-allowlist-and-hook spec](../plans/done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | — |
| The exact wording of the new `description:` field (which is design-authored, not spec-authored). The spec fixes only the set of keywords that must appear (AC2); design chooses the prose. | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| The exact wording of the new `quartzite-paint-api` trigger bullet for AGENTS.md and the SKILL.md body. Design-authored, constrained by AC3 + AC4 to match each other. | [ui-design-model-invocable spec](../plans/done/2026-05-20-ui-design-model-invocable.spec.md) | — |
| **Description-field wording** — fixed in Approach § *Description-field wording strategy* above; AC2 keyword set verified. | [ui-design-model-invocable design](../plans/done/2026-05-20-ui-design-model-invocable.design.md) | — |
| **5th trigger bullet wording** — fixed in Approach § *5th trigger bullet wording* above; the same wording is reused by Subtask 1 (SKILL.md body) and Subtask 2 (AGENTS.md). | [ui-design-model-invocable design](../plans/done/2026-05-20-ui-design-model-invocable.design.md) | — |
| **Whether to also strip the stale `palette-state-groups.proposal.md` cross-link from any future Rust doc comments.** No such cross-link exists today, so nothing to do — but if #402's implementation work re-introduces one and the proposal is later removed from `design-system/` (it currently survives), a similar drift will recur. Left for the #402 task to handle in its own spec. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| **Whether `design-system/preview/dark-palette-compare.html` should be the doc-comment reference instead of `README.md § Dark theme`.** Both are extant; the README is canonical narration, the HTML is the rendered swatch. README is the better citation for "the values come from here" (it spells them out); the HTML is the better citation for "look here to see them". README chosen for the spec's `Key decisions` row; reviewer may flip during design. | [design-system-code-sync spec](../plans/done/2026-05-21-design-system-code-sync.spec.md) | — |
| **Should the also-stale `colors_and_type.css` mentions in `AGENTS.md` line 13, `.claude/agents/design.md` line 15, `.claude/agents/design-review.md` lines 26/34, and `design-system/SKILL.md` lines 7/15 be cleaned up in a follow-up PR?** Out of scope here; spec explicitly limits the fix to `dark_palette.rs:6`. Recording the question so it can be tracked as a separate issue. Note: `design-system/SKILL.md`, `design-system/README.md`, `design-system/proposals/text-edit-read-only-overlay.md`, and `design-system/fonts/README.md` are all design-system-internal and would need a parallel `design-system/` PR (the design-system folder is the source of truth; `design-system/palette-state-groups.proposal.md` still cites `colors_and_type.css` because the proposal predates the file's removal — may want to update the proposal too, or close it as superseded once #402 designs the replacement). | [design-system-code-sync design](../plans/done/2026-05-21-design-system-code-sync.design.md) | — |
| **Confirm `design-system/README.md § Dark theme` over `design-system/preview/dark-palette-compare.html` as the doc-comment citation.** Spec § Key decisions row 2 already chose README; spec § Open questions row 2 left it as a reviewer flip. Going with README per Key decisions unless the reviewer flips it during design-review. | [design-system-code-sync design](../plans/done/2026-05-21-design-system-code-sync.design.md) | — |
| Whether the doc-convention codification (AC8) lands as a third family entry inside the existing § *Self-sufficiency: no repo-internal references* subsection (Family A — internal-artefact citations; Family B — contributor-tooling instructions; new Family C — code-fence inline contributor comments) or as a sibling subsection with its own anchor. Both are discoverable; design picks during the design phase. Not design-blocking. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| Exact wording of the Scope §3 prompt added to each review file (AC9). The four files have different review-step structures (`project-review/SKILL.md` checklist, `review-findings.md` agent prompts, `self-review.md` agent prompts, `design-review.md` agent prompts); design tailors the prompt per file while keeping the rule reference uniform. Not design-blocking. | [doctest-fence-inline-comments spec](../plans/done/2026-05-21-doctest-fence-inline-comments.spec.md) | — |
| **Q1 (AC8 placement — Family C entry vs sibling subsection).** Resolved at design: new Family C entry inside the existing `## Self-sufficiency` subsection (single anchor) — keeps all four review files referencing one anchor; worked example drawn from the design's per-hit table (`paint_widget.rs:20` rewrite). | [doctest-fence-inline-comments design](../plans/done/2026-05-21-doctest-fence-inline-comments.design.md) | — |
| **Q2 (AC9 wording per review file).** Resolved at design: one tailored line per file with uniform rule-reference (`ai-docs/doc-convention.md` § Self-sufficiency anchor); per-file placement parallels each file's existing Pattern A/B entry. | [doctest-fence-inline-comments design](../plans/done/2026-05-21-doctest-fence-inline-comments.design.md) | — |
| Drift-point resolution shape (close vs. annotate) for `coverage.yml`'s `cargo llvm-cov --doctests`. → Design picks; default leaning is to close (add the env block). | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Whether the guard script should also assert that the toolchain action pin (`@v1` today) is uniform across files — adjacent concern but not part of the strictness-uniformity contract; design may include if cheap, otherwise leave for #384 (which already tracks pinning policy). | [rustflags-strictness-audit spec](../plans/done/2026-05-23-rustflags-strictness-audit.spec.md) | — |
| Should the skill detect Dependabot's `compatibility-score` badge from the PR body and surface low scores (e.g., < 80) as an extra signal in the routing matrix? Default: ignore for v1; the CI signal is authoritative. Can revisit once auto-merge usage produces enough data. | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Should the skill respect a project-side allow-list of "always auto-merge" dependency names? Default: no per-dep allow-list for v1; the matrix is the only gate. Revisit if the skill produces too many pause-for-user rounds. | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Whether to open a tracking issue for this task itself (`/dependabot-pr` skill creation) before authoring the design — orchestrator decision; the spec is portable to either path. | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| Exact verdict-translation contract between `/pr-ci-failed` and `/dependabot-pr` (KD-14) — sensible defaults exist (transient → `@dependabot rebase`; real fix on the bump → `@dependabot recreate`; real regression in dep code → bail-with-issue), but the design subagent may refine the mapping based on `/pr-ci-failed`'s actual verdict vocabulary. | [dependabot-pr-skill spec](../plans/done/2026-05-27-dependabot-pr-skill.spec.md) | — |
| **Q1 (KD-14 interpretation — is the prompt-level carve-out within bounds of "do NOT modify `/pr-ci-failed` to know about Dependabot"?)** — **RESOLVED via iteration.** Round-1's "STOP after Step 4" stop-point was rejected by design-review as incoherent with `/pr-ci-failed`'s actual Step-4 control flow (Blocker 1). The chosen replacement is "EXIT between Step 3 and Step 4" — the carve-out is delivered via the spawn prompt as a GENERIC stop-point directive (not Dependabot-specific baked into the child's source). KD-14 honesty argument detailed in § Approach point 2. | [dependabot-pr-skill design](../plans/done/2026-05-27-dependabot-pr-skill.design.md) | — |
| **Q2 (`cargo build` vs `cargo test` for `lockfile-only × all-green`)** — **RESOLVED.** Design default (`cargo build` only) matches spec KD-4 literal text: *"after a local `cargo build` validates the new lockfile resolves"*. CI green covers the test surface; running `cargo test` locally before every confirm-merge would be a multi-minute delay and duplicates CI work. No product-owner input required. | [dependabot-pr-skill design](../plans/done/2026-05-27-dependabot-pr-skill.design.md) | — |
| **Q3 (child progress-file path — parent-controlled vs child's built-in fallback)** — **RESOLVED.** Design default (child's built-in path `ai-docs/ci-fixes/pr-<N>.progress.md`) is KD-14-forced: passing a parent-controlled path to the child would require modifying the child to accept a path override, which violates KD-14. Two gitignored progress files per delegated round is the accepted cost. | [dependabot-pr-skill design](../plans/done/2026-05-27-dependabot-pr-skill.design.md) | — |
| **Q4 (spec-side open questions)** — **RESOLVED (spec-deferred).** Compatibility-score badge, per-dep auto-merge allow-list, and pre-design tracking-issue decision are v2-deferred per the spec body and require no design-time action. | [dependabot-pr-skill design](../plans/done/2026-05-27-dependabot-pr-skill.design.md) | — |
| Widen the Feature-matrix job's `Build` (`ci.yml`) and `Test` steps to `--all-targets` too — #587 only added `--all-targets` to that job's `Clippy` step (which compiles all targets anyway, so the `--all-targets` build is exercised transitively). Adding it to Build/Test would *separately* exercise the all-targets build/test, at the cost of extra CI time. Out of scope for #587 (spec AC3 scoped the change to the Clippy step); revisit only if a clippy-clean-but-build/test-broken `--all-targets` regression is plausible. | [macro-doc-deny-debt design](../plans/done/2026-05-30-macro-doc-deny-debt.design.md) | untracked |
| **Overlapping-sibling semantics in practice** — today's `Container` layouts do not overlap siblings, so AC2's "later sibling wins" rule is exercised only by a synthetic test. The rule is the correct inverse of paint order regardless; revisit if an explicit z-index / `raise()` API lands (§ Out of scope). | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| **Final crate name + exact resolver home** — `quartzite-hit-test` vs `quartzite-widget-tree`, and whether the shared `WidgetResolver` lands in the hit-test crate or a still-lower widget-tree crate. Design phase picks; any choice satisfying the paint-free + single-shared-resolver constraints is acceptable (resolved in design: `quartzite-hit-test`, resolver in-crate). | [hit-testing-reverse-z-order spec](../plans/done/2026-05-30-hit-testing-reverse-z-order.spec.md) | — |
| **`Point::ZERO` does not exist in `quartzite-geometry`.** The impl uses `Point::default()`. Could optionally add a `pub const ZERO` to `Point` as a tidy-up, but that is out of this spec's scope. | [hit-testing-reverse-z-order design](../plans/done/2026-05-30-hit-testing-reverse-z-order.design.md) | — |
| **Umbrella facade exposure of `hit_test`** — should `quartzite` re-export a `hit_test` module (and under which feature)? Not required by any AC; left optional pending orchestrator confirmation (see § Risks). | [hit-testing-reverse-z-order design](../plans/done/2026-05-30-hit-testing-reverse-z-order.design.md) | — |
