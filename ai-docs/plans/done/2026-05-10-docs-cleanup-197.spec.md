# Docs cleanup post-#197 — `all-features = true` propagation, intra-doc-link audit, bare-backtick conversion, and macro-generated-doc audit

**Source:** issue #199
**Date:** 2026-05-10
**Tracked in:** #199

## Scope

Three follow-up items from PR #197 (the `--all-features` doc-gate convention), bundled into one PR because they share a root cause (intra-doc references that don't render or don't link silently when the convention is violated). The work is mostly mechanical; Part C is the substantive new audit added during implementation kickoff.

### Part A — propagate `all-features = true` to remaining `[package.metadata.docs.rs]` blocks

PR #197 updated only the umbrella `Cargo.toml` and `quartzite-core/Cargo.toml`. The remaining 11 workspace leaf crates each carry a `[package.metadata.docs.rs]` block with `rustdoc-args` / `rustc-args = ["--cfg", "docsrs"]` but **no** `features` / `all-features` line, so docs.rs builds them with no features enabled.

Update every leaf crate's `[package.metadata.docs.rs]` block to add `all-features = true`. The 11 crates and their current optional-feature exposure:

| Crate | `[features]` table today | Why the change matters |
|---|---|---|
| `quartzite-runtime` | `serde`, `verbose-tracing` | snapshot module gated on `serde`; missing on docs.rs without flag |
| `quartzite-geometry` | `std` | `std`-gated APIs missing on docs.rs without flag |
| `quartzite-events` | none | adopt convention so future feature additions are auto-included |
| `quartzite-renderer` | none | same |
| `quartzite-style` | none | same |
| `quartzite-widgets` | none | same |
| `quartzite-paint-api` | none | same |
| `quartzite-event-types` | none | same |
| `quartzite-macros` | none | same (proc-macro crate; still adopt for consistency) |
| `quartzite-style-types` | none | same |
| `quartzite-paint` | none | same |

The change is mechanical: insert `all-features = true` as the first line of every `[package.metadata.docs.rs]` block, matching the shape already present in `Cargo.toml` (umbrella) and `quartzite-core/Cargo.toml`.

### Part B — audit crate-prefixed intra-doc links for transitive-dep targets

Audit pass completed during interview (`rg '\[`?\w+`?\]\(quartzite_\w+::' --type rust`). All candidate intra-doc links target a **direct** dependency of the linking crate:

| File | Lines | Linked crate | Direct dep of linking crate? |
|---|---|---|---|
| `src/lib.rs` (umbrella) | 275, 276, 278, 279 | `quartzite_runtime` | yes |
| `src/lib.rs` (umbrella) | 300–308 | `quartzite_paint_api` | yes |
| `src/lib.rs` (umbrella) | 309 | `quartzite_geometry` | yes |
| `src/lib.rs` (umbrella) | 318–320 | `quartzite_style` | yes (optional, behind `style` feature — already fixed in #197) |
| `quartzite-renderer/src/lib.rs` | 12 | `quartzite_runtime` | yes |
| `quartzite-renderer/src/render_harness.rs` | 11 | `quartzite_runtime` | yes |
| `quartzite-style-types/src/color_role.rs` | 16 | `quartzite_paint_api` | yes |

**No cross-crate-link source conversions are required.** The single transitive-dep instance (`quartzite_style_types` from the umbrella) was already fixed in PR #197. The deliverable for Part B is captured in the combined doc-convention paragraph (see Part D below); the audit itself is the verification step.

### Part C — convert bare-backticked identifier mentions to intra-doc links

Surfaced during implementation kickoff: doc comments across the workspace contain many backticked identifier mentions in prose that are **not** wrapped as intra-doc links. Two distinct patterns:

- **Bare TitleCase identifier** — e.g., `` `Object` `` instead of `` [`Object`] ``.
- **Bare qualified path with crate prefix** — e.g., `` `quartzite_runtime::snapshot::capture_object` `` instead of `` [`quartzite_runtime::snapshot::capture_object`] ``.

Both render as inert code-style text rather than clickable cross-references. Rustdoc has no lint for "should be a link" — these can only be caught by an audit pass.

#### Inventory

**TitleCase identifiers (`lib.rs` files only — the bulk of crate-level prose):**

| File | Lines with backticked identifier in doc comment | Lines already linked | Candidate bare sites |
|---|---:|---:|---:|
| `src/lib.rs` (umbrella) | 43 | 27 | ~16 |
| `quartzite-macros/src/lib.rs` | 21 | 5 | ~16 |
| `quartzite-widgets/src/lib.rs` | 6 | 3 | ~3 |
| `quartzite-core/src/lib.rs` | 3 | 1 | ~2 |
| Other 9 lib.rs files | (small) | (small) | ~0 |
| **Total just from lib.rs** | | | **~37** |

Non-`lib.rs` files (`src/**/*.rs`) carry additional prose on individual public items; full audit surface is plausibly 50–150 candidate sites once those are included. Exact count is established by the audit step itself.

**Qualified `crate_name::path` references (subset; full pass on `src/**/*.rs`):**

| File | Bare qualified-path mentions in doc comments | Notes |
|---|---:|---|
| `quartzite-core/src/lib.rs` | 1 (`quartzite_runtime::snapshot`) | Forced bare — `quartzite-runtime` is downstream of `quartzite-core`, not a dep. Cannot be linked. |
| `quartzite-core/src/snapshot.rs` | 4 (`quartzite_runtime::snapshot::capture_*` / `restore_*`) | Same — forced bare. |
| `quartzite-core/src/meta.rs` | 2 | Same. |
| `quartzite-core/src/snapshot/tree.rs` | 3 | Same. |
| `quartzite-macros/src/lib.rs:225` | 1 (`quartzite_core`) | Forced bare — `quartzite-core` is not a direct dep of `quartzite-macros` (only `quartzite` umbrella, as dev-dep). |

In every case located so far, the qualified-path target is in a crate that is **not** a direct dep of the linking crate, so the link cannot be created — bare backticks are the only valid option. Part C's deliverable for these is therefore an explicit audit confirmation, not source edits. The convention paragraph (Part D) must call out this case so a future contributor doesn't try to "fix" them by introducing broken links.

#### Conversion criteria

| Identifier shape | Action |
|---|---|
| Bare TitleCase: type / trait / macro / fn defined in this crate | Convert to `` [`Name`] `` |
| Bare TitleCase: type from a **direct** dep | Convert to `` [`Name`](direct_dep::Name) `` if disambiguation needed; else `` [`Name`] `` if the use is unambiguous |
| Bare TitleCase: type from a **transitive / non-dep** crate | Leave bare — cannot be linked from here. Document the reason in commit message if non-obvious. |
| Bare qualified path `pkg::path::Item` where `pkg` is a direct dep | Convert to `` [`pkg::path::Item`] `` (rustdoc resolves the full path) |
| Bare qualified path `pkg::path::Item` where `pkg` is **not** a direct dep | Leave bare — link is structurally impossible (e.g., `quartzite-core` mentioning `quartzite_runtime::*`). Convention text covers this case explicitly. |
| Generic placeholder (`T`, `Self`, `K`, `V`) | Leave bare — not a real item |
| Cargo feature name (`` `serde` ``, `` `std` ``, `` `derive` ``) | Leave bare — features have no doc page |
| English noun in TitleCase (`Object` as a concept, `Cargo` the tool, `OSS`, `GUI`) | Leave bare — prose, not an identifier |
| Method or field name in TitleCase prose | Leave bare unless the parent type is in scope and the method has a doc page that the link can target (rare) |

**Verification:** `cargo doc --no-deps --workspace --all-features` exits 0 (already AC3). Any link the converter writes that fails to resolve trips `#![deny(rustdoc::broken_intra_doc_links)]` immediately. Bare candidates left intentionally are documented inline (commit message or short justification) when the choice is non-obvious.

### Part E — audit macro-generated doc strings

Surfaced after T4 (Part C) was completed and PR #200 opened: proc macros in `quartzite-macros` emit `#[doc = "..."]` attributes that become rendered docs in any user's `#[derive(Object)]` / `#[object_impl]` / `#[derive(MetaEnum)]` / `#[derive(Extend)]` types. Bare-backticked identifiers in those generated docs are the same class of issue as Part C — but invisible to a `grep` over `src/**/*.rs` doc comments because they're string literals inside `quote!` blocks.

**Why our existing doc gate doesn't catch this.** `cargo doc --no-deps --workspace --all-features` (AC10) renders the docs for the macro source, not for derived types. The macros are only expanded against test types (under `#[cfg(test)]`), which are not part of `--no-deps` rustdoc output. So a missing intra-doc link in a macro-emitted doc string is invisible to AC10 — only end users feel it when they `cargo doc` their own derived types.

#### Inventory

| File | Static `#[doc = "literal"]` sites | Dynamic `#[doc = #ident]` sites | Notes |
|---|---:|---:|---|
| `quartzite-macros/src/object/codegen.rs` | 14 | 5 (`bullet`, `example_doc` ×2, plus `#[doc(hidden)]` non-prose) | Static literals contain bare `` `Auto` ``, `` `Queued` `` (enum variants of `quartzite_core::ConnectionType`), bare `` `f` `` / `` `receiver` `` (parameter names — leave bare per criteria), and existing intra-doc links to `[`quartzite::core::ReceiverGuard`]` / `[`quartzite::core::QueuedDispatcher`]`. Dynamic `bullet` only emits `` `arg{i}` `` (parameter names — leave bare). Dynamic `example_doc` emits a ` ```no_run ` fenced block — content is compiled, not linkable; audit prose around it for any bare TitleCase. |
| `quartzite-macros/src/extend/codegen.rs` | 1 (`#[doc(hidden)]`) | 3 (`trait_doc`, `acc_doc`, `acc_mut_doc`) | Dynamic strings already use `` [`{self_name}`] `` form for the type name. Audit for any other bare identifier — none expected, but verify the format-string sources line by line. |
| Other macro codegen files (`object_part`, `meta_enum`, `object_impl`) | 0 | 0 | No `#[doc]` emission — confirmed by grep. No audit needed. |

Total static-literal sites with bare identifiers warranting audit: ~14 lines in `object/codegen.rs`. Total dynamic-string format-source sites to inspect: 4 (`example_doc` ×2 in object, `trait_doc`/`acc_doc`/`acc_mut_doc` in extend).

#### Conversion criteria

The same Part C criteria table applies, but the **link-resolution context** is the **downstream user crate** (the crate that writes `#[derive(Object)]`, etc.), not `quartzite-macros` itself. So:

- "In scope" means **reachable from the user crate** — typically through the `quartzite` umbrella (`` [`quartzite::core::ConnectionType::Auto`] ``), since users add `quartzite` (with the `derive` feature) to get the macros, and `quartzite` re-exports `core`.
- Existing links like `` [`quartzite::core::ReceiverGuard`] `` are already in this form — they resolve in the downstream crate via the `quartzite` direct dep.
- Per the criteria table, parameter names (`` `f` ``, `` `receiver` ``, `` `arg{i} ``) and the like remain bare.

#### Verification

`cargo doc --no-deps --workspace --all-features` does **not** catch missing/wrong intra-doc links in macro-emitted doc strings (the macros aren't expanded for any non-test type in this workspace). Two complementary verification approaches:

1. **Build-side check:** add a tiny doc-only smoke type that exercises the macro paths (e.g., `#[derive(Object)]` with a `#[signal]` field for both `connect` and `connect_queued` paths) under `#[cfg(any())]` or behind a test-only feature, then `cargo doc -p <crate-with-smoke>` to render the macro-emitted docs and let `#![deny(rustdoc::broken_intra_doc_links)]` validate them. **Rejected** if the smoke type would inflate test surface or the rendered output noisy — discuss in design.
2. **Source-side check:** manual review of the 14 static-literal sites + 4 dynamic-string format sources after edits; sanity-check by `cargo expand` (where available) on one existing test in `quartzite-macros/tests/`. Visual confirmation that converted links use the `quartzite::*` umbrella path.

Pick one in the design pass; the spec leaves the choice to the design.

### Part D — combined doc-convention paragraph covering Parts B, C, and E

Append a single section (with three short before/after pairs) to the existing **Linking and code references** section of `ai-docs/doc-convention.md`, codifying three related rules. The convention text **already** covers Part E by virtue of being context-agnostic — a macro-emitted `#[doc = "..."]` string is just a doc string with extra steps. The Part E audit applies the same rules; no separate convention text needed. (If during the design pass it emerges that the convention text needs an explicit "applies to macro-emitted docs too" sentence, that's a small addition to the existing Part D edit, not a new deliverable.)

The three rules:

1. **Cross-crate links must target direct dependencies.** Crate-prefixed intra-doc links (`[Type](other_crate::path)`) must point at a direct dep of the linking crate. If the linked type lives in a transitive dep, either re-route through a direct dep that re-exports it (the fix used in `src/lib.rs:319-320` in #197 — `quartzite_style_types::ColorRole` → `quartzite_style::ColorRole`), or demote the link to plain backticked prose.
2. **Backticked TitleCase identifiers in prose should be intra-doc links when in scope.** When prose mentions a type/trait/macro/fn by name in backticks (`` `Foo` ``), wrap it as an intra-doc link (`` [`Foo`] ``) when the identifier resolves to an item in scope (own crate or direct dep). Leave bare when the candidate is a generic placeholder (`T`, `Self`), a feature name (`` `serde` ``), or an English noun that happens to be capitalized.
3. **Bare qualified paths follow the same direct-dep rule.** Backticked qualified paths in prose (`` `quartzite_runtime::snapshot::capture_object` ``) should be wrapped as intra-doc links (`` [`quartzite_runtime::snapshot::capture_object`] ``) when the leading crate is a direct dep of the linking crate. When it isn't (e.g., `quartzite_core` mentioning `quartzite_runtime::*` — runtime is downstream of core, not a dep), the link is structurally impossible — leave bare. Don't introduce a broken link to "match the convention"; bare backticks are the correct rendering when the dep graph forbids the link.

One worked before/after pair per rule (3 pairs total).

## Out of scope

- The `--all-features` doc-gate convention itself — landed in #197.
- A custom CI script to enforce the direct-dep rule — `rustdoc::broken_intra_doc_links` already covers any feature combination CI builds, so the convention text plus the existing lint and the `--all-features` gate are sufficient.
- Reviewing or modifying the `rustdoc-args` / `rustc-args` lines in any crate — only `all-features` is added.
- Any change to the umbrella `Cargo.toml` or `quartzite-core/Cargo.toml` (already converted in #197).
- Conversion of bare-backticked identifiers that are **not** in scope (e.g., types from optional `dev-dependencies`, or types from features not enabled in the linking crate's default feature set). These remain bare; the convention permits it.
- Method-call prose (`` `foo()` ``, `` `Foo::bar` ``) — out of audit scope unless the link target has its own rustdoc page and the conversion is unambiguous.
- Macro codegen files **outside** `quartzite-macros/src/object/codegen.rs` and `quartzite-macros/src/extend/codegen.rs` — confirmed by grep that other codegen files (`object_part`, `meta_enum`, `object_impl`, `parse`, `mod`) emit no `#[doc = ...]` attributes. Re-grep is part of AC13 to defend against silent additions.
- `#[doc(hidden)]` emissions — these are not user-visible documentation, so audit does not apply.
- Macro expansion of doc strings using `concat!` of compile-time-only constants — none observed in the codegen files; if discovered, surface to user before in-flight conversion.

## Deferred

(none — all three follow-ups plus the convention text are addressed in this task.)

## Key decisions

| Question | Decision |
|---|---|
| Bundle Parts A through E in one PR? | Yes — extending PR #200 on `fix/docs-cleanup-197`. Issue #199 originally consolidated A + B; C was added during implementation kickoff (user noted local-type linking); E was surfaced after PR #200 had been opened (user noted macro-generated docs are the same class). Adding follow-up commits to PR #200 over opening a sibling PR because the work is one logical concern (intra-doc-link health) and a single review is more efficient than two. |
| Add `all-features = true` to crates with no `[features]` table today? | Yes. The convention is "everywhere" (per `ai-docs/doc-convention.md` § *Feature-gated documentation*); a no-op today guarantees no future contributor adds an optional feature without docs.rs picking it up. |
| Convert any of the audited Part B intra-doc links? | No — all already target direct dependencies. Audit is the verification, not a conversion task. |
| Audit scope for Part C — `lib.rs` only or full `src/**/*.rs`? | Full `src/**/*.rs` per crate. The convention applies to every doc comment, not just module-level prose. The bulk of fixes will land in `lib.rs` files; per-item docs add a smaller tail. |
| Where does the new convention text live? | Append to the existing **Linking and code references** section of `ai-docs/doc-convention.md` as a single combined paragraph covering both Parts B and D rules. Not a new top-level section. |
| Position of `all-features = true` inside each metadata block? | First line of the block, matching the order already present in `Cargo.toml` (umbrella) and `quartzite-core/Cargo.toml`. |
| How to justify "left bare" decisions in Part C? | Implicit by the criteria table in the *Part C* scope section. Non-obvious choices documented in the commit message or a one-line `// reason` adjacent to the doc comment. No separate audit log file. |

## Technical constraints

- `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` must continue to pass (already passes post-#197). This is the primary safety net for Part C — any bad intra-doc link the converter writes is caught by `#![deny(rustdoc::broken_intra_doc_links)]`.
- No edits to `rustdoc-args` / `rustc-args` arrays — only the `all-features = true` line is inserted (Part A).
- No edits to `Cargo.lock` are expected (metadata-only and doc-comment changes don't touch lockfile resolution).
- Branch must be a feature branch (`fix/docs-cleanup-197`) — never edit on `master`. AGENTS.md *Workflow* axiom 1.
- The `ai-docs/doc-convention.md` edit triggers no Propagation Rule sync-group (it is not in any sync group); no other instruction file mirrors this convention text.
- Part C edits touch doc comments only — no code semantics, no public-API shape changes, no test additions required. Test surface is the doc-build gate.

## Acceptance criteria

| # | Criterion |
|---|-----------|
| AC1 | Every workspace leaf crate (`quartzite-runtime`, `quartzite-events`, `quartzite-renderer`, `quartzite-style`, `quartzite-widgets`, `quartzite-paint-api`, `quartzite-event-types`, `quartzite-macros`, `quartzite-geometry`, `quartzite-style-types`, `quartzite-paint`) has `all-features = true` as the first line of its `[package.metadata.docs.rs]` block. |
| AC2 | `rg -A 4 '^\[package\.metadata\.docs\.rs\]' Cargo.toml */Cargo.toml` shows `all-features = true` in every workspace `Cargo.toml` (13 total: umbrella + 12 leaf crates). |
| AC3 | `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` exits 0. |
| AC4 | `cargo build --workspace` exits 0 (sanity check that metadata changes don't affect the build). |
| AC5 | `ai-docs/doc-convention.md` § *Linking and code references* gains a single paragraph covering both: (a) crate-prefixed intra-doc links must point at a direct dep, and (b) backticked TitleCase identifiers in prose should be intra-doc links when in-scope, bare otherwise (with the criteria from the *Part C* table summarised). One before/after pair per rule. |
| AC6 | `rg '\[`?\w+`?\]\(quartzite_\w+::' --type rust` continues to return only links pointing at direct dependencies of their linking crate (re-run as the verification of the Part B audit). |
| AC7 | `cargo fmt -- --check` and `cargo clippy --workspace -- -D warnings` exit 0. |
| AC8 | `Cargo.lock` is unchanged after the diff (no resolver impact from metadata-only edits). |
| AC9 | Every workspace `.rs` file under `src/**/*.rs` has been audited for bare-backticked TitleCase identifier mentions **and** bare qualified-path mentions (`pkg::path::Item`) in doc comments. Identifiers in scope (own crate or direct dep) are converted to intra-doc link form (`` [`Foo`] `` or `` [`pkg::path::Item`] ``); identifiers out of scope (placeholders, feature names, English nouns, non-dep qualified paths) are left bare per the criteria table in the *Part C* scope section. |
| AC10 | A single re-run of `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` after Part C edits exits 0 with no `warning:` or `error:` line — `#![deny(rustdoc::broken_intra_doc_links)]` is the safety net catching any bad link the conversion introduced. |
| AC11 | Spot-check: at least 5 prior-bare TitleCase candidate sites cited in the *Part C* inventory table (e.g., `src/lib.rs:13`, `:194`, `:257`, `:264`, `:280`) now render as clickable links in `target/doc/quartzite/index.html` (or the equivalent crate doc page). Manual visual check is sufficient — no scripting required. |
| AC12 | Bare qualified-path mentions where the leading crate is **not** a direct dep of the linking crate (e.g., `quartzite-core` mentioning `quartzite_runtime::snapshot::*`) remain bare. Spot-check: the 11 sites listed in the qualified-path inventory table are unchanged in the diff — they are correctly bare per the criteria table and the convention text. |
| AC13 | Static-literal `#[doc = "..."]` sites in `quartzite-macros/src/object/codegen.rs` and `quartzite-macros/src/extend/codegen.rs` are audited per the Part E inventory. Bare-backticked identifiers that resolve from the **downstream user crate** (typically through the `quartzite::*` umbrella) are converted to intra-doc-link form using paths reachable from that crate (e.g., `` `Auto` `` → `` [`quartzite::core::ConnectionType::Auto`] ``). Parameter names (`` `f` ``, `` `receiver` ``, `` `arg{i}` ``) remain bare per the Part C criteria table. |
| AC14 | Dynamic doc-string format sources (`bullet`, `example_doc` ×2 in `object/codegen.rs`; `trait_doc`, `acc_doc`, `acc_mut_doc` in `extend/codegen.rs`) are audited line-by-line. Any bare-backticked identifier in the format string that resolves from the downstream user crate is converted to intra-doc-link form using `quartzite::*` paths. The `bullet` source emits only parameter names and remains unchanged. |
| AC15 | Macro-emitted intra-doc links resolve correctly when expanded in a downstream context. The design picks one of: (a) a build-side smoke type exercising the macro paths under a doc-only feature, validated by `cargo doc -p <crate>` + the broken-intra-doc-link deny lint; (b) a source-side check via `cargo expand` on an existing `quartzite-macros/tests/` type, with manual confirmation that emitted `#[doc = ...]` strings carry the new link forms. The chosen verification is recorded in the design's *Test Design* table. |

## Open questions

(none — scope, deliverables, and verification are all specified. Part C is bounded by the audit criteria; bad-link risk is bounded by the doc-build safety net.)
