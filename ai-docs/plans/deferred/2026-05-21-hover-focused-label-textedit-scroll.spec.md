# Hover / pressed / focused rendering on Label / TextEdit / ScrollArea

**Source:** issue #403
**Date:** 2026-05-21
**Tracked in:** #403
**Blocked by:** #402 (palette state-groups + `ColorRole::FocusRing`)

> Surfaced by `/triage` from [`ai-docs/deferred/widget-backlog.md`](../../deferred/widget-backlog.md). Generalises the [`Button` hover / pressed / focused state-rendering pattern](../done/2026-05-15-button-hover-pressed-focused-states.spec.md) across `Label`, `TextEdit`, and `ScrollArea`. Event plumbing prerequisites are in place — `WidgetBase` carries `hovered` / `pressed` / `focused` flags, and `WidgetExt` exposes `is_hovered` / `is_pressed` / `is_focused` accessors. Only `DefaultStyle` paint impls change.
>
> **Round 2 Q1 resolution: wait for #402.** The spec is written against the clean post-#402 palette API (`palette.color(role, ColorGroup::{Normal, Hover, Pressed})` + `ColorRole::FocusRing`). Implementation cannot start until #402 lands and renames `Palette::color` / `Palette::with_role` to the two-axis signature and adds the `FocusRing` slot to both shipped palettes. Once #402 is merged, this spec moves out of `deferred/` and becomes implementation-ready as written — no rework expected.

## Scope

Add `hovered` / `pressed` / `focused` state-aware rendering to the existing `DefaultStyle` paint impls for `Label`, `TextEdit`, and `ScrollArea`, using the clean palette-state-groups API introduced by #402. The visual idioms follow [`design-system/README.md` § *Animation, hover, press*](../../../design-system/README.md) verbatim; the source-of-truth recipe is reproduced inline in § Key decisions below. The `Button` paint impl in `quartzite-style/src/default_style.rs` (post-#402) is the established pattern reference for how the state branches are wired.

Concretely:

- `impl Paint<Label> for DefaultStyle` in `quartzite-style/src/default_style.rs` reads `WidgetExt::is_hovered` / `is_pressed` / `is_focused` and produces the per-widget visuals decided in § Key decisions. Existing idle behaviour (`Window`-role fill + `WindowText`-role text in `w.alignment`) remains the baseline when all three flags are `false`.
- `impl Paint<TextEdit> for DefaultStyle` reads the same three flags and produces the per-widget visuals decided in § Key decisions. Existing idle behaviour (`Base`-role fill + 1 px `Text`-role outline + `Text`-role plain-text glyphs, with the read-only overlay still applied on top) remains the baseline. The `read_only` overlay logic is orthogonal — it applies in every state.
- `impl Paint<ScrollArea> for DefaultStyle` reads the same three flags and produces the per-widget visuals decided in § Key decisions. Existing idle behaviour (`Base`-role fill + 1 px `WindowText`-role outline) remains the baseline. The scrollbar-thumb / track work tracked separately under #315 stays out of scope.
- State resolution uses the `ColorGroup` axis: `let group = if w.is_pressed() { Pressed } else if w.is_hovered() { Hover } else { Normal };`, then every fill / text / pen lookup is `palette.color(role, group)`. No `Color::blend` arithmetic in the new paths — the blend lives inside `Palette::color`'s default derivation.
- Disabled (`!WidgetExt::is_enabled()`) treatment carries over via the existing `maybe_disabled` helper: every fill / text / pen color computed by the new state paths goes through `maybe_disabled(color, enabled)` before being handed to the painter. Applied to all three widgets uniformly.
- Focus outline reads `palette.color(ColorRole::FocusRing, ColorGroup::Normal)` for the pen stroke (per the design-system recipe + #402 § *FocusRing role*); pen width is `2.0` (overrides the 1 px idle outline on `TextEdit` / `ScrollArea`; adds the 2 px outline on `Label` which has no idle outline).
- Recording-painter unit tests in `quartzite-style/src/default_style_tests.rs` (one per visible state per widget, plus a `(disabled, focused)` precedence test per widget) toggle the matching flag on the widget's `WidgetBase` and assert the captured `Brush` / `Pen` matches the expected `palette.color(role, group)` value. Existing idle-state tests for the three widgets MUST still pass unchanged (flags default to `false`).
- Snapshot tests in `quartzite-style/tests/snapshots.rs` add one golden per visible state per widget under the existing `shared/` flow (light theme), plus one focused-state golden per widget under the existing dark-theme flow. Naming mirrors the Button pattern: `label_hovered.png`, `label_pressed.png`, `label_focused.png`, `text_edit_hovered.png`, `text_edit_pressed.png`, `text_edit_focused.png`, `scroll_area_hovered.png`, `scroll_area_pressed.png`, `scroll_area_focused.png`, plus dark `dark_label_focused.png`, `dark_text_edit_focused.png`, `dark_scroll_area_focused.png`.
- No `Style` / `Paint` trait surface redesign. The Button spec proved that per-widget state-awareness can be added directly inside each `Paint<W>` impl without touching the trait surface; with #402 in place the per-widget code shrinks further. If the design phase discovers a concrete trait-level need (e.g. a state-precedence helper shared across impls), it lands via a Design Amendment.

## Out of scope

- `LineEdit` hover / pressed / focused rendering. Tracked separately under [#406](https://github.com/maratik123/quartzite/issues/406) (deferred row in `widget-backlog.md`). The issue body and source spec deferred row both list only `Label` / `TextEdit` / `ScrollArea`; conflating the two issues would muddle the AC matrix.
- Scrollbar track / thumb rendering on `ScrollArea` (tracked under [#315](https://github.com/maratik123/quartzite/issues/315)). This spec only touches the chrome paint path; the eventual scrollbar work will need its own state-aware path on the thumb.
- New `ColorRole` variants beyond `FocusRing` (which lands in #402). No new roles introduced by this spec.
- Adding new `ColorGroup` variants beyond the three #402 introduces (`Normal` / `Hover` / `Pressed`). `Disabled` and `Focused` remain orthogonal modifiers per the #402 design.
- Event plumbing (renderer → widget tree). Hit-testing, `MouseEnter` / `MouseLeave` synthesis, focus traversal, and per-widget event routing are already handled by the prior input-plumbing pass (Button spec). Tests in this spec toggle the `WidgetExt::set_hovered` / `set_pressed` / `set_focused` setters directly on the widget's `WidgetBase`, exactly as the Button spec did.
- Mutating `WidgetBase::focus_policy` semantics. `Label` defaults to `FocusPolicy::NoFocus`; whether a focused-Label visual ever fires at runtime depends on the focus traversal pass and on whether the user opts the Label in via `focus_policy = StrongFocus`. The renderer paints whatever flag state it is given; AC tests toggle the flag directly regardless of policy.
- Cursor-shape changes on hover. Tracked under [#404](https://github.com/maratik123/quartzite/issues/404).
- TextEdit caret / selection / scroll-offset rendering. Tracked under [#317](https://github.com/maratik123/quartzite/issues/317).
- `Container` hover / pressed / focused rendering. `Container` is a grouping widget with no inherent interactive surface; not mentioned by the issue body. Defer to a separate issue if interactive-container semantics ever land.
- `Style` / `Paint` trait surface redesign. See § Scope final bullet.
- Re-aligning the `Button` paint impl with the post-#402 API. That refactor lives inside #402 itself (see `palette-state-groups.proposal.md` § *`DefaultStyle::draw_button` refactor*). This spec assumes Button has already been migrated and uses the same idiom.

## Deferred

- Per-widget state-precedence helper shared across `Paint<W>` impls | if the three new impls + the post-#402 Button impl end up duplicating the same `if w.is_pressed() { Pressed } else if w.is_hovered() { Hover } else { Normal }` skeleton, a small `fn state_group(w: &impl WidgetExt) -> ColorGroup` helper inside `default_style.rs` may be worth extracting | spec authors should leave this to the design phase; not a blocker
- Snapshot coverage for state combinations beyond single-flag goldens (e.g. `text_edit_focused_read_only.png`, `text_edit_disabled_focused.png`) | the Button spec capped at single-state goldens too; combinations are not enumerated unless a regression surfaces | follow-up if review flags a missing combo
- Re-paint trigger on state transition (calling `update()` after `set_hovered` / `set_pressed` / `set_focused`) | the input-plumbing pass owns the event-loop side and will set state + call `update()` together; this spec only changes paint output | input-plumbing pass
- Dark-theme `hovered` / `pressed` snapshot coverage on all three widgets | only `dark_*_focused.png` ships in v1 to keep the golden count proportional to the Button delta; the dark `Hover` / `Pressed` cells of `Window` / `Base` already mirror `Normal` per #402's default derivation (`WindowText` is the blend target; for foreground roles the cells are `=`), so the dark hover/pressed goldens for these three widgets would be near-identical to idle in this palette | follow-up only if a theme override surfaces visible divergence

## Key decisions

| Question | Decision |
|---|---|
| Where do the new render paths live | Inside the existing `impl Paint<Label> for DefaultStyle` / `impl Paint<TextEdit> for DefaultStyle` / `impl Paint<ScrollArea> for DefaultStyle` blocks in `quartzite-style/src/default_style.rs`. No new modules, no new public API on `Style` / `DefaultStyle`. Mirrors the Button spec's "Where the new render path lives" decision. |
| Scope of `Style` redesign | None. The post-#402 Button impl proves that per-widget state-awareness reduces to a one-line `ColorGroup` selector plus the existing two-axis palette lookups; the issue body's "likely a wider `Style` redesign" hedge does not apply once #402's API is in place. If the design phase discovers a concrete trait-level pain point, it returns NO-GO with the proposed redesign as a Design Amendment. |
| Visual idioms — source of truth | **`design-system/README.md` § *Animation, hover, press*** + the post-#402 palette derivation rule from [`design-system/palette-state-groups.proposal.md` § *Default derivation*](../../../design-system/palette-state-groups.proposal.md). User answered all three Round-1 questions with "how design system describes it?", which the spec-writer reads as "the visual output must match the design-system text". The design-system recipe: `Hover = role × Hover` cell (default-derived as `c.blend(WindowText.Normal, 0.06)` inside `Palette::color`); `Pressed = Highlight × Pressed` fill + `HighlightedText × Pressed` text (role-swap + group lookup, both via the palette); `Focused = additive 2 px outline using `palette.color(ColorRole::FocusRing, ColorGroup::Normal)`; `Disabled = ×0.5 alpha applied after role/group resolution via `maybe_disabled` (unchanged). Per-widget rows below pin which paint primitives carry each flag. |
| Disabled-state interaction | Apply `maybe_disabled(color, enabled)` (already exported from `default_style.rs`) to every state-derived fill / text / pen color before painting, same as the post-#402 Button impl. Existing per-widget read-only / placeholder logic (TextEdit) stays orthogonal. |
| Snapshot test coverage shape | One golden per visible state per widget under `shared/` (light), plus one focused-state golden per widget under the existing dark-theme flow. Matches the Button pattern's coverage shape adjusted for the three widgets here. Concrete file list pinned under § Acceptance Criteria. |
| Recording-painter test layout | Each new state per widget gets a `#[test]` in `default_style_tests.rs` alongside the existing per-widget idle / read-only tests. Mirrors the Button spec's AC3–AC6 shape (toggle one flag, assert one Brush/Pen attribute matches the expected `palette.color(role, group)` value). |
| No new public API | `WidgetBase` / `WidgetExt` / `Style` / `Paint` / `DefaultStyle` public surfaces are unchanged. Only `impl Paint<…> for DefaultStyle` bodies change. The `ColorGroup` enum + `ColorRole::FocusRing` slot + two-axis `Palette::color` / `Palette::with_role` signatures are added by #402, not by this spec. |
| `Label` visual mapping | **Idle:** `palette.color(Window, Normal)` fill + `palette.color(WindowText, Normal)` text in `w.alignment` (unchanged shape; uses the explicit `Normal` group lookup once #402 lands). **Hover:** fill = `palette.color(Window, Hover)`; text = `palette.color(WindowText, Hover)`. **Pressed:** fill = `palette.color(Highlight, Pressed)`; text = `palette.color(HighlightedText, Pressed)` (role-swap matching post-#402 Button-pressed). **Focused:** additive 2 px outline using `palette.color(FocusRing, Normal)`; base fill / text follow whichever of idle / hover / pressed applies. **Disabled:** every resolved colour passes through `maybe_disabled`. (Label has no outline at idle; the 2 px focus outline only paints when `focused` is set.) |
| `TextEdit` visual mapping | **Idle:** `palette.color(Base, Normal)` fill + 1 px `palette.color(Text, Normal)` outline + `palette.color(Text, Normal)` plain-text glyphs, with the read-only overlay still applied on top (unchanged shape with explicit `Normal` group). **Hover:** fill = `palette.color(Base, Hover)`; outline width 1 px, colour = `palette.color(Text, Hover)`; text colour = `palette.color(Text, Hover)`. **Pressed:** fill = `palette.color(Highlight, Pressed)`; text colour = `palette.color(HighlightedText, Pressed)`; outline width 1 px, colour = `palette.color(HighlightedText, Pressed)` (matches text for legibility under the inverted fill). **Focused:** outline widened to 2 px using `palette.color(FocusRing, Normal)`; fill / text follow whichever of idle / hover / pressed applies. **Read-only overlay applies on top of every state's fill** (orthogonal). **Disabled:** every resolved colour passes through `maybe_disabled`. |
| `ScrollArea` visual mapping | **Idle:** `palette.color(Base, Normal)` fill + 1 px `palette.color(WindowText, Normal)` outline (unchanged shape with explicit `Normal` group). **Hover:** fill = `palette.color(Base, Hover)`; outline width 1 px, colour = `palette.color(WindowText, Hover)`. **Pressed:** fill = `palette.color(Highlight, Pressed)`; outline colour = `palette.color(HighlightedText, Pressed)`. **Focused:** outline widened to 2 px using `palette.color(FocusRing, Normal)`; fill follows whichever of idle / hover / pressed applies. **Disabled:** every resolved colour passes through `maybe_disabled`. |
| State precedence (fill / text axis) | Carries over from the Button spec: `disabled` > `pressed` > `checked` > `hovered`. `checked` does not apply to Label / TextEdit / ScrollArea (none of the three has a `checked` flag), so the effective precedence on these three widgets is `disabled` > `pressed` > `hovered`. `focused` remains an additive outline modifier orthogonal to the fill axis (the Button impl's design carries over verbatim). |
| Why this is blocked on #402 | The spec body refers to `palette.color(role, ColorGroup::{Normal,Hover,Pressed})` and `ColorRole::FocusRing` — both introduced by #402. Attempting to implement now would either (a) duplicate the `Color::blend(WindowText, 0.06)` derivation inline in each `Paint<W>` impl (creating four call sites to rewrite when #402 lands) or (b) fork the focus-ring colour from `Highlight` (no `FocusRing` role exists yet). Both produce throwaway code. Round-2 user decision: wait. |

## Technical constraints

- **Hard prerequisite:** #402 must be merged before any work begins. Verify by `grep -n 'ColorGroup' quartzite-style-types/src/*.rs` returns hits in `palette.rs` + `lib.rs`, and `grep -n 'FocusRing' quartzite-style-types/src/color_role.rs` returns the new variant.
- `quartzite-style` already depends on `quartzite-widgets`; the new code reads `WidgetExt::is_hovered` / `is_pressed` / `is_focused` exactly as the post-#402 Button impl will. No new dependency edges.
- The post-#402 `Palette::color(role, group)` and `Palette::with_role(role, group, color)` signatures are the only palette APIs used by this spec. No direct `Color::blend` calls land in the new paint paths; the blend lives inside `Palette::color`'s default derivation.
- The existing `maybe_disabled` / `disabled` / `brush` / `read_only_overlay` helpers in `default_style.rs` are reusable as-is. (`maybe_disabled` accepts a `Color`, so it composes cleanly over the post-#402 `palette.color(role, group)` lookups.)
- The existing `RecordingPainter` fixture in `default_style_tests.rs` is reusable as-is for per-state Brush / Pen assertions.
- The existing snapshot harness (`quartzite-style/tests/snapshots.rs` + `tests/support/mod.rs`) is reusable — new tests follow the AC8 pattern from the Button spec.
- Doc gate: no new public items expected; the `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` gate must stay clean.
- Lint gate: `cargo clippy --workspace --all-targets -- -D warnings` clean.
- File-size budget: `default_style.rs` is currently 269 lines; the three new state-aware paths together with the post-#402 Button refactor SHOULD keep the file below the 500-line soft target. If the design phase finds it crowding 500, factoring helpers (e.g. `fn state_group`) out is fine.

## Acceptance Criteria

> All ACs are written against the post-#402 API (`palette.color(role, group)` + `ColorRole::FocusRing` + two-axis `Palette::with_role`). The spec is implementation-ready once #402 lands; no rewrite expected.

| # | Criterion |
|---|-----------|
| AC1 | **Label state-aware rendering.** `impl Paint<Label> for DefaultStyle` reads `is_hovered` / `is_pressed` / `is_focused`. Recording-painter tests assert: idle fill brush colour equals `palette.color(Window, Normal)`; hover fill brush colour equals `palette.color(Window, Hover)` and text brush colour equals `palette.color(WindowText, Hover)`; pressed fill brush colour equals `palette.color(Highlight, Pressed)` and text brush colour equals `palette.color(HighlightedText, Pressed)`; focused outline pen width equals `2.0` and pen colour equals `palette.color(FocusRing, Normal)`. |
| AC2 | **TextEdit state-aware rendering.** `impl Paint<TextEdit> for DefaultStyle` reads the same three flags. Recording-painter tests assert: hover fill brush colour equals `palette.color(Base, Hover)`, outline pen colour equals `palette.color(Text, Hover)`, text brush colour equals `palette.color(Text, Hover)`; pressed fill brush colour equals `palette.color(Highlight, Pressed)`, text brush colour equals `palette.color(HighlightedText, Pressed)`, outline pen colour equals `palette.color(HighlightedText, Pressed)`; focused outline pen width equals `2.0` and pen colour equals `palette.color(FocusRing, Normal)`. Existing `read_only` overlay logic still applies orthogonally to state visuals — a separate recording-painter test toggles `(read_only, hovered)` and asserts both the overlay fill and the hover-state base fill are captured. |
| AC3 | **ScrollArea state-aware rendering.** `impl Paint<ScrollArea> for DefaultStyle` reads the same three flags. Recording-painter tests assert: hover fill brush colour equals `palette.color(Base, Hover)`, outline pen colour equals `palette.color(WindowText, Hover)`; pressed fill brush colour equals `palette.color(Highlight, Pressed)` and outline pen colour equals `palette.color(HighlightedText, Pressed)`; focused outline pen width equals `2.0` and pen colour equals `palette.color(FocusRing, Normal)`. |
| AC4 | **State precedence.** On all three widgets, `disabled` (`!is_enabled()`) overrides any other fill via `maybe_disabled`; `pressed` overrides `hovered` on the fill axis where both apply; `focused` is an additive outline modifier orthogonal to the fill axis. Recording-painter tests per widget exercise `(disabled, focused)` to confirm the outline still fires at 2 px under disabled (with the outline colour itself passing through `maybe_disabled`), and `(pressed, hovered)` to confirm `pressed` wins — i.e. the fill brush equals `palette.color(Highlight, Pressed)`, not `palette.color(Window, Hover)` / `palette.color(Base, Hover)`. |
| AC5 | **Idle baseline preserved.** All existing `default_style_tests.rs` tests for Label / TextEdit / ScrollArea still pass — the new state flags default to `false`, so idle rendering produces the same event sequence and the same `palette.color(role, Normal)` lookups as before this spec. |
| AC6 | **Snapshot coverage.** Twelve new goldens under `quartzite-style/tests/snapshots/`: light variants `label_hovered.png`, `label_pressed.png`, `label_focused.png`, `text_edit_hovered.png`, `text_edit_pressed.png`, `text_edit_focused.png`, `scroll_area_hovered.png`, `scroll_area_pressed.png`, `scroll_area_focused.png` under `shared/`; dark variants `dark_label_focused.png`, `dark_text_edit_focused.png`, `dark_scroll_area_focused.png` under the `dark_` prefix flow. (Dark `hovered` / `pressed` goldens deferred — see § Deferred row 4: most of the cells mirror `Normal` in `DARK_PALETTE` per #402's derivation, so the dark goldens for those states would be near-identical to idle.) |
| AC7 | `cargo clippy --workspace --all-targets -- -D warnings` clean. |
| AC8 | `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` clean. |
| AC9 | **Prerequisite check.** Before the implementation PR opens, verify #402 has merged by checking `gh issue view 402 --json state` returns `CLOSED` AND that `grep -n 'ColorGroup' quartzite-style-types/src/palette.rs` returns hits. If either fails, this spec stays in `deferred/` and the PR does not open. |

## Open questions

_None remain — all visual idioms are pinned in § Key decisions, the prerequisite is declared in the header, and the API surface is defined by #402. When #402 lands, the spec moves out of `deferred/` and proceeds to design + implementation as written._
