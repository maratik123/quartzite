# Progress: Docs cleanup post-#197

**Branch:** fix/docs-cleanup-197
**base_commit:** 95e9e15c21b34da1e8ef1f64fb5c8028b6ab197c
**Issue:** [#199](https://github.com/maratik123/quartzite/issues/199)
**PR:** [#200](https://github.com/maratik123/quartzite/pull/200) (open; Parts A–D pushed in commit `60289c4`)
**Spec:** [`2026-05-10-docs-cleanup-197.spec.md`](2026-05-10-docs-cleanup-197.spec.md) — amended for Part E
**Design:** [`2026-05-10-docs-cleanup-197.design.md`](2026-05-10-docs-cleanup-197.design.md) — amended for Part E (round 1 of fresh budget → GO)
**Last build:** PASS (post-Parts-A–D; AC10 was clean at commit `60289c4`)

## Subtasks

### Parts A–D — DONE in commit `60289c4` (PR #200)

- [x] T1 — Feature branch (`fix/docs-cleanup-197`)
- [x] T2 — Part A — `all-features = true` in 11 leaf-crate metadata blocks
- [x] T3 — Part D — 3-bullet convention paragraph + 3 before/after pairs in `ai-docs/doc-convention.md`
- [x] T4 — Part C — 12-batch audit + conversion across `src/**/*.rs`
- [x] T5 — AC2 verified
- [x] T6 — AC6 verified (counts: `src/lib.rs:18, color_role.rs:1, renderer/lib.rs:1, render_harness.rs:1` — umbrella +1 vs baseline from intentional `Palette`/`ColorRole` prose conversion in batch 1; all targets remain direct deps)
- [x] T7 — AC11 + AC12 verified
- [x] T8 — Final gates clean
- [x] T9 — Single commit `60289c4`, push, PR #200 open

### Part E — LIVE (T10–T15)

- [x] T10 — Audited static-literal `#[doc = "..."]` sites in `quartzite-macros/src/object/codegen.rs`. Two conversions applied: L449 `` `Auto` `` → `` [`quartzite::core::ConnectionType::Auto`] ``; L504 `` `Queued` `` → `` [`quartzite::core::ConnectionType::Queued`] ``. All other static literals contain only parameter names (`` `f` ``, `` `receiver` ``), pre-existing intra-doc links (`[`quartzite::core::ReceiverGuard`]` / `[`quartzite::core::QueuedDispatcher`]`), or markdown headings — left unchanged per Part C criteria. `extend/codegen.rs` carries no static prose `#[doc = "literal"]` (only the dynamic `trait_doc`/`acc_doc`/`acc_mut_doc` which already use `[`{self_name}`]`).
- [x] T11 — Audited dynamic doc-string format sources line-by-line. `bullet` (object/codegen.rs:347–349) emits only `` `arg{i}` `` parameter names — unchanged. `example_doc` (L445, L500) is a fenced ` ```no_run ` doctest body — content is compiled Rust, not prose; no link conversion applies. In `extend/codegen.rs`: `trait_doc` (L114), `acc_doc` (L122), `acc_mut_doc` (L130) already use `[`{self_name}`]` form — verified line-by-line, no other identifier in the format strings warrants conversion.
- [x] T12 — Defensive grep `rg -n '#\[doc' quartzite-macros/src/` confirms only `object/codegen.rs` and `extend/codegen.rs` emit `#[doc]` (the `extend/codegen.rs:636` hit is a `///` comment inside `#[cfg(test)]`, not an emission). No silent additions.
- [x] T13 — AC15 verified via `RUSTFLAGS='--cfg feature="std"' cargo expand -p quartzite-macros --test object --all-features`. `Counter`'s `connect_count_changed_auto` (expanded line 398) and `connect_count_changed_queued` (expanded line 435) both carry the new link form on their summary doc line. Bare `` `Auto` `` / `` `Queued` `` no longer appear anywhere in the expanded output. `Extend`-emitted `trait_doc`/`acc_doc`/`acc_mut_doc` correctly substitute `{self_name}` to `Counter` (expanded lines 23, 48, 73). RUSTFLAGS workaround is needed because `quartzite-macros` doesn't declare `feature = "std"` itself — the `#[cfg]` is evaluated against the test crate at expand time. Captured snippets included in T14 commit message.
- [x] T14 — Follow-up commit on `fix/docs-cleanup-197` covering Part E edits.
- [x] T15 — `git push` + AGENTS.md Workflow Axiom 2 PR-body sync.

## Files touched (Parts A–D in commit 60289c4)

- 11 leaf-crate `Cargo.toml` files (`+all-features = true`)
- `ai-docs/doc-convention.md` (convention paragraph)
- ~30 source files across `src/**/*.rs` (Part C TitleCase conversions)
- See `git show 60289c4 --stat` for the full list

## Files to touch in Part E (T10–T15)

- `quartzite-macros/src/object/codegen.rs` (static + dynamic-source edits)
- `quartzite-macros/src/extend/codegen.rs` (likely no edits — the dynamic format strings already use `[`{self_name}`]` form; verify)

## Next action

Self-review (Step 10 of `/task` workflow). Spec/design/progress not yet moved to `done/` — that's Step 12, after self-review passes.

## Notes acknowledged from design-review (round 1 of fresh budget — GO)

1. Dynamic-source count: spec says ~5, design clarified to 4 (`bullet`, `example_doc` ×2, `parameters_doc`) for `object/codegen.rs`. Doesn't affect implementation — T11 audits by name, not count.
2. Macro name corrected: `Object` not `DeriveObject` (was a spec/design typo). Both files updated.
3. Edge case: users who depend on `quartzite-macros` directly without the `quartzite` umbrella would see unresolvable links. Acknowledge in T14 commit message; documented consumption path is via umbrella + `derive` feature.
