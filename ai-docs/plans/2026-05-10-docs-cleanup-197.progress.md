# Progress: Docs cleanup post-#197

**Branch:** fix/docs-cleanup-197
**base_commit:** 95e9e15c21b34da1e8ef1f64fb5c8028b6ab197c
**Issue:** [#199](https://github.com/maratik123/quartzite/issues/199)
**Spec:** [`2026-05-10-docs-cleanup-197.spec.md`](2026-05-10-docs-cleanup-197.spec.md)
**Design:** [`2026-05-10-docs-cleanup-197.design.md`](2026-05-10-docs-cleanup-197.design.md)
**Last build:** PASS (cargo build + `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` after T2)

## Subtasks (per amended design)

- [x] T1 — Create feature branch off master (`fix/docs-cleanup-197`)
- [x] T2 — Part A — Added `all-features = true` to all 11 leaf-crate `[package.metadata.docs.rs]` blocks. AC2 verified: 13/13 manifests show `all-features = true`. Doc gate clean.
- [x] T3 — Part D — Appended 3 rule bullets + 3 before/after fenced-text pairs to `ai-docs/doc-convention.md` § *Linking and code references* (after the existing 4th bullet, before `## Language`). Markdown only — no build impact.
- [ ] T4 — **Part C** — Per-crate batched audit + conversion (12 batches: umbrella `src/` + 11 leaves). Each batch: candidate-list grep → criteria-table judgement → per-crate `cargo doc --no-deps -p <crate> --all-features` gate.
  - [x] Batch 1 — umbrella `src/lib.rs` — 9 TitleCase conversions (Object trait + ecosystem bullet types + macros + geometry + events + widgets + style Palette/ColorRole prose). Doc gate clean.
  - [x] Batch 2 — quartzite-core/src/ — converted self-referencing TitleCase in `lib.rs`/`object_base.rs`/`traits.rs`/`id.rs`/`snapshot.rs`. The 6 forced-bare qualified-path mentions (`quartzite_runtime::snapshot::*` in `snapshot.rs`/`tree.rs`/`meta.rs`/`lib.rs`) and the bare `ObjectTree` references remain — runtime is downstream of core, AC12 spot-checks. Doc gate clean.
  - [x] Batch 3 — quartzite-runtime/src/ — converted self-referencing TitleCase in `timer_drivers.rs`/`application.rs`/`object_ref.rs`/`connection_table.rs`/`factory.rs`. Doc gate clean.
  - [x] Batch 4 — quartzite-macros/src/ — no source edits required. `quartzite-core` is NOT a normal dep of `quartzite-macros` (only a dev-dep via the `quartzite` umbrella), so every TitleCase identifier from `quartzite_core` (`AsObject`, `Object`, `ObjectBase`, `MetaObject`, `IntoValue`, `FromValue`, `EnumMeta`, `Signal`, `Value`) is forced bare. The local proc-macros (`Extend`, `Object`, `object_impl`, `object_part`, `MetaEnum`) are already linked at every mention. The L225 `quartzite_core` qualified mention is the spec-cited forced-bare site. Doc gate clean.
  - [x] Batch 5 — quartzite-widgets/src/ — converted self-referencing TitleCase in lib.rs (Label/Button/LineEdit/BoxLayout/GridLayout) and per-widget files (TextEdit/Label/LineEdit/Button/Container/WidgetBase/BoxLayout/GridLayout/ScrollArea). `AsObject` (from `quartzite-core` direct dep) linked via `quartzite_core::AsObject`. Doc gate clean.
  - [x] Batch 6 — quartzite-events/src/ — no source edits required. Bare-TitleCase matches are physical-key labels (`F1`–`F12`) and an operator-syntax mention (`|`); all true types in this crate were already linked. Doc gate clean.
  - [x] Batch 7 — quartzite-renderer/src/ — no source edits required. All bare-TitleCase candidates were already linked or refer to dev-dep types (`WidgetExt` in `quartzite-widgets`) or 3rd-party-image types (`RgbaImage`) that aren't direct deps. Doc gate clean.
  - [x] Batch 8 — quartzite-style/src/ — converted 2 self-referencing `Style` mentions in `style.rs`. Doc gate clean.
  - [x] Batch 9 — quartzite-paint-api/src/ — converted 2 self-referencing `Image` mentions in `image.rs`. Doc gate clean.
  - [x] Batch 10 — quartzite-event-types/src/ — no source edits required. All bare-TitleCase candidates were already linked. Doc gate clean.
  - [x] Batch 11 — quartzite-geometry/src/ — no source edits required. Doc gate clean.
  - [x] Batch 12 — quartzite-style-types/src/, quartzite-paint/src/ — no source edits required. style-types `Style` mention is forced bare (style is downstream of style-types); `Color::TRANSPARENT` left bare in awkward `default != Color::TRANSPARENT` comparison expression. Doc gates clean.
- [ ] T5 — Verify AC2 (already done at T2 — re-run as part of T8 final gate)
- [ ] T6 — Verify AC6 (`rg -c '\[`?\w+`?\]\(quartzite_\w+::' --type rust` returns 17/1/1/1)
- [ ] T7 — Verify AC11 (5 prior-bare TitleCase sites render as `<a>`) + AC12 (11 qualified-path bare sites unchanged in diff)
- [ ] T8 — Final gates: fmt, clippy, build --workspace, doc --workspace --all-features (AC10), Cargo.lock unchanged (AC8)
- [ ] T9 — Single commit + push + open PR (PR body cites #199 and AC1–AC12)

## Files touched

- `Cargo.toml` (umbrella) — verified: already `all-features = true` from PR #197 (no edit this round)
- `quartzite-runtime/Cargo.toml`, `quartzite-events/Cargo.toml`, `quartzite-renderer/Cargo.toml`, `quartzite-style/Cargo.toml`, `quartzite-widgets/Cargo.toml`, `quartzite-paint-api/Cargo.toml`, `quartzite-event-types/Cargo.toml`, `quartzite-macros/Cargo.toml`, `quartzite-geometry/Cargo.toml`, `quartzite-style-types/Cargo.toml`, `quartzite-paint/Cargo.toml` — `+all-features = true` as first line of `[package.metadata.docs.rs]`
- `ai-docs/doc-convention.md` — appended 3 bullets + 3 fenced before/after pairs to § *Linking and code references*

## Next action

T4 — Part C audit + conversion. Handed off to fresh agent (see context-reset note below).

## Context-reset planning

Per `/context-reset`: T1 + T2 + T3 = 3rd subtask complete; total 9 ≥ 5; handoff fires now.

T4 is internally batched into 12 per-crate sub-batches:
1. umbrella `src/` (the largest by candidate count — ~16 TitleCase per spec inventory)
2. quartzite-core/src/
3. quartzite-runtime/src/
4. quartzite-macros/src/ (~16 TitleCase per spec inventory)
5. quartzite-widgets/src/ (~3 TitleCase per spec inventory)
6. quartzite-events/src/
7. quartzite-renderer/src/
8. quartzite-style/src/
9. quartzite-paint-api/src/
10. quartzite-event-types/src/
11. quartzite-geometry/src/
12. quartzite-style-types/src/, quartzite-paint/src/ (small — can pair)

The fresh agent should:
1. Read this progress file + the spec (esp. Part C criteria table) + design (esp. Part C section).
2. For each crate batch: generate candidate list (`rg -n '`[A-Z][A-Za-z0-9_]+`' <crate>/src/ --type rust` + `rg -n '`quartzite_[a-z_]+::[A-Za-z0-9_:]+`' <crate>/src/ --type rust`), filter out already-linked lines (anything matching `\[`), apply spec criteria per candidate, run `cargo doc --no-deps -p <crate> --all-features` after the batch.
3. Update this progress file after each crate batch (mark `[x]` for that crate, append to "Files touched", note any non-obvious bare-left choices).
4. After all 12 batches, run T8 final gates and proceed to T9 commit + push + PR.

The 11 forced-bare qualified-path sites (per spec inventory: `quartzite-core/src/lib.rs`, `quartzite-core/src/snapshot.rs`, `quartzite-core/src/meta.rs`, `quartzite-core/src/snapshot/tree.rs`, `quartzite-macros/src/lib.rs`) MUST remain bare — the linking crate doesn't have the target as a direct dep, so the link is structurally impossible. AC12 spot-checks this.
