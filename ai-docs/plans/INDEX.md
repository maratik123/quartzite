# Plan Index

Legend: ✅ done · 🟢 ready (spec+design, no blockers) · 🟡 spec-only (no design yet) · 🔴 blocked

## Active plans

| Plan | Crate(s) | Status | Blocked by |
|------|----------|--------|------------|
| [triage-deferred-jsonl](done/2026-05-31-triage-deferred-jsonl.spec.md) | instruction surface (`.claude/settings.json` + `.claude/agents/triage-runner.md` + `.claude/skills/{triage,next,task}/SKILL.md` + `.claude/skills/task/inbox-propagation.md` + `.claude/skills/task/reference.md` + `.claude/skills/ai-audit/SKILL.md` + `AGENTS.md` + `ai-docs/{agent-docs-index,workflow,miri-policy,triage-runner-bridge,triage-runner-design-links}.md` + `.gitignore`) + data (`ai-docs/deferred/**` markdown → JSONL) | ✅ implemented (0 new Rust tests; instruction-file + data-file PR — both phases in ONE PR per user override of the issue's separate-PRs note. **Phase 1 (tooling unblock):** widened `.claude/settings.json` allow-list with `jq`/`awk`/`sort`/`wc`; gave `triage-runner` an explicit `tools:` front-matter line incl. `Write`; reworked the Phase-7.5 umbrella-body recipe off the bare `>` redirect to `body=$(…)` → `Write ai-docs/triage/umbrella-<N>.body.md` → `gh issue edit --body-file` → `rm -f`; widened `.gitignore` for the umbrella staging files; propagated across the Triage group. **Phase 2 (JSONL migration):** converted the ~1070-row deferred markdown corpus to per-theme `.jsonl` (one JSON object per line; 3 row shapes — thematic / `kind:"widget"` / `_inbox`), deleted the 10 `.md` files + `deferred-items.md`, rewrote `/next` + `/triage` (SKILL+runner) + `/task` Step 12 + `inbox-propagation.md` read/write to baked-in `jq` one-liners (the six spec-shape parse rules unchanged — only the sink: markdown row → JSON line), updated the AGENTS.md `_inbox` write-AXIOM + all deferred-store references to `.jsonl`. **Lossless reconciliation (AC7):** per-theme `wc -l` matches the index exactly (30/23/44/10/59/90/358/6); widget-backlog = 46 widget + 136 thematic = 182; all 802 data rows round-trip byte-identical. **AC8:** `/next` candidate set byte-identical before/after (318 candidates). Design-review GO on re-review Round 2 (Round 1 GO with 3 issues: 1 spec-amending — AGENTS.md char-size gating dropped to OUT OF SCOPE via Spec Amendment, deferred to a future `/ai-audit` session per user decision; + AC8-proof + escaped-pipe-count fold-ins). Design widget count corrected 47→46 (live-verified off-by-one) via the design subagent before self-review. Self-review APPROVE Round 1 zero findings. AC1–AC9 PASS. 8 commits on the branch: `c3578a0` (allow-list + Write), `a6dddd8` (bare-`>` rework + gitignore), `7a5e851` (Triage-group propagation), `b4b2137` (markdown→JSONL conversion), `14febc7` (delete markdown corpus), `4534bd4` (/next + /triage JSONL rewrite), `ef265a5` (/task Step 12 sink swap), `f2f8ed5` (AGENTS.md + repo-wide reference sweep). Closes #596.) | — |
| [hit-testing-reverse-z-order](done/2026-05-30-hit-testing-reverse-z-order.spec.md) | `quartzite-hit-test` (new) `quartzite-style-dispatch` | ✅ implemented (17 unit tests + 2 doc-tests — new paint-free `quartzite-hit-test` crate with `hit_test(root: ObjectId, point: Point, resolver: &dyn WidgetResolver) -> Option<(ObjectId, Point)>`, the structural inverse of `dispatch_paint`: walks the widget tree depth-first but iterates children in **reverse** and tests child-before-parent, so the visually-topmost (last-painted) widget wins; returns the hit id + accumulated origin offset (root-local → hit-local, for `point - offset` mapping). Mirrors paint's visibility-skips-subtree, `children_clip_rect()` clip gate, and resolver-miss `tracing::warn!`. **Coordinate inversion:** node membership uses a local-space `Rect::new(Point::default(), widget_base().geometry.size())` test, NOT the parent-relative `geometry()` rect — AC1b/AC4 are the regression guards. The immutable `WidgetResolver` trait + closure blanket impl **moved** from `quartzite-style-dispatch` into the new crate; `quartzite-style-dispatch` gained a dep + `pub use quartzite_hit_test::WidgetResolver;` re-export so every existing import path stays valid (the separate mutable `quartzite_widgets::layout::WidgetResolver` untouched). Dep cone: `quartzite-core`, `quartzite-geometry`, `quartzite-widgets`, `tracing` — no paint/style types. Not wired into the `quartzite` umbrella facade (deferred). **Hit-test traversal primitive only** — the event-dispatch loop is a deferred follow-up. Design-review GO Round 1 (2 minor notes folded into design before Step 8). Self-review APPROVE Round 1 zero findings. Step 8 ran across 2 handoff groups (3+2) with `/context-reset` between. AC1–AC10 PASS. 4 commits on the branch: `c50d278` (crate skeleton), `03d5f6e` (WidgetResolver move + re-export), `0024f8a` (hit_test impl + AC9 doc-test), `9f05c1a` (test module AC1–AC8). Closes #395.) | — |
| [bump-msrv-rust-1-96](done/2026-05-29-bump-msrv-rust-1-96.spec.md) | workspace `quartzite-paint-api` `quartzite-style-dispatch` `quartzite-core` `quartzite-widgets` `quartzite-renderer` `quartzite-runtime` `quartzite-style` `quartzite-macros` | ✅ implemented (MSRV `1.95`→`1.96` at `[workspace.package] rust-version` (members inherit) + README + design-system prose. Adopted std's now-stable (1.96) `assert_matches!` in test code via the crate-root re-export import `use core::assert_matches;` (no_std `quartzite-paint-api`) / `use std::assert_matches;` (std crates + `tests/`) — NOT the nightly module path `::assert_matches::assert_matches` (E0432 on stable). Governing Debug policy: derive only where CHEAP — public-trait `Debug` supertraits REJECTED (macro-generated `AsWidget` + core `Object` stay supertrait-free), so `WidgetView`/`Box<dyn Object>`/`ObjectTree`/`Result<non-Debug,_>` sites are carved out (22 documented `assert!(matches!)` carve-outs: 8 doc-`///` + 8 WidgetView + 4 runtime-dyn + 2 singleton-Result). Workspace-wide cheap-`Debug` sweep: 18 public structs/enums gained `#[derive(Debug)]` (style `StyleRegistry`; widgets `WidgetChildren`/`WidgetChildrenIter`/`BoxLayout`/`GridLayout`; runtime `ApplicationBuilder`/`ConnectionRecord`/`ConnectionTable`/`EventLoop`/`ThreadPool`/`ThreadDriver`/`AppDriver`/`RegistryGuard`/`LoopRegistry`/`TimerState`; renderer `WindowedApplicationBuilder`/`RenderHarnessBuilder`/`ActiveLoopGuard`) + private `LocalBrushKind` + the 9 concrete widget structs. `.github/workflows/miri.yml` pinned nightly `nightly-2026-05-01`→`nightly-2026-05-29`; `coverage.yml` left unpinned. 13 commits `61d657e`/`71acbc4`/`0f879f0`/`fde2691`/`c7c6f09`/`c226b66`/`61a0e63`/`7e49ca3`/`2593140`/`672e549`/`10ef390`/`e57ca47`/`82ac7dc`. Closes #584.) | — |
| [per-widget-clip-rect](done/2026-05-27-per-widget-clip-rect.spec.md) | `quartzite-macros` `quartzite-widgets` `quartzite-paint-util` `quartzite-style-dispatch` `quartzite-style` | ✅ implemented (28 new tests — AC1–AC11 unit/dispatch + AC12 Vello snapshot. `AsWidget::children_clip_rect(&self) -> Option<Rect>` default-None on the `AsWidget` trait (emitted by the `Extend` macro at the `WidgetBase` root); `#[clip_rect(method = "content_rect")]` field annotation on `ScrollArea`'s `widget_base: WidgetBase` field causes the macro to emit the `children_clip_rect` override; resolves Rust E0119 (a second hand-written `impl AsWidget for ScrollArea` is forbidden alongside the macro-emitted one). `geometry_root()` facade-aware helper added to `quartzite-macros/src/util.rs`. `ScrollArea::content_rect() -> Rect` returns the zero-inset local-frame rect (v1; #315 adds chrome inset). `TranslateGuard::with_clip(painter, origin, clip)` added to `quartzite-paint-util` (calls `save → clip_rect(clip) → translate(origin)`; `Drop`'s `restore` pops both the clip and the translate). `dispatch_paint::visit` reads `parent.children_clip_rect()` once per parent and routes each visible child through `with_clip` when `Some`. `ClipRect(Rect)` added to test-only `PaintEvent` enum. AC11 `ClippingWidget` fixture proves polymorphic dispatch beyond `ScrollArea`. AC12 snapshot routes through `dispatch_paint` with a 200×200 `Label` in a 64×64 `ScrollArea` canvas. 4 commits on the branch: `b6890cb` (macros), `a91f88d` (widgets), `362924b` (dispatch), `616020e` (snapshot). Closes #397.) | — |
| [ac-doc-leak-guard](done/2026-05-27-ac-doc-leak-guard.spec.md) | CI infra (`scripts/check-ac-doc-leaks.sh` (new) + `scripts/check-rustdoc-internal-refs.sh` propagation patch + `.github/workflows/ci.yml` `docs` job step) | ✅ implemented (0 new Rust tests — shell-script + workflow only; 5-injection AC3/AC4/AC5/AC6/AC7 smoke matrix in Subtask 3 verifies behaviour at the precedent script's invariant ports). New stand-alone `scripts/check-ac-doc-leaks.sh` (189 lines, mode `100755`) scans `**/src/**` (minus `tests/`, `benches/`, `target/`, `quartzite-test-helpers/src/`) for `///` / `//!` / `#[doc = "..."]` lines containing `\bAC[0-9]+[a-z]*\b`, filters `#[cfg(test)]`-region hits via the precedent's `is_cfg_test_enclosed` awk helper + `sibling_re` regex (copied verbatim), exits 1 on retained hits. New `Check rustdoc has no AC# leaks` step in `.github/workflows/ci.yml` `docs` job between the existing repo-internal-refs check and RUSTFLAGS-uniformity check; inherits `ripgrep` install, `push: master` + `pull_request: master` triggers, and `cargo doc` upstream — no new workflow file needed. **Round-2 design propagation patch** to `scripts/check-rustdoc-internal-refs.sh`: `known_sibling_site` updated from stale `quartzite-style/src/default_style.rs` to `default_style/mod.rs` (post-PR-#492 path); silent-skip `if [[ -f ... ]]; then ... fi` wrapper replaced with upfront fail-loud `exit 2` guard so a future site-relocation surfaces at script-startup time in BOTH scripts; docblock + inline citation refreshed to `default_style/mod.rs:392-394`. Design ran 2 design-review rounds (R1 ITERATE: 1 major — stale `known_sibling_site` propagated verbatim into the new script would inherit the precedent's silent no-op smoke check; +2 minors. R2 GO no findings — M expanded 4→5 with new Subtask 4 for the precedent propagation patch). Self-review APPROVE Round 1 with one informational nit (smoke-check position after early-exit; design GO accepted this flow). AC1–AC10 all PASS. 4 commits on the branch: `91671c9` (skeleton + regex + scoped rg), `6bdabb4` (cfg(test) filter + fail-loud sibling-site smoke check), `72231b0` (precedent propagation patch), `7e0a83c` (workflow wiring + `actionlint` clean). Closes #559. | — |
| [dependabot-pr-skill](done/2026-05-27-dependabot-pr-skill.spec.md) | instruction surface (`.claude/skills/dependabot-pr/SKILL.md` + `.claude/skills/dependabot-pr/reference.md` + `.gitignore` + `AGENTS.md` § Agent Docs + `ai-docs/claude-tools-hierarchy.md` §3c + `.claude/skills/pr-ci-failed/SKILL.md` sentinel) | ✅ implemented (0 new Rust tests; instruction-file-only PR — new `/dependabot-pr` skill that drives one round of Dependabot cargo-PR triage. Re-invocable; never auto-merges; never pushes to bot branches. Routes via 2×3 `(diff-scope × CI-state)` matrix: `lockfile-only × all-green` → print `gh pr merge --merge <N>` and pause for user (KD-4); `lockfile-only × red` → delegate to `/pr-ci-failed` with EXIT-between-Step-3-and-Step-4 prompt directive, then apply verdict-translation table on `(class, reproducer outcome)` returned by child → `@dependabot rebase` / `@dependabot recreate` / bail-with-issue per KD-5; `lockfile-only × pending` → pause-for-user (KD-13); `scope-drift × *` → bail-with-comment (KD-7). Design-review ran 2 rounds (R1 ITERATE: 2 blockers + 1 major — original "STOP after Step 4" stop-point incoherent with `/pr-ci-failed` Step-4 control flow (inline-fix classes apply edits to workspace; delegation classes push end-to-end via `/bugfix` — both violate KD-8); inheritance claim on parent's `allowed-tools` structurally wrong; propagation safety-net mitigation overstated. R2 GO with 2 minor notes + 2 recommendations, folded into design before Step 8). Carve-out reframed to "EXIT between Step 3 and Step 4" — child runs Steps 0–3, exits without entering Step 4, no fix applied, no `/bugfix` delegation, no push (KD-14 honest: directive is GENERIC stop-point, not Dependabot-specific). Parent's `allowed-tools` re-derived from first principles — `Bash(git push *)`, `Bash(git commit *)`, `Bash(git add *)`, `gh pr merge` all absent (Blocker-2 fix). Major-3 mitigation via option (b) sentinel comment in `/pr-ci-failed/SKILL.md` near Step 3/4 boundary (pointer-only; does NOT branch behaviour on Dependabot detection). Self-review APPROVE Round 1 zero findings. 14/15 ACs PASS at Step 9; AC14 manual end-to-end dry-run vs PR #574 captured in PR body at Step 12. 3 implementation commits on the branch: `91fdc44` (SKILL.md + `.gitignore`), `24d723c` (reference.md per-cell bodies + delegation template + verdict table), `2e8fe3c` (propagation triple). Closes #577.) | — |
| [dependabot-config](done/2026-05-26-dependabot-config.spec.md) | repo infra | ✅ implemented (0 new tests; config-only — adds `.github/dependabot.yml` with `version: 2`, two `updates:` entries (`cargo` + `github-actions`), both targeting `master` weekly, minor+patch grouped into a single rollup PR (`cargo-deps` / `github-actions` groups), major bumps filtered via `ignore:`. Commit-message prefixes `chore(deps)` / `chore(ci)`. AC1–AC10 PASS; AC11 deferred to post-merge Dependabot UI check. Single commit: `23e3836`. Closes #570.) | — |
| [signal-arity-relaxation](done/2026-05-25-signal-arity-relaxation.spec.md) | `quartzite-core` `quartzite` | ✅ implemented (PR #569 — `connect_signal_to_signal` / `connect_signals` arity check relaxed from `!=` to `<`: source with ≥ target args succeeds, excess dropped via `&args[..to_arity]`. `connect_signal_to_slot` gains meta-validated path: when slot `MethodMeta` is present, validates arity + type-name prefix at connection time, invokes with `&args[..slot_arity]`; fallback path unchanged (`&[]`). `ArityMismatch` variant repurposed to mean `from < to`. Test module extracted to `quartzite-core/tests/connect.rs` (1201 lines) to keep `src/connect.rs` within 1500-line limit; only `auto_cross_thread_posts_to_dispatcher` stays inline (needs private `crate::signal::tests` access). 6 commits. AC1–AC10 all PASS. Closes #566.) | — |
| [annotated-attribute-docs](done/2026-05-26-annotated-attribute-docs.spec.md) | `quartzite-macros` `quartzite-widgets` + `ai-docs/{doc-convention,code-style}.md` | ✅ implemented (cargo unit tests + 8 trybuild fixtures — tri-state `#[undocumented(allow|warn|deny)]` diagnostic on annotated items; per-item + per-invocation + global scopes with `deny`-wins precedence under `--all-features`. Visibility lift + doc-add on 10 annotated fields across 9 widget/layout files (WidgetBase + 6 concrete widgets + Container::children + 2 layouts). New `## Annotated items` sections in `doc-convention.md` and `code-style.md` documenting the convention + opt-out surfaces. Subtask 5.5 throwaway probe empirically confirmed the synthesised `#[deprecated]` + `const _: () = { __probe(); };` shape fires the `deprecated` lint under same-expansion def+use (KD1 option (a) verified). 8 trybuild fixtures cover AC7 paths (a)–(h): warn fires (default), silent when documented, per-item allow, per-invocation allow, global allow via cargo feature, per-item deny, per-invocation deny, per-item allow beats per-invocation deny (precedence). Design ran 5 rounds (R1 ITERATE 5 findings + Spec Amendment 1 for stale rows; R2 cancelled mid-flight after user added tri-state amendment; R3 tri-state landed; R4 round-2 design-review fixes; R5 Spec Amendment 3 — user reverted the `#[prop]`→`#[property]` and `#[invokable]`→`#[invoke]` rename mid-PR, design re-revised). Design-review ran 4 rounds (R1 ITERATE, R2 ITERATE on tri-state-specific findings, R3 GO on tri-state design, R4 GO on Amendment-3 design with 2 minor notes folded back). Self-review ran 4 rounds (R1 APPROVE with 3 minors; R2 REJECT on stale mutex wording in design after the precedence amendment; R3 APPROVE; R4 REJECT on 3 stale-rename doc surfaces missed by the revert plan; final APPROVE after fix). Spec ran 3 interview rounds (Q1 rename both, Q2 doc-scope override pub-only, Q3 enforcement — user-amended mid-flight from warn-only to opt-outable, then to full tri-state, then reverted the rename entirely). KD9 amendment during Step 11: discarded the `compile_error!` mutex on `undocumented-allow` + `undocumented-deny` features (incompatible with `cargo doc --all-features`) and replaced with `deny`-wins precedence — mirrors rust's native lint-level precedence, doc-gate-safe, single-valued. Amendment 3 (post-PR-open): user-requested revert of the rename — 3 revert commits restored `#[prop]` / `#[invokable]` across declaration side, workspace use-sites, and trybuild fixtures; tri-state diagnostic preserved unchanged. 14 commits total on the branch. Closes #564.) | — |
| [windowed-application-new](done/2026-05-25-windowed-application-new.spec.md) | `quartzite-renderer` | ✅ implemented (1 new test — `windowed_application_new_succeeds` exercises early-return path via `Application` singleton; accepts Ok / Application(AlreadyExists) / EventLoop(_) triad. `WindowedApplication::new() -> Result<Self, RendererError>` added as `#[inline]` shorthand for `builder().build()` — mirrors `Application::new()`. All no-option `builder().build()` call sites swept (3 doc-examples + 1 integration test). `xvfb_smoke.rs` prose updated. Closes #567.) | — |
| [tickless-eventloop](done/2026-05-24-tickless-eventloop.spec.md) | `quartzite-runtime` `quartzite-core` | ✅ implemented (8 new tests — AC9a tickless-run exits on request_stop, AC9b no spurious wake-ups, AC9c tick-based fires multiple ticks, AC15 EventLoop class_name + object_base/id, AC17/AC19 invoke_method("stop") exits loop via reflection, AC19(b) Signal<()> connected to Application::quit via quit(&self), AC20 Application has non-zero ObjectId, not in ObjectTree; tickless EventLoop (`Option<Duration>` tick policy: `None` → `recv()` blocks until message, `Some(d)` → `recv_timeout(d)`); `EventLoop::with_tick(Option<Duration>)` + `EventLoop::new()` stays tickless-default; `Application::new()` removed — `Application::builder().build()` is the sole entry point; `ApplicationBuilder` with `tick_duration(Option<Duration>)` setter; `WindowedApplicationBuilder::tick_duration` forwarded to inner `ApplicationBuilder`; `EventLoop::spawn(tick, f)` signature extended; `const TICK_MS` removed; `EventLoop` derives `#[derive(Extend, Object)] #[root]` with `#[slot] stop(&mut self)`; `Application` hand-rolled `AsObject` + `Object` (b1 path — tuple struct `Application(Arc<ApplicationInner>)` cannot use `#[derive(Object)]` which requires named fields); `request_stop(&self)` shim on `EventLoop`; `Application::quit(&self)` unified (callable from any context); `SlotEntry::callback` bound widened from `Send` to `Send + Sync`; `#[doc(hidden)] pub fn tick()` test-only accessor; AC18 amended: `#[slot]` annotation not applicable to b1 hand-rolled impl; spec amendment recipe applied. 7 commits: `52a3a08` (subtask 1 — tick policy + ObjectBase field), `3b26d13` (subtask 3 — ApplicationBuilder + Application hand-rolled Object), `f49469d` (subtask 4 — WindowedApplicationBuilder tick_duration), `2241f84` (subtask 5 — workspace-wide call-site sweep), `31df570` (subtask 6 — new tests AC9a–c/AC15/AC17/AC19/AC20), `c797b10` (docs — intra-doc link fix), `08c9cf0` (self-review fixes: `#[inline]` on 4 concrete trait-impl methods, AC17/19 test strengthened, AC19(b) signal-to-quit test added, spec/design amended). AC1–AC21 PASS. Closes #561.) | — |
| [inspect-repr-annotation-usage](done/2026-05-24-inspect-repr-annotation-usage.spec.md) | `quartzite-widgets` `quartzite-geometry` `ai-docs/code-style.md` `AGENTS.md` | ✅ implemented (0 new tests; refactoring — strips 5 decorative `#[repr(i64)]` annotations from `MetaEnum`-derived enums (`FocusPolicy`, `SizePolicy`, `CursorShape` in `quartzite-widgets/src/enums.rs`; `ScrollPolicy` in `quartzite-widgets/src/widgets/scroll_area.rs`; `HAlignment` in `quartzite-geometry/src/h_alignment.rs`); adds `## Enum repr` usage rule to `ai-docs/code-style.md` documenting when `#[repr]` is required (`enumflags2::bitflags`, external numeric spec) vs forbidden (decorative). 4 commits: `8dfea2b` (enums.rs ×3), `5d888de` (scroll_area.rs), `4b918aa` (h_alignment.rs), `63a959a` (code-style.md + AGENTS.md). AC1–AC11 PASS. Closes #558.) | — |
| [fix-ac10-public-docs](done/2026-05-24-fix-ac10-public-docs.spec.md) | `quartzite-renderer` `quartzite-core` `quartzite-style-types` `quartzite-style` | ✅ implemented (0 new tests; docs-only fix — strip every `AC<digit>` token from `///` doc comments in `src/**` across 8 files; correct factual error at `vello_painter.rs:27` (gradient brushes ARE supported via vello; only future `#[non_exhaustive]` `BrushKind` variants fall through to the no-brush sink); 4 commits on the branch: `e43a6d3` (quartzite-renderer sweep), `f97c1c2` (quartzite-core sweep), `aa52dc4` (quartzite-style-types sweep), `8005977` (quartzite-style sweep + workspace gates). Deferred guard gh issue #559 created. AC1–AC12 all PASS. Closes #557.) | — |
| [design-system-conformance](done/2026-05-24-design-system-conformance.spec.md) | `quartzite-paint-api` `quartzite-renderer` `quartzite-style` | ✅ implemented (new symbolic test `button_and_label_use_vertical_centre` (AC8) + golden regen for Button + Label + LineEdit + TextEdit — extends `Painter::draw_text_in` with `h_align: HAlignment, v_align: VAlignment` (Design Amendment 2 — PR #556 reviewer request: split `Alignment` → `HAlignment { Left, Center, Right, Justify }` + `VAlignment { Top, Center, Bottom }`, type system enforces no `Justify` on vertical axis; clean break, all impls updated in lockstep: `VelloPainter`, `RecordingPainter`, `NoopPainter`, dispatch shim, paint-widget thunk, registry dispatch, style dispatch, third-party-paint test fake, `paint-util` panic-safety impl); vertically centres Button + Label text (`VAlignment::Center`); replaces PR #554 LineEdit smaller-rect recipe with `draw_text_in(geom, …, HAlignment::Left, VAlignment::Center)`; TextEdit uses explicit top-anchor (`HAlignment::Left, VAlignment::Top`); **`ai-docs/design-conformance-audit.md`** written covering Button / Label / LineEdit / TextEdit / ScrollArea / Container — no open `❌` rows at PR merge; Label mock-drift row classified `N/A` (documentation-only; separate follow-up); 12 Button goldens + 6 Label goldens + 3 TextEdit goldens regenerated (light + dark); `context.md` + README.md updated. Design-review ran 3 rounds (R1 GO; R2 GO; R3 GO after Design Amendment replacing non-canonical `# Parameter order` section with inline bold prose). Self-review ran 2 rounds (R1 REJECT: 3 findings — non-canonical doc section, missing `#[should_panic]` test, Label audit `❌` row; R2 APPROVE zero findings). 7 commits on the branch: `9816b38` (paint-API extension), `8fd1303` (Button + Label centring), `61276a1` (LineEdit v_align), `cf58e8d` (symbolic tests + AC8), `c2645ab` (conformance audit doc), `99099ae` (golden regen), `8fe6aa8` (self-review R1 fixes). Closes #555.) | — |
| [propagate-embedded-tools](done/2026-05-23-propagate-embedded-tools.spec.md) | instruction surface (`.claude/skills/ai-audit/SKILL.md` + `.claude/skills/bugfix/SKILL.md` + `.claude/agents/design-review.md` + `.claude/agents/spec-writer.md` + `ai-docs/claude-tools-hierarchy.md`) | ✅ implemented (0 new Rust tests — instruction-file-only PR — drops project-side simulations of embedded Claude Code primitives in favour of `Agent`-spawn calls to the embedded Subagents that already provide the same capability. **Class A** (Class A1+A2): `/ai-audit` Phase 2 rewritten to spawn the embedded `claude-code-guide` Subagent via `Agent` (one call, returns verbatim shape contracts for skill frontmatter / Subagent file structure / Hook event matchers across `code.claude.com/docs/en/{skills,sub-agents,hooks-guide}`); anti-pattern note (line 186) updated to point at the `claude-code-guide` spawn instead of `WebFetch`. **Class B** (B1): `/bugfix` Step 1 read-only trace rewritten to spawn the embedded `Explore` Subagent via `Agent` with the verbatim `.claude/rules/ast-index.md § Rules for subagents` block embedded in the prompt (per AGENTS.md mandatory-block rule); ASCII trace template + inner steps (artefact creation, `current_step` tracking) preserved — `Explore`'s output feeds the existing template. **Class C** (C1–C4): `.claude/agents/design-review.md` + `.claude/agents/spec-writer.md` `tools:` frontmatter rewritten — both drop `Grep`/`Glob` and add `Agent` (commits the project to nested `Agent`-from-Subagent spawns as a supported pattern); `Read` + `Bash` retained on both per Class F decisions. `design-review.md` body line 20 rewritten: `Every suspicion — investigate via Read/grep` → `Every suspicion — investigate via Read for known paths, or spawn Explore via Agent for code search across the workspace`. `spec-writer.md` body line 30 rewritten: `Use Grep against AGENTS.md` → `Use Bash to grep -nE '<keyword>' AGENTS.md` (AGENTS.md is a known single path → `Bash` `grep` lighter than spawning `Explore`; parity with the existing C5 Rule-5 mechanical-check block). C5 (Rule-5 substring blacklist `printf '%s\n' … \| grep -iE …`) byte-for-byte unchanged — verified by AC10. **Class D** (D1–D4): `ai-docs/claude-tools-hierarchy.md` §1a gains rows for `Grep` (🟦, regex search across files, full parameter list) and `Glob` (🟦, fast filename pattern matching, parameters `pattern`/`path`); §2b `Tools`-column synced for both rewritten Subagents (`design-review` → `Read`, `Bash`, `Agent`; `spec-writer` → `Read`, `Write`, `Edit`, `Bash`, `Agent`); existing column order preserved; 6 stable anchors intact (AC12). **Class E** (audit-only, no rewrite this PR): per-Subagent audit table E1–E6 recorded inline in the spec for `design` / `self-review` / `review-findings` / `triage-runner` / `learnings-escalation-audit` / `self-improve` — outcome 5 × ✅ keep, 1 × ⛔ inline mandatory (E6 `self-improve` lacks the `Agent` primitive per 2026-05-15 learnings); zero 🟡 candidates → no follow-up rewrites surfaced. **Class F** (`Bash`-retention review for Class C rewritten Subagents): F1 `design-review` keep (`cargo` / `gh` shell-outs); F2 `spec-writer` mandatory keep (Rule-5 mechanical-check block + Class C4 `Bash` `grep` rewrite + `gh issue view` lookups). **Spec ran 4 interview rounds** with round-3 user-directed promotion of the two deferred items into in-scope Class E + Class F. **Design-review ran 2 rounds** (R1 GO with 2 spec-amending notes on broken AC10 + AC13 verifiers — both folded via Spec Amendment recipe; R2 GO with 1 minor note on subtask 9(a) stale wording — folded back). **Step 8 executed across 3 handoff groups** (3+4+2 subtasks) with `/context-reset` between each. **Self-review APPROVE Round 1** zero findings, one informational `nit` recorded re: consistency drift between `design-review.md` body rewrite and sibling subagent files (intentional per spec § Out of scope; surfaced for future `/triage`). AC1–AC16 all PASS. **Two learning entries appended in-session** documenting (a) misleading "SendMessage not available" framing in `/interview` round-2+ cold-spawn, and (b) follow-up investigation for warm-hold spec-writer across rounds; both `Escalated? no` per Boundary rule 2. 8 commits on the branch: `50958ea` (subtask 1 — ai-audit Phase 2 Class A1), `3b129ff` (subtask 2 — ai-audit anti-pattern Class A2), `c807bfe` (subtask 3 — bugfix Step 1 Class B1), `9a4df4c` (subtask 4 — design-review tools Class C1), `e37e22f` (subtask 5 — design-review body Class C2), `cb4a625` (subtask 6 — spec-writer tools Class C3), `06dbdc2` (subtask 7 — spec-writer body Class C4), `5600fdf` (subtask 8 — claude-tools-hierarchy Class D1–D4). Closes #549.) | — |
| [harness-taxonomy-unify](done/2026-05-23-harness-taxonomy-unify.spec.md) | instruction surface (`AGENTS.md` + `ai-docs/claude-tools-hierarchy.md` (new) + `ai-docs/workflow.md` (new `## Spec-Amendment group` section) + 15 `.claude/skills/**/*.md` + 8 `.claude/agents/**.md` + `.claude/rules/ast-index.md` + `ai-docs/{code-style,doc-convention,context,agent-writing-style,corrections-log}.md` + `.claude/skills/verify/` → `.claude/skills/verify-change/` (folder rename) + `.claude/skills/ai-audit/{SKILL.md,reference.md}` (Checklist O) + `ai-docs/templates/progress-format.md` + `ai-docs/plans-summary.md` + `ROADMAP.md` (auto-regen)) | ✅ implemented (0 new Rust tests; instruction-file refactor only — unifies Claude-Code-harness terminology across the project's full instruction-file corpus around `ai-docs/claude-tools-hierarchy.md` as single source of truth for the four naming axes (`Tool`/`Subagent`/`Skill`/`Hook`). AC1–AC5 substring blacklists swept clean across 37 in-scope files (`the X tool` / `sub-agent` / lowercase `agent` → `subagent` with 5-item carve-out whitelist / generic `slash command` / hook references without event-name+matcher); AC6 on-demand pointer pattern preserved. AC11 stable-anchor freeze: new `## Stable anchors` section in canonical doc + Option-B `<a id="x"></a>` + `<!-- stable-anchor: #x -->` pairs above each of 6 load-bearing headings (no blank line between marker/anchor/heading). AC10 new Propagation Rule sync-group row added verbatim per spec wording. AC16 new AGENTS.md AXIOM block (three-row fail-loud table per `agent-writing-style.md § Patterns`) forbidding project-vs-embedded name clashes (project side renames; embedded never). AC17 `/ai-audit` Checklist O ("Embedded-name clash scan") added with `FNR==1{f=0}` per-file reset on both project-names awk invocations (Round 1 self-review caught a multi-file state-leak bug; Step 11 commit `7b8a1da` fixed it before push). AC18 first incidental fix under the new policy: `.claude/skills/verify/` → `.claude/skills/verify-change/` (4 inbound refs updated; clash with embedded `/verify` Skill closed). AC19 post-rename clash-scan baseline empty (intersection of 26 project names vs 55 embedded names = zero). AC21 new §3a row in `claude-tools-hierarchy.md` for the embedded `verify` Skill with verbatim upstream description (Embedded Skills count 11 → 12). AC9 hard size guard held: AGENTS.md ended at 36,792 chars (≤ 36,808 ceiling; 16-char cushion); `.claude/agents/triage-runner.md` held exactly at 37,916 (warning-band ceiling preserved via offsetting "actually " trim at L406); no in-scope file crossed 40k. AC14 Propagation Rule co-edits all satisfied across 8 sync groups. Net corpus delta +6,471 chars (largest contributor: Checklist O recipe +3,159 chars in `ai-audit/reference.md`; offset on AGENTS.md side by 4 pre-budgeted shrinkages targeting L213+L214+L215 cross-reference collapse, L216 Spec-Amendment row extraction into new `## Spec-Amendment group` section in `ai-docs/workflow.md`, L210 self-row tightening, L211 Learning-Log parenthetical trim). Design-review ran 2 rounds (R1 ITERATE: 2 majors — anchor-slug GFM auto-slug mismatch + AC9 offset arithmetic missed +42 chars from AC3 sweep; both fixed with Option-B explicit anchors + +1,012 char-corrected budget; R2 GO with 2 `note` items folded back per AGENTS.md GO-with-notes contract). Self-review ran 2 rounds (R1 REJECT: 1 major Checklist O awk bug + 1 minor §3c description copy-paste + 1 nit AC10 row paraphrased; all 3 fixed in `7b8a1da`; R2 APPROVE no regressions). Spec ran 4 interview rounds (round-cap hit exactly — user added clash-rename scope between R2 and R3 + embedded `verify` §3a row scope between R3 and R4). 8 implementation commits on the branch: `8787745` (plan artefacts), `e5f400a` (canonical doc Group A.1), `0bd3880` (AGENTS.md core Group A.2), `85f873b` (`.claude/skills/` sweep Group A.3), `e856fd1` (`.claude/agents/` + `.claude/rules/` sweep Group B.4), `1bc3c90` (`ai-docs/*.md` sweep Group B.5), `a998e63` (`verify` rename + Checklist O Group B.6), `7b8a1da` (Step 11 round-1 self-review fixes). Closes #547.) | — |
| [triage-ui-design-blocker](done/2026-05-23-triage-ui-design-blocker.spec.md) | instruction surface (`.claude/skills/triage/SKILL.md` + `.claude/agents/triage-runner.md` + new `ai-docs/triage-runner-bridge.md` + new `ai-docs/triage-runner-design-links.md` + `AGENTS.md` § Agent Docs + `ai-docs/agent-docs-index.md`) | ✅ implemented (0 new Rust tests; instruction-files-only PR — extends `/triage` so every newly-created gh issue is classified design-work / plain before `gh issue create`, with design-work rows linked to a `ui-design`-labelled umbrella via the convention #539–#542 already use. **Hybrid classification trigger**: keyword scan (23-entry list `style`/`paint`/`palette`/`colorrole`/`widget`/`chrome`/`visual`/`theme`/`snapshot`/`Highlight`/`FocusRing`/`caret`/`selection`/`scrollbar`/`popup`/`tooltip`/`dialog`/`modal`/`icon`/`font`/`IME`/`RTL`/`BiDi`; case-insensitive substring scan against `Item` cell + `Source spec` filename); hit → y/n confirm; no-hit → LLM auto-detect (orchestrator reads ONE source-spec file, infers `DESIGN-WORK | PLAIN` + one-line reason, emits y/n confirm with inference as default). **Numbered umbrella menu** ordered by keyword-overlap rank (same 23-entry list; ties broken by `#N` ascending), followed by text options `new` / `none` / `defer`. **`new` branch** collects title + body via two follow-up prompts, calls `gh issue create --label ui-design` inline in the same Phase 7.5 pass (queue partitioned `[umbrellas..., children...]` so umbrella `#N` is available before dependent children construct their `**Blocked by:** #N` body line). **Umbrella body auto-edit**: after each child link, fetch umbrella body via `gh issue view --json body`, locate `## Child issues (blocked on this epic)` anchor (verbatim convention #539–#542 use), append `- #<child-N> — <title>` at end of section's bullet list (EOF-or-next-h2 boundary detection), push back via `gh issue edit --body-file`; idempotency via `#<child-N> ` trailing-space sentinel; **two distinct fallback sub-lists** in Phase 8 summary — `Body-edit skipped — anchor absent` (per-umbrella structural state) AND `Body-edit failed — gh API error` (per-run transient state). **`/next` SKILL.md propagation step**: every umbrella-mutating run greps `.claude/skills/next/SKILL.md` for stale specific-umbrella references in the same run (mandatory; edit optional). **Progress-file `design_link:` sub-field** with 5-value enum (`none` / `umbrella=#N` / `umbrella=#N (new)` / `skip-link` / `defer`); resume from partly-prompted partition skips already-recorded decisions. **Phase 4 dedupe-map shape extended** from `{number → {state, title}}` to `{number → {state, title, labels, body}}` via `--json number,state,title,labels,body` — labels for the `ui-design` filter, body for overlap-ranking; pagination watchdog (≥ 450) + one-bulk-call contract preserved. **Design phase ran 4 design-review rounds** — R1 GO with 3 minor notes (folded into design before Step 8 start); R2 ITERATE with 2 blockers post-Group-B return (handoff-plan rule-(b) violation + char-budget math off; both resolved); R3 GO with 1 round-trip handoff-plan sync note; R4 GO with 2 minor notes after Amendment 5 Task-9 sibling redirect. **Step 8 executed across 4 handoff groups** (3+3+3+2 per amended plan) with `/context-reset` between each. **5 design amendments** during Step 8 (Amendment 1 retroactive Group-B compression acknowledgment; Amendment 2 Task 6.5 bridge extraction; Amendment 3 char-budget table with verified math; Amendment 4 handoff-plan rule-(b) conformant regrouping; Amendment 5 Task 9 sibling redirect after Group D budget breach). **Two new sibling reference files** created via Task-6.5 extraction pattern: `ai-docs/triage-runner-bridge.md` (Phase 4.5 action semantics; 1,896 chars) + `ai-docs/triage-runner-design-links.md` (Phase 8 Design-link outcomes content per AC6 + AC10; 3,719 chars). Both propagated to AGENTS.md § Agent Docs row + `ai-docs/agent-docs-index.md` body. **Hard-ceiling preservation**: `triage-runner.md` final 37,916 chars (342-char cushion under 38,258 amended ceiling; under 40k AGENTS.md hard cap). Group B silent-compression deviation retroactively accepted in Amendment 1 (operational contract preserved); Group C Task 7 (+4 chars over per-task ceiling) and Group D Tasks 9+10 (+443 chars combined over budget) properly surfaced — NOT silently compressed (Amendment 3's anti-pattern check works as intended). Self-review APPROVE Round 1 zero findings. AC1–AC11 PASS. 4 implementation commits on the branch: `d772533` (Group A — map shape + keyword list + classification trigger), `3f91366` (Group B — LLM read-scope + ranking + body auto-edit), `846919f` (Group C — bridge extract + text-options + design_link field), `3958678` (Group D — design-link outcomes extracted + closing notes). Closes #545.) | — |
| [next-ui-designer-queue](done/2026-05-23-next-ui-designer-queue.spec.md) | instruction surface (`.claude/skills/next/SKILL.md`) | ✅ implemented (0 new Rust tests; instruction-file-only edit — four mechanical inserts into `.claude/skills/next/SKILL.md` per design § Exact edit recipe: (a) `ui-design`-label skip bullet appended to Default-mode Selection rules at line 72 parallel to the existing `blocked` skip bullet at line 71; (b) same skip bullet appended to Small-mode Selection rules at line 83 parallel to existing `blocked` skip bullet at line 82; (c) new "UI-designer label" mini-section inserted at lines 95–101 immediately after the existing "Blocked-issues label" mini-section (lines 87–93), documenting the `ui-design` label's color `#E91E63`, description "Design-system designer pass / visual spec work required", apply command `gh issue edit <N> --add-label ui-design`, remove command, and the rendering rule that filtered issues surface in the new informational section; (d) new "Candidates for UI-designer handoff (informational)" bullet appended to the "Output (both modes)" block at line 119 immediately after the existing "Candidates needing `/triage` (informational)" bullet at line 118, carrying the `#N — <title> (<link>): <one-line rationale>` per-row format, the verbatim contract sentence "**Items in this section are never the top-line recommendation or a runner-up**" mirroring the `/triage` informational sibling, the out-of-harness reminder, and AC8's schema-stability rule "**Section always renders** — when zero issues carry the label, the body is the single line \`No issues currently labelled \\\`ui-design\\\`.\`" with the literal escaped-backtick string preserved character-for-character. Propagation grep `grep -rn "ui-design" .claude/skills/triage/ .claude/agents/triage-runner.md` re-run at Group A: zero hits (AC7 satisfied — `ui-design` label is a `/next`-only concept; Triage group siblings need no edit). Post-edit `wc -c .claude/skills/next/SKILL.md` = 7,985 bytes (pre-edit 5,989; +1,996 chars / +11 lines), far under the 35k early-warning cap. Design-review verdict Round 1 = GO with zero issues and zero recommendations (anchors verified line-by-line; live `gh issue list` confirms exactly 4 `ui-design`-labelled issues #539, #540, #541, #542; propagation grep claim verified; design-system visual rules N/A — instruction-file-only). Self-review APPROVE Round 1 zero findings. AC1–AC8 PASS. Single commit on the branch: `825a315` (four inserts in one logical unit per design § Decomposition M=1). Closes #543.) | — |
| [rustflags-strictness-audit](done/2026-05-23-rustflags-strictness-audit.spec.md) | CI infra (`.github/workflows/{ci,coverage}.yml` + `scripts/check-rustflags-uniformity.sh` (new)) | ✅ implemented (0 new Rust tests — CI-workflow + bash-script changes only; design-phase counter-example validation per AC5 was transient and not committed). Three-dimension audit (RUSTFLAGS env / `setup-rust-toolchain@v1` `rustflags:` input / RUSTDOCFLAGS env on rustdoc cargo steps) confirms uniformity across all 12 `setup-rust-toolchain@v1` invocations at the `-D warnings` action default; the audit table was lifted verbatim from `design.md` § Audit table into the spec body § Audit table per the Artefact-placement note. AC3 closes the single observed drift point — `coverage.yml`'s `Generate coverage` step (`cargo llvm-cov ... --doctests`) gains a step-level `env: RUSTDOCFLAGS: "-D warnings -D missing-docs"` block; local nightly validation under the post-fix RUSTDOCFLAGS: 89 doctests, 0 warnings. AC4 ships `scripts/check-rustflags-uniformity.sh` — fail-closed bash guard mirroring `check-rustdoc-internal-refs.sh`'s precedent, three orthogonal invariants A/B/C with three separate allow-list arrays (`ALLOWLIST_RUSTFLAGS_INPUT`, `ALLOWLIST_RUSTFLAGS_ENV`, `ALLOWLIST_RUSTDOCFLAGS_EXEMPT`, all empty at authoring time), POSIX bash + `rg` (PCRE2 capability probed at startup), unified `<file>:<line>: <invariant>: <message>` output to stderr, exit codes `0`/`1`/`2`. Invariant C tactical departure from the design's PCRE2 multi-line capture sketch: awk block-capture from the `run:` line stopping at the next `- name:` step header — same contract, more robust boundary; documented in script header step-shape note. AC5 counter-example validation cycled each invariant exit-1↔0 cleanly. AC6 wires `Check RUSTFLAGS uniformity` step into `ci.yml` `docs` job at line 448-449 immediately after `Check rustdoc has no repo-internal references` (reuses the existing `ripgrep` apt install at line 441). AC7 `actionlint .github/workflows/coverage.yml .github/workflows/ci.yml` exit 0. Design GO Round 2 with 3 minor notes + 2 recommendations (all folded into `design.md` before Step 8). Self-review APPROVE Round 1, 3 nits, zero blockers/majors/minors. 3 commits: `e0e1647` (AC3 drift fix), `d3deb4d` (AC4 guard script), `ad07a19` (AC6 CI wiring). Closes #385.) | — |
| [rename-code-review-to-project-review](done/2026-05-22-rename-code-review-to-project-review.spec.md) | instruction surface (`.claude/skills/project-review/SKILL.md` renamed from `.claude/skills/code-review/SKILL.md` + `.claude/agents/{review-findings,self-review,self-improve,learnings-escalation-audit}.md` + `AGENTS.md` + `.claude/skills/context-reset/SKILL.md` + `.claude/skills/ai-audit/reference.md` + `ai-docs/{doc-convention,corrections-log,agent-writing-style,agent-docs-index}.md` + `ai-docs/templates/progress-format.md` + `ai-docs/plans/INDEX.md` + `ai-docs/deferred/{_inbox,ci-docs-workflow}.md` + `ROADMAP.md` (auto-regen)) | ✅ implemented (0 new Rust tests; pure instruction-file rename — `/code-review` skill renamed to `/project-review` to resolve semantic collision with the diff-style `self-review` / `review-findings` agents it spawns. `git mv` preserves SKILL.md history chain (R091 rename detection). Token swap across 15 instruction files plus the moved SKILL.md itself. Two-agent `description:` frontmatter fields re-read for grammaticality after substitution. Typo-source substring `skillcode-review` preserved verbatim on `learnings-escalation-audit.md:105` + `corrections-log.md:15` per the typo-correction example contract; AC6 grep excludes those two files. `.claude/skills/context-reset/SKILL.md` canonical anchor `## Compaction recovery (re-entry)` preserved verbatim. `ai-docs/plans/INDEX.md:61` `done/2026-05-14-sonnet-skill-reentry-protocol.spec.md` file-link target preserved (only narrative-cell `code-review` token rewritten). `.claude/settings.local.json` is gitignored — on-disk rewrite for naming-consistency but does NOT enter PR diff; AC13 grep verifies on-disk content. Pre-publish API-stability AXIOM applies: no compat shims, clean break — users who type `/code-review` will see "skill not found" after merge (intended). Design-review ran 3 rounds (R1 ITERATE: AC10 AGENTS.md size cap unachievable as written + subtask 4 underspec; R2 GO; R3 ITERATE→GO after Step-8 spec amendment for AC6 exclusion list + settings.local.json gitignored correction + ROADMAP.md auto-regen documentation). Self-review APPROVE Round 1 zero findings. AC10 (former AGENTS.md size gate) dropped during R1 spec amendment — extraction work belongs to `/ai-audit`'s lane, not this rename. AC1–AC16 PASS. 5 commits on the branch: `a436749` (skill rename + SKILL.md), `87c09ee` (Review-group sibling agents), `200ccbe` (AGENTS.md + context-reset SKILL), `2958fe7` (remaining forward-living files), `9a8c356` (historical-narrative + ROADMAP.md auto-regen). Closes #529.) | — |
| [raii-guard-painter-transform](done/2026-05-22-raii-guard-painter-transform.spec.md) | `quartzite-paint-util` (new) `quartzite-style-dispatch` | ✅ implemented (7 new tests — 5 unit tests (constructor order, drop-once, full lifecycle, accessor, zero-origin) + 1 panic-safety integration test (`drop_records_restore_when_scope_panics` via `catch_unwind`) + 1 doc test; adds `quartzite-paint-util` `no_std` crate with public `TranslateGuard<'a>` RAII guard — constructor calls `Painter::save` then `Painter::translate(origin)`, `Drop` calls `Painter::restore`; explicit `painter(&mut self) -> &mut dyn Painter` accessor chosen over `DerefMut`; `#[inline]` on all three concrete methods; `std` default feature gates panic-safety test; refactors `quartzite-style-dispatch::dispatch::visit` to use the guard — all 12 existing dispatch tests pass byte-identically. Design-review ran 2 rounds (R1 ITERATE: `std` feature aggregation required root-crate absent-dep fix — switched to default feature; 3 minors fixed; R2 GO no issues). Self-review ran 2 rounds (R1 REJECT: 5 doc-convention violations — AC3/AC8 spec citations in H2 headings + missing `# Parameters` + 2 missing `# Examples`; R2 APPROVE no findings). AC1–AC9 PASS. Closes #410.) | — |
| [collapse-widgetext-paint](done/2026-05-22-collapse-widgetext-paint.spec.md) | `quartzite-widgets` `quartzite-style` `quartzite-renderer` | ✅ implemented (0 new tests; pure-deletion + one-call-site migration — removes the no-op `WidgetExt::paint` default method (zero in-tree overrides) and the now-unused `quartzite_paint_api::Painter` import from `widget_ext.rs`. Migrates the sole in-tree caller (`snapshot_widget` test helper in `quartzite-widgets/tests/support/mod.rs`) to drive `DefaultStyle.draw_widget(widget, p, &Palette::default())` directly, matching the sync-group sibling pattern from `quartzite-style/tests/snapshots.rs`. `quartzite-widgets/tests/no_style_dep.rs` updated to scope its `cargo tree` assertion to `--edges=normal` (production edges only) to permit `quartzite-style` as a dev-dependency without breaking the cycle-break guard. Documentation sweep: 5 files updated to drop/rewrite `WidgetExt::paint` references (`quartzite-widgets/tests/snapshots.rs`, `quartzite-style/tests/snapshots.rs`, `quartzite-style/tests/support/mod.rs`, `quartzite-renderer/src/render_harness.rs`, `quartzite-renderer/src/window_root.rs`). Design-review ran 2 rounds (R1 ITERATE: `no_style_dep.rs` cycle-break blocker + 3 minor issues; spec amended adding AC11 + widening AC2 exemption list; R2 GO with 2 notes — redundant cast dropped from subtask 3 snippet, doc gate before grep ordering in subtask 6). Self-review APPROVE Round 1 no findings. AC1–AC11 PASS. 5 commits on the branch: `4f35296` (no_style_dep --edges=normal), `c1ae2ae` (quartzite-style dev-dep), `70502e7` (snapshot_widget migration), `f4134c1` (WidgetExt::paint removal), `7d6b71a` (doc sweep). Closes #409.) | — |
| [facade-style-dispatch-feature](done/2026-05-22-facade-style-dispatch-feature.spec.md) | `quartzite` (facade `Cargo.toml` + `src/lib.rs`) | ✅ implemented (1 new compile-only test; additive facade feature — adds `style-dispatch = ["dep:quartzite-style-dispatch", "style", "widgets"]` to `quartzite/Cargo.toml` with `##` doc-comment; appends `"style-dispatch"` to `[package.metadata.docs.rs] features`; adds `pub mod style_dispatch { pub use quartzite_style_dispatch::*; }` gated on `#[cfg(feature = "style-dispatch")]` + `#[cfg_attr(docsrs, doc(cfg(feature = "style-dispatch")))]` between `widgets` and `prelude` modules; adds Ecosystem bullet for `quartzite-style-dispatch` after the `paint` bullet; adds AC6 compile-only test verifying all four chained-feature surface items (`dispatch_paint`, `WidgetResolver`, `Palette`, `WidgetBase`). Design-review GO Round 1 no issues. Self-review APPROVE Round 1 no findings. AC1–AC11 PASS. 4 commits. Closes #393.) | — |
| [reframe-size-cap-axiom](done/2026-05-22-reframe-size-cap-axiom.spec.md) | `AGENTS.md` + `.claude/skills/ai-audit/reference.md` + `ai-docs/skill-size-exemptions.md` + `ai-docs/agent-writing-style.md` | ✅ implemented (0 new Rust tests; instruction-surface only — reframes the AGENTS.md instruction-file size-cap AXIOM as advisory severities (no mechanical gate) and widens the audited file set. 5 commits across 4 instruction files (text-only edits). AGENTS.md AXIOM block (lines 61–70): three-band table reframed to `major` (≥ 40,000) / `minor` (35,000–39,999) / `OK` (< 35,000) — vocabulary unified with `/ai-audit` Sub-check 9 severity column (so audit findings referencing the rule need no translation); `STOP` removed; trailing "Until `scripts/check-instruction-file-sizes.sh` lands as a pre-commit / CI gate, ..." sentence removed (the script was the source of #383 in its original framing; user direction 2026-05-22 explicitly rejected the gate — sizing work stays in `/ai-audit`'s lane). Applies-to prose + `wc -c` quick-scan command widened to `.claude/skills/**/*.md` (was `.claude/skills/**/SKILL.md` — now covers `reference.md` siblings + any other in-skill markdown) AND new `.claude/rules/*.md` flat glob (rules surface introduced by PR #521). Asymmetric `**` (skills, nested per-skill) vs `*` (rules, flat — currently single `ast-index.md`) is documented in spec Key Decisions row 5 so future Propagation-Rule sweeps don't "fix" the inconsistency. AGENTS.md downstream rows (line 220 Propagation-Rule Size-exemption-index row + line 253 Agent Docs row): "deferred `scripts/check-instruction-file-sizes.sh` (#383)" forward pointers dropped. `.claude/skills/ai-audit/reference.md` Sub-check 9 (lines 132–155): severity `blocker` → `major` (35,000–39,999 stays `minor`); `wc -c` command widened to match AGENTS.md (single-invocation form preserved per spec § Technical constraints); trailing "mechanical pre-commit gate is planned in #383..." sentence replaced with plain restatement "Sub-check 9 is the sole enforcement surface for the size-cap rule". `ai-docs/skill-size-exemptions.md`: line-22 Consumers bullet + lines 110–114 Notes "Deferred" bullet both dropped — Consumers list collapses to single `/ai-audit` Checklist K item 1 entry. `ai-docs/agent-writing-style.md § 8`: 2-line forward-pointer sentence dropped; severity table reframed `STOP` → `major` + `minor` label added; covered-set bullet widened (`.claude/skills/**/*.md`) + new `.claude/rules/*.md` flat bullet. Propagation-Rule sweep across `.claude/agents/`, `.claude/skills/`, `.claude/rules/`, `ai-docs/agent-writing-style.md` confirms remaining narrow-glob hits are all in named non-size-cap contexts (Checklist M intro + audited-corpus, Checklist N forward-direction `## Patterns` corpus, anchor-aware re-verification × 2, Propagation-rule corpus). AGENTS.md 36,046 → 35,795 chars (net −251; still in `minor` band but moving downward — well under the 40k threshold per AC7). 5 implementation commits on the branch: `eb6243b` (AGENTS.md AXIOM block), `e16a823` (AGENTS.md downstream rows), `0d96476` (`/ai-audit` Sub-check 9), `e226903` (`skill-size-exemptions.md` cleanup), `d7a55f6` (`agent-writing-style.md § 8` propagation). Design-review verdict Round 1 = GO with 2 minor notes (Test Design AC1 active-vs-historical surface clarification + Subtask 5 expected-false-positives enumeration) — both folded into the design before Step 8 per the GO-with-notes round-trip contract. Self-review APPROVE Round 1 zero findings; AC1–AC7 PASS pre-merge; AC8 = this APPROVE. Closes #383.) | — |
| [lineedit-hover-pressed-focused](done/2026-05-22-lineedit-hover-pressed-focused.spec.md) | `quartzite-style` (`impl Paint<LineEdit> for DefaultStyle`) | ✅ implemented (10 new recording-painter tests + 9 new snapshot goldens; brings `impl Paint<LineEdit> for DefaultStyle` to parity with the post-#403 Label/TextEdit/ScrollArea widgets, folds in #407 disabled-axis parity with TextEdit. Reuses the shared `state_group(pressed, hovered)` selector + post-#402 two-axis palette API + `maybe_disabled` / `read_only_overlay` / `disabled` / `FOCUS_RING_WIDTH` helpers — no new helpers, no new public API, no `Style`/`Paint` trait changes. State-resolved + `maybe_disabled`-wrapped `(fill_color, text_color, outline_color_idle)` flow through the preserved 3-arm placeholder/read-only/text-content ladder; placeholder is drawn at `disabled(maybe_disabled(palette.color(text_role, group), enabled))` so its dim composes orthogonally on top of the state-resolved text colour (enabled-placeholder ≈ × 0.5, disabled-placeholder ≈ × 0.25). `FocusRing` 2 px outline exempt from `maybe_disabled` (full alpha always, per design-system additive-overlay rule + shipped Button/Label/TextEdit/ScrollArea behaviour). **#407 fold-in:** the pre-spec `impl Paint<LineEdit>` applied `maybe_disabled` to none of its colours — disabled-LineEdit on master rendered identically to enabled-LineEdit; post-spec it matches TextEdit's halved-alpha treatment on `Base` fill + `Text` outline + `Text` glyphs. 10 new recording-painter tests under `default_style_tests.rs`: 5 state-axis (`hovered/pressed/focused/disabled_and_focused/precedence_pressed_hovered`), 1 disabled-idle anchor (`line_edit_disabled_idle_dims_base_text_outline` — AC2 / #407 fold-in), 1 read-only×state interaction, 3 placeholder×state (incl. `line_edit_disabled_placeholder_composes_double_dim` for the × 0.25 composition). 9 new snapshot goldens under `quartzite-style/tests/snapshots/shared/`: 7 light (`line_edit_{idle,hovered,pressed,focused,disabled,read_only,placeholder}.png`; `line_edit_disabled.png` is the visible #407 anchor) + 2 dark (`dark_line_edit_{idle,focused}.png`). All 9 existing `line_edit_*` recording-painter tests + read-only variants (lines 1023–1244 of `default_style_tests.rs`) pass byte-for-byte unchanged (AC6 — new state flags default to false, `is_enabled() == true` defaults `maybe_disabled` to identity). `default_style.rs` 358 → 388 lines (78% utilisation vs 500-line soft target, 112-line headroom). Design-review ran 1 round (GO with 2 minor notes + 3 recommendations, all folded into design before Step 8: drifting TextEdit-precedent line-range citations dropped; intentional-`HighlightedText`-role-swap inline comment added to `line_edit_pressed_placeholder_tracks_pressed_text` test body; Subtask 1's row density accepted as-is with explicit acknowledgment; Step 12 PR body carries a "Visible behaviour change" callout pairing `line_edit_disabled.png` ↔ `line_edit_idle.png` as the auditable #407 fold-in pair). Self-review APPROVE Round 1 no findings. AC1–AC9 PASS. 3 implementation commits on the branch: paint impl rewrite + 10 recording-painter tests + 9 snapshot goldens. Closes #406, #407.) | — |
| [integrate-claude-rules-dir](done/2026-05-21-integrate-claude-rules-dir.spec.md) | `AGENTS.md` + `.claude/skills/ai-audit/reference.md` + `.claude/agents/{self-improve,learnings-escalation-audit}.md` + `ai-docs/corrections-log.md` + `ai-docs/plans-summary.md` | ✅ implemented (0 new Rust tests; instruction-file integration pass — adopts the new on-demand `.claude/rules/` directory introduced by PR #521 into the project's safety net. 8 commits across 5 instruction files (text-only edits). AGENTS.md gains: (1) `.claude/rules/` appended to the Propagation-Rule grep paths in BOTH sites (table row body at `:210` + Procedure step 1 at `:225`); (2) new `§ Agent Docs` row for `.claude/rules/ast-index.md` (after the `.claude/skills/ui-design/SKILL.md` row at `:273` to keep the `.claude/` family contiguous); (3) dedicated Propagation-Rule row for `.claude/rules/<file>.md` at `:221` immediately before the "Any other instruction file" fallback; (4) new `rules:[name]` value in the `Escalated?` enum at `:324` between `agent:[name]` and `doc-convention`. `/ai-audit` Checklist M audited corpus extended with `.claude/rules/**/*.md` at `.claude/skills/ai-audit/reference.md:114` (Q-B = yes); deliberately did NOT touch the `wc -c` invocation at `:137` (Q-A = no size cap for rule files — on-demand, not auto-loaded — don't pay per-invocation cost). `rules:[name]` enum mirror across all four Learning-Log group parse-sites: `.claude/agents/learnings-escalation-audit.md` table row at `:32` + Step 2 verification bullet at `:63` (`grep -c 'rules:'` = 2); `.claude/agents/self-improve.md:200` Commit A surfaces prose with literal backticked `` `rules:[name]` `` token; `ai-docs/corrections-log.md` surfaces parenthetical at `:39` + new field-glossary paragraph at `:45`. AC6 row in `_inbox.md` satisfied by Step 12 PIPEBULLET3 auto-extraction from § Deferred (no hand-edit; avoids AGENTS.md:185 AXIOM conflict — spec § Deferred row 1 amended at design-review round 1 to carry the literal `.claude/rules/` substring so the auto-extracted Item matches AC6's `grep -n '#383' \| grep -i 'claude/rules'`). AGENTS.md grew 35,528 → 36,046 chars (in 35k–40k warning band, ~3,954 chars headroom under 40k hard cap — accepted risk per design § Risks entry 1, reported explicitly in PR body per AC7). Design-review ran 2 rounds (R1 ITERATE with 5 issues, including the AC6 / § Deferred spec-amendment trigger; R2 GO zero issues). Self-review APPROVE Round 1 no findings. AC1/AC2/AC3/AC5/AC7 verified PASS pre-merge; AC4 deferred to post-merge `/ai-audit` Phase 2 Checklist M run; AC6 satisfied at this Step 12 by auto-extraction; AC8 = this APPROVE. 8 commits on the branch: `127dcc1` (Propagation grep paths), `00c1b1b` (Agent Docs row), `6e8c681` (dedicated Propagation row), `4759718` (Checklist M corpus), `1154a89` (Escalated? enum declaration), `8567ca4` (learnings-escalation-audit mirror), `71d40db` (self-improve mirror), `115099c` (corrections-log mirror). Closes #522.) | — |
| [palette-state-groups](done/2026-05-21-palette-state-groups.spec.md) | `quartzite-style-types` `quartzite-style` `design-system/` | ✅ implemented (10+ new tests; `ColorGroup` enum (Normal/Hover/Pressed) + `ColorRole::FocusRing` (12th variant) + `Palette` refactored to 2D `[[Color; 3]; 12]` storage with eager Hover/Pressed derivation in `const fn new()` (Hover: blend 6% toward WindowText, Pressed: blend 16%); `DARK_PALETTE` updated with per-state seeds via `with_role_dark` private helper; all `DefaultStyle` palette call sites migrated to 2-arg `color(role, group)` API; `draw_button` refactored to use `ColorGroup` state selection + `FocusRing` outline; 4 snapshot goldens regenerated (button_hovered/pressed light+dark); design-system README+SKILL refreshed to present-tense. Self-review APPROVE Round 2. AC1–AC14 PASS. Closes #402.) | — |
| [doctest-fence-inline-comments](done/2026-05-21-doctest-fence-inline-comments.spec.md) | workspace published rustdoc surface (9 source files across 7 crates: `quartzite-core/src/{signal,traits}.rs` + `quartzite-style-dispatch/src/{lib,dispatch}.rs` + `quartzite-style/src/paint_widget.rs` + `quartzite-widgets/src/widget_ext.rs` + `quartzite-runtime/src/application.rs` + `quartzite-renderer/src/vello_painter.rs` + `src/lib.rs`) + `ai-docs/doc-convention.md` + four review-gate files (`.claude/skills/project-review/SKILL.md` + `.claude/agents/{review-findings,self-review,design-review}.md`) | ✅ implemented (0 new Rust tests; doc-comment + convention + four-file propagation PR — follow-up to #336 / PR #516 that extends the prose-side audit's spirit into code-fence content. Two-pass sweep: Pass A `rg --type rust -n '^\s*(///\|//!)\s*//\s'` returns 65 raw / 63 in-scope (excluding 2 `quartzite-test-helpers/src/lib.rs` hits via `[lib] doc = false`); Pass B walks `src/lib.rs`'s `#![cfg_attr(feature = "derive", doc = r#"..."#)]` Quickstart fence for 6 inline `//` lines. Combined in-scope: 69 hits — owner-approved per-hit table classified K=51 / R=12 / D=6. 12 rewrites + 6 drops applied: `quartzite-core/src/traits.rs:49` (drop in-fence; move advice up to prose with backticks since `quartzite-core` lacks dependency on `quartzite-runtime` where `ObjectTree` lives — matches existing convention at `quartzite-core/src/object_base.rs:139-140`); `signal.rs:180` ("Application::new()" → generic "higher-level runtime does this automatically"); `style-dispatch/lib.rs:47` (drops "inside WidgetRoot::paint (or a test)" coupling); `style-dispatch/dispatch.rs:119-122` (collapses 4-line `// In a WidgetRoot::paint implementation:` + pseudo-`fn paint` block to a single self-explanatory `// Then call dispatch_paint(root, &resolver, painter, palette).`; design-review round 2 empirically verified the `dispatch_paint` import stays used under `RUSTDOCFLAGS="-D warnings -W unused_imports"`); `paint_widget.rs:20/:59-60/:63` (4 rewrites + 1 drop — drops `DefaultStyle::draw_widget` / `quartzite-widgets` / `#[derive(Extend)]` repo-internal handles); `widget_ext.rs:479` (drops contributor-model "input-plumbing pass calls this"); `application.rs:160` (drops "in tests" framing); `vello_painter.rs:38` (drops undefined `widget.paint` reference); `src/lib.rs:127` ("populated by the runtime" → "ObjectTree containing named objects"); `src/lib.rs:44-46` Pass-B collapse (3 lines about `#[derive]` codegen + rustdoc's implicit-main wrapper → single "Explicit `fn main` keeps the derived types at module scope."). Codifies the rule in `ai-docs/doc-convention.md` § *Self-sufficiency: no repo-internal references* (anchor `#self-sufficiency-no-repo-internal-references`) as a new Family C entry parallel to Family A/B, with the `paint_widget.rs:20` rewrite as the worked example; lead-in updated "Two families" → "Three families" (post-edit 31,561 chars, well under the 35k AGENTS.md early-warning band from a 30,323 baseline). Propagates a tailored Family C check to all four review surfaces: `project-review/SKILL.md` (gate-checklist table row); `review-findings.md` (full-sweep §6 bullet); `self-review.md` (diff-touched-only §6 bullet); `design-review.md` (conditional planned-fence §3 sub-check). AC11 anchor-grep + secondary "Family C" grep both contained to the 5 edited files; no fifth file required reconciliation. AC10 post-edit `wc -c` for all 5 instruction files: doc-convention=31,561 / project-review SKILL=9,873 / review-findings=21,672 / self-review=24,536 / design-review=6,453 — all <35k. Interview ran 3 rounds (Round 2 widened sweep to cover `#[doc = r#"..."#]` raw-string attributes after owner spotted `src/lib.rs:44` was missed by the line-prefix-only Pass A pattern). Design-review ran 2 rounds (R1 ITERATE: dispatch.rs:119 D-verdict would have broken `unused_imports` doctest gate; AC2 owner-approval gate timing; 3 minor wording/count drifts — all resolved; R2 GO with empirically verified dispatch.rs:119 fix). Self-review APPROVE Round 1 no findings. AC1–AC11 PASS (AC3 PARTIAL — 10 sampled URLs listed for PR-body reviewer verification). 5 commits on the branch: `8e11166` (paint-API + style core), `5dce08d` (widgets + runtime + renderer), `7c44cdd` (facade-crate src/lib.rs), `e39f366` (doc-convention.md Family C), `5408580` (4-file review propagation). Closes #517.) | — |
| [rustdoc-strip-internal-refs](done/2026-05-21-rustdoc-strip-internal-refs.spec.md) | workspace published rustdoc surface (8 source files across 7 crates) + `quartzite-test-helpers/Cargo.toml` + `ai-docs/doc-convention.md` + four review-gate files (`.claude/skills/project-review/SKILL.md` + `.claude/agents/{self-review,review-findings,design-review}.md`) + new `scripts/check-rustdoc-internal-refs.sh` + `.github/workflows/ci.yml` | ✅ implemented (0 new Rust tests; doc-comment + convention + CI gate PR — rewrites 10 published-surface doc-comment leaks (Family A internal-artefact citations + Family B contributor-tooling instructions) per the design's replacement-prose table, drops repo-internal pointers without information loss: `quartzite-style-dispatch/src/dispatch.rs` (`AGENTS.md` citation removed), `quartzite-core/src/snapshot.rs` (3 sites: `[#39]` GitHub link, `#[error]` Display string, `this implementation` field doc), `quartzite-core/src/snapshot/tree.rs` (`this implementation` validator doc), `quartzite-widgets/{widget_base,layout/mod}.rs` (2 sites citing "plan #47"), `quartzite-renderer/src/render_harness.rs` (`spec AC1` citation + `tests/support/mod.rs` near-miss strip), `quartzite-style-types/src/dark_palette.rs` (2 sites citing `design-system/README.md`), `src/lib.rs` (drops "Verify locally with `cargo build -p quartzite --no-default-features`" contributor instruction). Excludes `quartzite-test-helpers` from `cargo doc --workspace` via `[lib] doc = false` (single-line manifest change; no CI workflow or AGENTS.md doc-gate alteration). Codifies the rule in `ai-docs/doc-convention.md` § new `### Self-sufficiency: no repo-internal references` h3 under `## Scope` (with explicit `<a id>` HTML anchor for grep-findability; post-edit 30,302 chars, under 35k AGENTS.md cap). Propagates a one-bullet `[Self-sufficiency](...)`-anchored gate to all four review-group + design-review files (`rg -l self-sufficiency-no-repo-internal-references` returns five files). Lands the regression-prevention CI gate in this PR (vs fast-follow per design Key decision row 1): new `scripts/check-rustdoc-internal-refs.sh` (231 lines, `set -euo pipefail`, shellcheck clean) runs both audit patterns scoped to published surface, applies option-(b) backward-scan `#[cfg(test)]`-region filter handling both inline `#[cfg(test)] mod tests { ... }` and sibling-file `#[cfg(test)] #[path = "..."] mod tests;` shapes (filters 3 known false positives — `default_style_tests.rs:2`, `timer_drivers.rs:450`, `render_harness.rs:543`); wired into `.github/workflows/ci.yml` `docs:` job as one new step after `cargo doc`; `actionlint` clean. AC7 demo on `src/lib.rs:2`: Pattern A + Pattern B injections each triggered exit 1, bit-for-bit reverts confirmed. Audit Pattern A broadened beyond the issue body in round 3 with `design-system/` + `CONTRIBUTING.md` + `.claude/` tokens after PR #515 surfaced two `design-system/README.md` cites in `dark_palette.rs` that the narrower pattern missed; round 5 added `\b#[0-9]{1,4}\b` bare-`#NN` defence-in-depth token after a workspace `#[0-9]+` audit confirmed 6-/8-char colour hex codes in `quartzite-paint-api/src/color.rs:46–82` don't collide. Interview ran 5 rounds (cap-extended once). Design-review ran 3 rounds (R1 blocker: 2 `this implementation` published-surface hits missing from rewrite table; R2 blocker: regression script's path filters don't strip 3 `#[cfg(test)]`-enclosed false positives; R3 GO with 3 pseudocode-correction notes + 1 `#[error]` adjacency recommendation — all folded into design before Step 8). Self-review APPROVE Round 1 no findings. AC1–AC2, AC4–AC13 PASS (AC3 awaits CI run on push). 6 commits on the branch: `aa027f6` (workspace doc rewrites), `24d2ef1` (quartzite-test-helpers `[lib] doc = false`), `1b1a590` (doc-convention.md self-sufficiency h3), `1a8396a` (review-gate propagation × 4), `56dc5fa` (regression script), `78085b5` (CI wiring). Post-edit `wc -c` for all five instruction files strictly below 35k: 30,302 / 9,560 / 20,985 / 24,080 / 6,012. Closes #336.) | — |
| [design-system-code-sync](done/2026-05-21-design-system-code-sync.spec.md) | `quartzite-style-types` | ✅ implemented (0 new tests; audit + 1-line doc-comment fix — narrow follow-up to the recent design-system refresh. Spec audit (per-slot comparison table across both shipped palettes — light + dark, 11 `ColorRole` slots each) found **zero colour-value drift** between the Rust workspace and the post-refresh `design-system/` folder. Single artefact: `quartzite-style-types/src/dark_palette.rs:6` cited the removed `design-system/colors_and_type.css` file; line 6 deleted and the trailing ` and` connector on line 5 stripped so the surviving `design-system/README.md § *Dark theme*` reference closes the sentence (file shortens by one line; `git diff --stat HEAD~1` = 1 file, 1 ins, 2 del). All five spec ACs PASS: AC1 audit on-disk in spec; AC2 zero `colors_and_type.css` hits in `dark_palette.rs`; AC3 gate suite green (`cargo build` / `cargo test --workspace` 1470+89 doctests / `cargo clippy --workspace --all-targets -- -D warnings` / `cargo fmt -- --check` / `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features` / `cargo build -p quartzite --no-default-features --features libm` all exit 0); AC4 snapshot trees byte-identical (no PNG regen); AC5 single module-level `//!` doc-comment text edit cannot reach `pub` API. Design-review verdict Round 1 = GO with 2 minor notes (Approach AC2-interpretation paragraph for the "replace = delete-the-duplicate" choice; AC5-proxy phrasing tightened to "1 file changed, net -1 line" in gate cadence) — both folded into design before Step 8 per the GO-with-notes round-trip contract. Self-review APPROVE Round 1 no findings. Out of scope: issue #402 (palette state groups, `FocusRing` role, `ColorGroup` axis, `Palette::color(role, group)` signature — separate task session) and issue #336 (broader rustdoc repo-internal-reference sweep — touches `AGENTS.md`, `.claude/agents/*.md`, `design-system/SKILL.md`, and other surfaces with their own propagation profile). Single commit: `3b91c0a`. Closes #514.) | — |
| [ui-design-model-invocable](done/2026-05-20-ui-design-model-invocable.spec.md) | Claude Code skill metadata (`.claude/skills/ui-design/SKILL.md` + `AGENTS.md`) | ✅ implemented (0 new Rust tests; instruction-surface only — flips `.claude/skills/ui-design/SKILL.md` frontmatter `disable-model-invocation: true` → `false` so the model auto-loads the skill on visual work without waiting for `/ui-design`; matches deliberate-model-invocable in-tree precedent (`pr-ci-failed`, `master-ci-failed`). Rewrites the `description:` field to embed 9 explicit trigger keywords (`paint`, `Style impl`, `DefaultStyle`, `Palette`, `ColorRole`, `snapshot`, `quartzite-style`, `quartzite-widgets`, `quartzite-paint-api`) so the auto-pick signal is unambiguous. Adds a 5th trigger bullet (`quartzite-paint-api` painter primitives, brush, pen, path, font, color) to the SKILL.md body trigger list AND to AGENTS.md § *Design system* — both surfaces stay in sync verbatim. Pointer-only contract preserved (skill still reads `design-system/` on demand; non-visual sessions pay no context-budget cost). AGENTS.md 35,306 → 35,399 chars (+93; 4,601 below 40k hard cap, in 35k early-warning band — proactive extraction not yet required). 2 commits on the branch: `a974087` (SKILL.md edits) + `7c25ec9` (AGENTS.md bullet). Self-review APPROVE Round 1 no findings. AC1–AC8 PASS. Closes #510.) | — |
| [ast-index-allowlist-and-hook](done/2026-05-20-ast-index-allowlist-and-hook.spec.md) | dev-tooling / instruction surface (`.claude/settings.json` + new `scripts/update-ast-index.sh` + `ai-docs/learnings.md` bundled entry) | ✅ implemented (0 new Rust tests; zero `.rs` diff — dev-tooling PR. Adds three space-form wildcards to `.claude/settings.json` `permissions.allow` (`Bash(ast-index *)`, `Bash(grep *)`, `Bash(rg *)`) — migrates `grep`/`rg` from host-local `.claude/settings.local.json` to project-shared visibility, and grants `ast-index` first-class allow-list status alongside its `grep`/`rg` siblings. Adds a third `PreToolUse` Bash-matcher hook entry mirroring the `gen-roadmap.sh` shape (jq + grep `(^|[ ;&|`])git[[:space:]]+commit\b` extractor) but **non-blocking** (`timeout: 30`, `statusMessage: "Refreshing ast-index..."`, hook command always `exit 0` even on script failure — contrast `gen-roadmap.sh` which `exit 2`s because `ROADMAP.md` is checked-in; ast-index DB is host-local + gitignored, so a failed update must not break the commit). New `scripts/update-ast-index.sh` (17 lines, `#!/usr/bin/env bash` matching `gen-roadmap.sh` precedent, `set -eu`): `command -v ast-index` no-op guard for hosts without the binary installed; captures `ast-index update` combined output with `\|\| true`; on detecting sentinel string `Index not found` in stdout invokes `ast-index rebuild` once (live probe established the missing-DB case exits 0 with the message to stdout — exit-code detection is empirically impossible); explicit trailing `exit 0`. AC1–AC10 PASS on developer host. AC2/AC3 carry a `(host-local — gitignored file)` qualifier added during Step 12 finalisation: `.claude/settings.local.json` is gitignored (`.gitignore:3:/.claude/settings.local.json`); Group B subtask 4 cleanup deleted 2 wildcards + 32 specific `rg` + 5 specific `grep` entries on-disk only, producing no commit (per-host hygiene, not cross-host propagation; project-shared wildcards in `.claude/settings.json` are what propagate). Design-review round 1 GO with 3 minor notes — all folded into the design before Step 8 per the GO-with-notes round-trip contract (Note 1 spec-count framing reworded; Note 2 5 redundant `Bash(ast-index ...)` specifics at `.claude/settings.local.json:627,629–632` acknowledged as out-of-scope follow-up per spec §4; Note 3 bash shebang adopted matching `gen-roadmap.sh` precedent). Self-review APPROVE Round 1 with 3 nits (host-local qualifier on AC2/AC3 + PR-body callout + commit-bundling — nits 1+2 folded into spec/design at Step 12; nit 3 informational). Commit `18b78c6` bundles unrelated pre-existing `ai-docs/learnings.md` entry (commit-block hook false-positives → use `--body-file` instead of inline heredocs containing `git commit` substrings) per user direction. 4 commits on branch: `ed02192` (script), `be26cf8` (settings.json wildcards), `a35c18d` (settings.json hook), `18b78c6` (spec + design + learnings bundle). Closes #507.) | — |
| [clippy-narrow-type-complexity](done/2026-05-20-clippy-narrow-type-complexity.spec.md) | `quartzite-runtime/src/object_tree.rs` (test module) + `quartzite-renderer/src/wrapped_handler.rs` (test module) + root `Cargo.toml` | ✅ implemented (0 new tests; lint-suppression narrowing — removes workspace `type_complexity = "allow"` from root `Cargo.toml`; introduces `type EmissionLog = Arc<Mutex<Vec<(String, Vec<Value>)>>>;` alias in the `#[cfg(test)] mod tests` block of `object_tree.rs` replacing all 3 raw-type occurrences (field + 2 return types); adds per-item `#[allow(clippy::type_complexity, reason = "…")]` on `CountingRoot::new` and `make_handler_with_root` in `wrapped_handler.rs` with distinct, site-specific reason strings. Mixed strategy per spec Key Decisions: alias for the runtime trio (shared shape), per-item `#[allow]` for the renderer pair (one-off shapes). AC1–AC7 PASS. Self-review APPROVE Round 1. Closes #482.) | — |
| [clippy-narrow-significant-drop-allows](done/2026-05-20-clippy-narrow-significant-drop-allows.spec.md) | `quartzite-runtime` (src + tests) + root `Cargo.toml` | ✅ implemented (0 new tests; lint-suppression narrowing — removes workspace `significant_drop_in_scrutinee = "allow"` from root `Cargo.toml`; adds per-site `#[allow(clippy::significant_drop_in_scrutinee, reason = "…")]` at 5 enumerated sites with site-specific atomicity rationale strings: `connection_table.rs:164` (connections write-guard across record extraction + 2 index lookups), `timer_drivers.rs:106`/`:202`/`:416` (handle guard across `.take()` + shutdown/join), `tests/timer.rs:251` (observed_thread guard across deref-Some match for assertion correctness). Follows PR #443 (`audit-workspace-clippy-allows`) precedent. AC1–AC7 PASS. Self-review APPROVE Round 1. Closes #481.) | — |
| [rename-derive-macros](done/2026-05-20-rename-derive-macros.spec.md) | `quartzite` `quartzite-macros` (all example/test/bench call sites) | ✅ implemented (0 new tests; alias-removal refactor — drops the `Object as DeriveObject` / `Object as ObjectDerive` import-site rename aliases across 10 files; the proc-macro was already named `Object` at its definition (`quartzite-macros/src/lib.rs:103`). Prelude re-export in `src/lib.rs:374` now reads bare `Object`; obsolete shadowing comment deleted; lib-crate doc at line 13, doctest at line 25, and intra-doc link at line 179 updated. 7 external call sites (benches/, tests/, examples/) + 2 quartzite-macros integration tests (via_facade.rs `DeriveObject` shape, object_impl.rs `ObjectDerive` shape) collapsed to bare `Object`. `ai-docs/plans-summary.md:22` refreshed. AC1–AC10 PASS. Closes #489.) | — |
| [extract-skill-size-exemption-index](done/2026-05-20-extract-skill-size-exemption-index.spec.md) | instruction surface (new `ai-docs/skill-size-exemptions.md` + six `.claude/skills/*/SKILL.md` (`master-ci-failed`, `pr-ci-failed`, `pr-commented`, `bugfix`, `interview`, `task`) + `.claude/skills/ai-audit/reference.md` + `AGENTS.md`) | ✅ implemented (0 new Rust tests; instruction-surface only — follow-up to #498 (PR #501). Centralises six per-skill `<!-- size-exemption: ... -->` HTML comments into a single dedicated index file modelled on the `panic-index.md` / `unsafe-index.md` precedent (preamble → `## Active entries` → six `### <skill>` entries with `{Field \| Value}` table → `## Notes`). Each entry carries 5 fields: SKILL path, post-strip `wc -l`, post-strip `wc -c`, load-bearing reason (paraphrased verbatim from the inline comment to preserve category vocabulary), category map (cat-2 round-template scaffolding / cat-3 workflow narrative / cat-3 workflow-time tables). Entries in descending `wc -l` order: master-ci-failed (373/22,885), pr-ci-failed (326/22,320), pr-commented (305/21,419), bugfix (257/14,095), interview (227/12,127), task (219/25,002). Six SKILL.md files lose their inline comment + the blank line immediately below it (-2 lines / -250–406 chars each); per-file `wc -l` shrink 375→373 / 328→326 / 307→305 / 259→257 / 229→227 / 221→219. `/ai-audit` Checklist K item 1 (in `.claude/skills/ai-audit/reference.md`) rewritten as 3-part recipe: (a) parse `ai-docs/skill-size-exemptions.md`, (b) flag SKILLs > 200 lines NOT in the index ("oversized + no exemption", severity `minor`, unchanged), (c) compare each indexed SKILL's cited `wc -l` against live `wc -l` and emit drift finding `"<path>: index cites X lines, live is Y lines"` (severity `minor`). Severity unchanged for both branches; deferred-script forward-pointer (#383) preserved. AGENTS.md gains two rows: `§ Agent Docs` row (after `api-naming.md`, before `ai-docs/templates/`) + `§ Propagation Rule` row (before catch-all "Any other instruction file") with `Size-exemption-index group` keyword binding index ↔ `reference.md`. Supersede-#498-AC1 contract documented forward in the new index's `## Notes` ("an entry in `ai-docs/skill-size-exemptions.md` OR the file is ≤ 200 lines") — `ai-docs/plans/done/2026-05-20-extract-oversized-skill-references.spec.md` NOT edited (done-tree specs are historical). Design-review verdict Round 1 = ITERATE (3 findings: major — pre-strip `wc -c` values labelled post-strip; minor — `/context-reset` not named at entry to Group A; note — § Approach phrasing implied `_inbox.md` edit). Round 2 = GO with 2 recommendations (subtask 5 step (g) dry-run replaces transient hand-edit; subtask 1 prose disclaimer that post-strip `wc -c` are pre-computed arithmetic) folded into design before Step 8 per the GO-with-notes round-trip contract. Self-review APPROVE Round 1 no findings. AC1–AC9 PASS. 4 commits on the branch: `518dc63` (new index file), `c2b9aeb` (strip six inline comments), `6b1ef5c` (Checklist K item 1 rewrite), `5b7bc7e` (AGENTS.md two rows). AGENTS.md post-edit = 35,306 chars (crossed 35k early-warning by 306; well under 40k hard cap — next AGENTS.md edit should consider proactive extraction). Closes #502.) | — |
| [extract-oversized-skill-references](done/2026-05-20-extract-oversized-skill-references.spec.md) | instruction surface (4 `.claude/skills/*/SKILL.md` + 4 new `.claude/skills/*/reference.md` + 3 exemption-comment-only SKILLs + 1 `ai-docs/agent-writing-style.md` anchor retarget) | ✅ implemented (0 new Rust tests; instruction-surface only — `/ai-audit` Sub-check K1 remediation. Total `wc -l` across the 7 in-scope SKILL.md files: 2172 → 1906 (−266 lines / −12.2 %). Per-skill before/after: master-ci-failed 403 → 375; ai-audit 369 → 187 (hits ≤ 200 target); pr-ci-failed 358 → 328; pr-commented 339 → 307; bugfix 257 → 259 (+2 exemption comment); interview 227 → 229 (+2); task 219 → 221 (+2). Four new `reference.md` siblings: ai-audit (235 lines, 25,347 chars — moves Checklist A–N detail bodies + Step 2.6 sub-step 4 anchor-aware bash; Step 2.3 collapsed to a 14-row table {letter, one-line purpose, anchor link}); master-ci-failed (49 lines, 5,558 chars — moves Spec Amendment recipe (master-ci-failed surface) + Edge cases + Re-invocation semantics); pr-ci-failed (51 lines, 5,114 chars — same shape); pr-commented (53 lines, 5,752 chars — same shape; pause-trigger list + Step 0 GraphQL/REST snapshots + Step 2 classification table EXPLICITLY KEPT INLINE as category-(3) workflow-time content). Six SKILLs carry inline `<!-- size-exemption: ... -->` HTML comments naming the load-bearing residue (round-template scaffolding + workflow narrative + workflow-time tables); ai-audit hits ≤ 200 with no exemption needed. Strict category-(1)-only extraction rule: workflow narrative + round-template scaffolding + workflow-time tables (log-fetch, classification, pause triggers, GraphQL/REST snapshots) stay inline per the same rationale that rejects Spec Amendment recipe centralisation in `ai-docs/templates/`. Design-review iter-1 ITERATE (3 issues: AC1 math gap + workflow-vs-reference inconsistency + risk classification); iter-2 GO with 2 minor wording notes (compaction-callout phrasing + ai-audit line-range double-count) — both folded into design body before Step 8. Self-review Round 1 REJECT (one `major` finding: `ai-docs/agent-writing-style.md:224` dangling anchor cited old `ai-audit/SKILL.md#m-agent-writing-stylemd-conformance` after Checklist M moved to `reference.md`); Round 2 APPROVE after retargeting to `reference.md#checklist-m--agent-writing-stylemd-conformance` (commit `c32cace`) and confirming no other live anchor breaks via broader sweep across all four extracted skills. AC1–AC7 PASS. 6 commits on the branch. Closes #498.) | — |
| [squeeze-agents-context-size](done/2026-05-20-squeeze-agents-context-size.spec.md) | instruction surface (`AGENTS.md` + `ai-docs/context.md` + `ai-docs/workflow.md` + new `ai-docs/plans-summary.md`) | ✅ implemented (0 new Rust tests; instruction-surface only — proactive shrink per `/ai-audit` Sub-check 9. `ai-docs/context.md` 43,351 → 27,717 chars (−15,634; AC1 < 35k met with ~7k headroom) via `### Maintenance plans (cross-cutting)` body extraction to new `ai-docs/plans-summary.md` (16,155 chars). `AGENTS.md` 37,673 → 34,800 chars (−2,873; AC2 < 35k met with 200-char headroom) via 4 mechanisms across 3 sections: (a) Workflow bullets lifted to `ai-docs/workflow.md` (1,484 → 6,399 chars; 14 new H2 sections); (b) Learning-Log Boundary-rule Exception bodies collapsed to one-line stubs against pre-existing `ai-docs/corrections-log.md` mirror; (c) Propagation-Rule Task/Design group rows 4 → 1 anchor + 3 see-also; (d) `## Agent Docs` row added for new `ai-docs/plans-summary.md`. All 9 AXIOM blockquotes + all action tables preserved verbatim; all MUST/NEVER/DENY/ASK directives preserved or relocated verbatim. AC1–AC12 PASS. Self-review APPROVE Round 1 no findings. Closes #497.) | — |
| [carrot-pass-improve-ai-audit](done/2026-05-19-carrot-pass-improve-ai-audit.spec.md) | instruction surface (`AGENTS.md` + `ai-docs/corrections-log.md` + `.claude/agents/*` + `.claude/skills/ai-audit/` + `.claude/skills/improve/` + `.claude/skills/context-reset/`) | ✅ implemented (0 new Rust tests; instruction-surface only — 3-PR series. PR-1 (#492 @ 7cc06c6): `Kind: correction \| validation` schema + `## Corrections Log` → `## Learning Log` rename across 7 mandatory files + Propagation-Rule row + Boundary-Rule-1 carve-out + worked-example retro-tag. PR-2 (#493 @ ce549a6): Carrot pass parallel Step 1b/2b in `self-improve.md` + verb-set enumeration + Kind-branched Step 6 reproducer (Phase 2); `🌱 Stale-validation` verdict in `learnings-escalation-audit.md` (Phase 3); `ai-audit/SKILL.md` Phase 1 prose for 🌱 + Checklist L `Kind:` row 4-location coverage + Checklist M sub-check 11 cross-shape verbs + new Checklist N bidirectional `## Patterns` ↔ `Kind: validation` (Phase 3); `AGENTS.md § Learning Log` threshold reframe + `improve/SKILL.md` lock-step restate (Phase 4); `## Patterns` block in `context-reset/SKILL.md` back-linked to 2026-05-19 worked-example entry (AC12 completion). PR-3 (this PR): Phase 5 cross-feed with user-local auto-memory — `self-improve.md` Step 1c (companion sweep with HTML-comment-anchored primitive-keywords block, reads MEMORY.md + `feedback_*.md`) + Step 2c (routing to `## Auto-memory candidates` report) + Anti-pattern bullet (never write back); `improve/SKILL.md § Auto-memory consent gate` with literal `AskUserQuestion` (header `auto-memory`, options Surface/Drop/Defer); 3-site write-guard convention; AGENTS.md untouched. Self-review APPROVE Round 1 on all three PRs. AC1–AC18 PASS. Closes #491.) | — |
| [dark-style-design-system](done/2026-05-19-dark-style-design-system.spec.md) | `quartzite-style-types` `quartzite-style` | ✅ implemented (12 new tests — 2 dark palette invariant unit tests + 10 dark snapshot tests; `pub const DARK_PALETTE: Palette` in `quartzite-style-types` with all 11 `ColorRole` slots set to dark-theme RGBA values from `design-system/README.md` § *Dark theme*; re-exported from `quartzite-style`; 10 dark goldens under `quartzite-style/tests/snapshots/shared/dark_*.png`; `DefaultStyle` paint logic untouched; `support/mod.rs` untouched. Self-review APPROVE Round 1. AC1–AC9 PASS. Closes #488.) | — |
| [const-default-fn](done/2026-05-19-const-default-fn.spec.md) | `quartzite-paint-api` `quartzite-events` `quartzite-style-types` `quartzite-style` | ✅ implemented (0 new tests; `pub const fn new() -> Self` added to 4 Group (A) types: `Path`, `CloseEvent`, `Palette`, `DefaultStyle`. `Path`/`CloseEvent`/`DefaultStyle` keep `#[derive(Default)]`; `Palette` uses explicit `impl Default { Self::new() }` (non-derivable body). 18 Group (B) types untouched. Geometry types already conformant. Design Amendment: reverted stale `#[allow(clippy::derivable_impls)]` suppression added in initial impl — keep derive where correct. Self-review APPROVE Round 3. AC1–AC9 PASS. Closes #484.) | — |
| [fix-missing-const-for-fn](done/2026-05-19-fix-missing-const-for-fn.spec.md) | root `Cargo.toml` + 27 source files across 11 crates | ✅ implemented (0 new tests; const fn promotion follow-up from PR #478 — promoted all 59 `missing_const_for_fn` lint hits to `const fn` plus cascade-unlock sites (quartzite-geometry/src/margins.rs, quartzite-renderer::{WindowedApplication::builder, VelloPainter::{brush_color,pen_color}}, quartzite-runtime::Timer::signals_blocked); single per-item `#[allow(clippy::missing_const_for_fn, reason = "libm::roundf is not const fn on the no_std path")]` for `round_f32`; workspace-level allow removed from `Cargo.toml`; 7 commits. Self-review REJECT Round 1 (missing `#[inline]` on RecordingRoot::new — fixed), APPROVE Round 2. AC1–AC7 PASS. Closes #479.) | — |
| [widgetbase-bool-bitflags](done/2026-05-19-widgetbase-bool-bitflags.spec.md) | `quartzite-widgets` `quartzite-style-dispatch` | ✅ implemented (0 new tests; mechanical refactor — collapsed 6 `pub bool` fields on `WidgetBase` (`visible`, `enabled`, `pending_update`, `hovered`, `pressed`, `focused`) into a single `pub state: BitFlags<WidgetState>` backed by `enumflags2`; added `enumflags2 = "0.7"` to `quartzite-widgets/Cargo.toml`; `WidgetState` + `WidgetStates` re-exported from `quartzite-widgets::lib`; all 14 `WidgetExt` accessor body touch-points updated to `contains`/`insert`/`remove`/`set`; both visibility checks in `dispatch.rs` updated via `state.contains`; all existing tests rewritten to `state.contains(...)`; `#[allow(clippy::struct_excessive_bools)]` removed. Self-review APPROVE Round 1. AC1–AC13 PASS. Closes #480.) | — |
| [audit-workspace-clippy-allows](done/2026-05-18-audit-workspace-clippy-allows.spec.md) | root `Cargo.toml` + 28 source files across 8 crates | ✅ implemented (0 new tests; lint audit — 9 workspace allows removed (`cast_possible_wrap`, `cast_possible_truncation`, `cast_precision_loss`, `float_cmp`, `items_after_statements`, `option_if_let_else`, `needless_pass_by_value`, `significant_drop_tightening`, `struct_excessive_bools`), 6 kept with refreshed hit-count comments (`must_use_candidate` 170 hits, `redundant_pub_crate` 60, `missing_const_for_fn` 59, `return_self_not_must_use` 12, `significant_drop_in_scrutinee` 5, `type_complexity` 5 with corrected file list). Per-item `#[allow(clippy::..., reason = "…")]` placed at ~100 narrowed sites across 28 files. `option_if_let_else` 4 of 8 hits rewritten idiomatically (`unwrap_or`, `is_some_and`, `map_or_else`, `map_or`); `needless_pass_by_value` local renderer hit fixed with ref-pattern. Key finding: `[lints] workspace = true` and per-crate `[lints.clippy]` are mutually exclusive in Cargo — all narrows use per-item `#[allow]`. `cargo fmt` reformatted multi-attribute `#[allow]` blocks in subtask 7 (reason= strings force multi-line). 4 implementation commits (`d17b4fe`, `5db78f6`, `5e08c0e`, `0b19440`, `064ecc8`) + 1 fmt-fix commit. Self-review APPROVE Round 1 no findings. AC1–AC12 PASS. Closes #443–#453 + #477.) | — |
| [audit-doc-link-code-allow](done/2026-05-18-audit-doc-link-code-allow.spec.md) | root `Cargo.toml` + `quartzite-runtime/src/object_tree_ext.rs` + `quartzite-style/src/default_style.rs` | ✅ implemented (0 new tests; lint config chore — removes `doc_link_code = "allow"` from `[workspace.lints.clippy]`; adds per-site `#[allow(clippy::doc_link_code, reason = "…")]` at the 2 confirmed adjacency-to-(args) sites: `quartzite-runtime/src/object_tree_ext.rs:31` (`pub trait ObjectTreeExt` — renders `Err(TreeAccessError)` with both identifiers intra-doc-linked) and `quartzite-style/src/default_style.rs:259` (`fn maybe_disabled` — renders `disabled(color)` with `disabled` intra-doc-linked). Future accidental hits now surface in CI. Survey (2026-05-18 master): exactly 2 sites; Fix/mass-normalise inapplicable (both depend on adjacency-to-(args) shape). Self-review APPROVE Round 1 no findings. ACs 1–8 PASS. Closes #454.) | — |
| [move-make-expand-utils](done/2026-05-18-move-make-expand-utils.spec.md) | `quartzite-macros` (`src/lib.rs` + `src/util.rs`) | ✅ implemented (0 new tests; pure code-motion refactor — moves `macro_rules! make_expand` from `lib.rs` into `util.rs` with a `pub(crate) use util::make_expand;` re-export at the crate root; three call sites unchanged. Closes #457.) | — |
| [escalate-clippy-warns-deny](done/2026-05-18-escalate-clippy-warns-deny.spec.md) | (instruction docs + lint policy — root `Cargo.toml` + `AGENTS.md` + `ai-docs/code-style.md` + `.claude/agents/self-review.md` + `.claude/agents/review-findings.md` + `.claude/skills/task/reference.md`) | ✅ implemented (0 new tests; lint-severity escalation — flips `large_stack_frames`, `large_stack_arrays`, and `undocumented_unsafe_blocks` from `"warn"` to `"deny"` at the declaration level in root `Cargo.toml`'s `[workspace.lints.clippy]` table. Net behavioural delta: local non-flagged `cargo clippy --workspace` now hard-fails on any violation of the three, instead of being swallowed when a developer forgets `-D warnings` locally. In-tree hit count was 0 pre-edit and remains 0 post-edit. The `pedantic` and `nursery` group entries remain at `level = "warn"` (escalating `nursery` is a footgun across toolchain bumps per issue #464); the 16-entry workspace allow-list is unchanged; the CI `cargo clippy --workspace --all-targets -- -D warnings` invocation stays as belt-and-braces. Option-3 (full sweep) per user direction: the `undocumented_unsafe_blocks` lint name was mirrored across 5 in-tree instruction-file mentions (`AGENTS.md` *Documentation* row line 97; `ai-docs/code-style.md` lines 137 + 411; `.claude/agents/self-review.md` line 85; `.claude/agents/review-findings.md` line 77; `.claude/skills/task/reference.md` line 233 — the latter three flipped `#![warn(...)]` → `#![deny(...)]` in checklist text); the `large_stack_*` parenthetical at `ai-docs/code-style.md:44–46` updated from `(both \`warn\`, ...)` to `(both \`deny\`, ...)` after design-review round 3 caught the prose miss. Spec amended three times during Step 7 (rounds 2 / 3 / 3-follow-up) for AC1 17→16 allow-list count drift, AC12 carve-out widening (5 narrative-surface excludes), `--hidden` flag on the `--glob` recipe (`.claude/` is hidden), and the AC10 anchored multi-line recipe (`large_stack_arrays`-prefixed match) for the legitimate line-42 pedantic/nursery residual. Round 4 GO with no notes. AC4 negative-half probe (`unsafe { 1u8.wrapping_add(1) }` in a throwaway `tests/_ac4_probe.rs` with no `// SAFETY:` comment) exited 101 with `error: unsafe block missing a safety comment` and `note: requested on the command line with -D clippy::undocumented-unsafe-blocks` — `-D` came from the manifest declaration, not from a CLI flag, confirming the declaration-level severity is load-bearing without `-D warnings`. Self-review APPROVE round 1, no findings. AC1–AC12 all PASS. Closes #464.) | — |
| [bump-vello-wgpu-major](done/2026-05-18-bump-vello-wgpu-major.spec.md) | `quartzite-renderer` (`Cargo.toml` + workspace `Cargo.lock`) + `ai-docs/context.md` version note | ✅ implemented (0 new tests; coupled major dep bump only — `quartzite-renderer/Cargo.toml` constraints lifted `vello = "0.8" → "0.9"` and `wgpu = "28" → "29"` per AGENTS.md *Dependency Versions* caret-default shape; `Cargo.lock` resolves `vello v0.9.0` + `wgpu v29.0.3` + `peniko v0.6.1` + `skrifa v0.42.1` (single copy each post-bump; transitive skrifa converges with the direct dep, retiring the previous 0.40/0.42 coexistence). Stale `# skrifa 0.42 coexists with vello's transitive skrifa 0.40` parenthetical comment dropped (AC10); `# Text shaping and font loading.` section header retained for the parley+skrifa pair. **Zero `.rs` edits required:** the design's per-call-site changelog audit against wgpu 29's breaking changes (`Surface::get_current_texture`, `InstanceDescriptor`, `PipelineLayoutDescriptor`, `WriteOnly<[u8]>` for write-mapped buffers, depth/stencil `Option` fields, `primitive_index` WGSL `enable`, `max_inter_stage_shader_*`) confirmed none touch the renderer surface; `Instance::default()` preserved at all 8 call sites (1 prod + 7 test); vello 0.9 `Scene` adds non-breaking `DrawGlyphs::font_embolden` / `brush_transform`. **AC5 vacuous PASS:** all 15 existing Linux/vulkan goldens (`quartzite-widgets/tests/snapshots/shared/{box_layout,button,grid_layout,label,line_edit}.png` + `quartzite-style/tests/snapshots/shared/{button_checked,button_disabled,button_focused,button_hovered,button_idle,button_pressed,label,scroll_area_chrome,text_edit_plain,text_edit_read_only}.png`) byte-identical under vello 0.9's rasterizer — `scripts/update-snapshots.sh` not invoked, no PNG diff committed. AC9: Win/Mac CI lanes still `continue-on-error: true` (no workflow files touched). `cargo test --workspace` 1454 passed / 0 failed / 12 ignored; `cargo clippy --workspace --all-targets -- -D warnings`, `RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features`, `cargo fmt -- --check`, `cargo build -p quartzite --no-default-features --features libm` all exit 0. Panic-index + unsafe-index `git diff` greps clean (zero `.rs` diff). `ai-docs/context.md:21` refreshed (`**wgpu pinned to 29** to match vello 0.9`). 1 commit on the branch: `5ececcc` (Cargo.toml + Cargo.lock). Design-review verdict Round 1 = GO with 2 minor notes (entry-into-Group-A `/context-reset` line, widen `Instance::default()` file column) + 2 recommendations (byte-stable PR-body disclosure, subtask-1 optional-self-review note) — all folded into the design doc before Step 8. Self-review APPROVE Round 1 no findings. Closes #439.) | — |
| [rename-design-skill-to-ui-design](done/2026-05-18-rename-design-skill-to-ui-design.spec.md) | repo instructions / Claude Code skill metadata (the old `design` skill slug moves to `ui-design`; touches `AGENTS.md` + `ROADMAP.md` + `ai-docs/context.md` + `ai-docs/deferred/_inbox.md` + `ai-docs/plans/INDEX.md` + the two `done/wire-design-system-context.{spec,design}.md` artefacts) | ✅ implemented (0 new tests; docs-only rename — moves the slash-discoverable skill directory from the old `design` slug to the new `ui-design` slug via `git mv` (preserves blame); updates the frontmatter `name:` field from `design` to `ui-design` inside the moved `SKILL.md`. Resolves the slug clash with the existing `design` agent at `.claude/agents/design.md` (issue #470). Sweep covers BOTH active and historical surfaces per user-confirmed Round 1 Q1 "sweep all" (path-form across 7 files; backtick-quoted name-literal across 5 files); the `design` agent and the canonical `design-system/SKILL.md` (`name: quartzite-design`) are NOT renamed (Out of scope); `ai-docs/learnings.md` carved out per Boundary rule 1 (append-only); the rename's own spec + design carved out as tautological from→to record. AC4 (path-form sweep) and AC4b (5-file backtick-quoted name-literal sweep) both gated to zero hits after carve-out filtering; substring-collision discipline anchors substitutions on the backtick-quoted name-literal so the unrelated `` `name: quartzite-design` `` literals (in `done/2026-05-18-wire-design-system-context.design.md:68`, `ROADMAP.md:38`, `ai-docs/context.md:188`, `ai-docs/plans/INDEX.md:10`) are preserved untouched. 4-round design-review loop (round 1 ITERATE: rename-spec self-corruption → spec amended for AC4 carve-out; round 2 ITERATE: line-number inventory errors → spec added AC4b, design v3 dropped line enumeration; round 3 ITERATE: AC4 filter anchor mismatch → spec/design fixed anchors + tightened AC3; round 4 GO, user-authorised extra round) + 1 Design Amendment review (subtask 4 originally enumerated path-form only; AC4b widened from 2-file to 5-file scope after Step 9.5 caught the active-surface name-literal omission). Self-review Round 1 APPROVE no blockers. Closes #470.) | — |
| [make-read-only-overlay-visible](done/2026-05-18-make-read-only-overlay-visible.spec.md) | `quartzite-style` | ✅ implemented (9 new tests — 3 assertion updates + 6 new covering overlay brush, text dimming, palette-agnosticism; `read_only_overlay` private helper + `READ_ONLY_OVERLAY_ALPHA`/`READ_ONLY_TEXT_ALPHA` consts; `text_edit_read_only.png` golden regenerated. Overlay changed from invisible `disabled(Window)` to visible `WindowText.with_alpha(0.10)`; read-only text dims to `Text.with_alpha(0.65)`. Closes #458.) | — |
| [wire-design-system-context](done/2026-05-18-wire-design-system-context.spec.md) | agent prompts / repo instructions (`AGENTS.md` + `.claude/agents/design.md` + `.claude/agents/design-review.md` + new `.claude/skills/ui-design/SKILL.md`) | ✅ implemented (0 new tests; docs-only wiring — wires the existing `design-system/` folder into Claude Code agent prompts as a **conditional, on-demand Read** context source. AGENTS.md gains a new `## Design system` pointer section after `## Project` (+799 B, under the 1 024 B AC1 cap; lands AGENTS.md at 36 592 B — past the 35 000 B proactive-extraction warning, ~3 408 B headroom against the 40 000 B hard cap), naming entry points `design-system/SKILL.md` + `design-system/README.md` and supporting subfolders `preview/`, `colors_and_type.css`, `ui_kits/widgets/`, with the four AC1 trigger conditions verbatim (any `Style` impl in `quartzite-style` incl. `DefaultStyle`; `quartzite-widgets` paint paths / widget views / user-facing rendering; `Palette` / `ColorRole` semantics or seeds; snapshot tests under `quartzite-style/tests/snapshots/`). New row in AGENTS.md § Agent Docs registers the new pointer skill. `.claude/agents/design.md` § Read before designing gains one bullet naming both entry points, qualified "only when the task is visual" with three trigger surfaces (UI-touching `quartzite-widgets` paint logic, `quartzite-style`, any user-facing surface). `.claude/agents/design-review.md` extends Step 2 `**Read context**` to require reading `design-system/README.md` § VISUAL FOUNDATIONS + `design-system/colors_and_type.css` whenever the design under review touches a widget / paint code / `Palette` / `ColorRole`, and adds a Step 3 severity-rubric clause flagging deviations from documented rules (outline width, radius, derivation formulas, focus overlay) as `major` — phrasing lifted from the existing handoff-grouping severity clause. New file `.claude/skills/ui-design/SKILL.md` (~10-line body, frontmatter `name: ui-design`, `disable-model-invocation: true`, `allowed-tools: Read`) is the slash-discoverable counterpart to the existing `design-system/SKILL.md` (which carries `name: quartzite-design` and is not slash-discoverable because Claude Code's loader only discovers skills under `.claude/skills/<slug>/SKILL.md`). Pointer-only throughout: AC6 final gate `grep -rn "VISUAL FOUNDATIONS\|Button.blend(Highlight" .claude/` returns only legitimate Read-trigger anchors in design-review.md; no design-system visual content inlined anywhere. CLAUDE.md is unchanged (AC2) — no `@<file>` import added; conditional Read keeps zero context-budget cost on non-visual sessions. PR base = `design-system` (not `master`); `master` was merged into `design-system` earlier in this turn (commit `e931e65`) to unblock the wiring on a tree where `design-system/` already exists. 4 commits on this feature branch: `df5641b` (AGENTS.md pointer + Agent Docs row), `b052d46` (.claude/agents/design.md bullet), `2c394c0` (.claude/agents/design-review.md Read-context + severity rubric), `6bd3d1b` (new `.claude/skills/ui-design/SKILL.md`). Design-review verdict Round 1 = GO with 2 notes + 2 recommendations (phantom "36 000-byte advisory" reference, `major`/`minor` phrasing parity, sync-group grep widening) — all folded into the design doc before Step 8 per the GO-with-notes round-trip contract. Self-review APPROVE Round 1 no findings. AC1–AC6 mechanically PASS; AC7/AC8 PASS by inspection of the wiring contract (conditional Read triggers gate the load to visual tasks; non-visual paths never enter design-system). Closes #462.) | — |
| [hover-focused-label-textedit-scroll](done/2026-05-21-hover-focused-label-textedit-scroll.spec.md) | `quartzite-style` (`DefaultStyle` paint impls for `Label` / `TextEdit` / `ScrollArea`) | ✅ implemented (28 new tests — 16 recording-painter unit tests covering hover/pressed/focused/disabled-and-focused/precedence-pressed-over-hovered for each of Label/TextEdit/ScrollArea (5 per widget) + 1 TextEdit-only `text_edit_read_only_hovered_overlay_plus_hover_base_fill` combo test (AC2) + 12 new snapshot goldens under `quartzite-style/tests/snapshots/shared/` (9 light: `{label,text_edit,scroll_area}_{hovered,pressed,focused}.png` + 3 dark: `dark_{label,text_edit,scroll_area}_focused.png`); private `#[inline] const fn state_group(pressed: bool, hovered: bool) -> ColorGroup` helper extracted into `default_style.rs` powering all four state-aware paint impls (Button refactored to call it — zero behaviour change); `impl Paint<Label>` / `impl Paint<TextEdit>` / `impl Paint<ScrollArea>` for `DefaultStyle` read enabled/hovered/pressed/focused via the existing `WidgetExt` accessors, swap roles on `pressed` (Label/TextEdit: `(Window→Highlight, WindowText→HighlightedText)`; ScrollArea: `(Base→Highlight, WindowText→HighlightedText)` for outline), apply `maybe_disabled` to all fill/text/non-focus-ring pen colours, widen the outline to 2 px `palette.color(FocusRing, Normal)` when `focused` (FocusRing pen exempt from `maybe_disabled` — full alpha always, per `design-system/README.md` § *Animation, hover, press* "never alpha-halved" + shipped Button precedent); TextEdit read-only overlay paint-order preserved (`FillRect(state_fill)` → `FillRect(overlay)` → `DrawRect(outline)` → `DrawTextIn(text)`); read-only text dim composes over state-resolved text colour (`if read_only { state_text.with_alpha(READ_ONLY_TEXT_ALPHA) } else { state_text }`). State precedence on these three widgets is `disabled > pressed > hovered` (no `checked` flag); `focused` is an additive outline modifier orthogonal to the fill axis. `default_style.rs` 280 → 358 lines (well under 500-line soft target); `default_style_tests.rs` 1271 → 1822 lines (under documented `#[cfg(test)]`-exempt cap). Design-review ran 2 rounds (Round 1 ITERATE: FocusRing alpha-halve violation flagged; design + spec amended for the FocusRing carve-out; Round 2 GO with 1 note + 2 recommendations folded in before Step 8: dark-palette `MERCURY`-blend reasoning corrected in spec § Deferred row 4; design Subtask 1 `_Simple._` clarification; design Risks gained note on `*_pressed.png` ↔ `*_focused.png` first-run visual inspection). Self-review APPROVE Round 1 no blockers (2 nit findings accepted as documented design choices). AC1–AC8 PASS. 6 implementation commits on the branch: `ac29d3c` (state_group helper + Button refactor), `db7b8dd` (Label), `34add8f` (TextEdit), `8322a91` (ScrollArea), `b253845` (16 recording-painter tests), `782e163` (12 snapshot goldens). Plus prep commit `953233f` (activate spec from deferred + design doc). Closes #403.) | — |
| [ci-clippy-bench-targets](done/2026-05-18-ci-clippy-bench-targets.spec.md) | CI / repo config (`.github/workflows/ci.yml` + `quartzite-test-helpers/src/lib.rs` + `quartzite-style-dispatch/src/dispatch.rs`) | ✅ implemented (0 new tests; CI gate + 2 pre-existing lint fixes — widened `cargo clippy --workspace -- -D warnings` to `cargo clippy --workspace --all-targets -- -D warnings` in `.github/workflows/ci.yml` `Clippy` step so bench, integration-test, and example targets are linted on every PR; fixed pre-existing `let_underscore_lock` in `quartzite-test-helpers/src/lib.rs:75` (`let _` → `let _guard`, aligned with module-doc) and `similar_names` in `quartzite-style-dispatch/src/dispatch.rs` test code (`present2_id`/`present2` → `sibling_id`/`sibling`), both surfaced by the widened invocation. Developer lint command updated in AGENTS.md, README.md, and 6 skill files per Propagation Rule. `actionlint` clean. 1 commit on branch: `049bad7`. Self-review APPROVE Round 1 no findings. Closes #465.) | — |
| [workspace-lints-lift](done/2026-05-17-workspace-lints-lift.spec.md) | all crates (root `Cargo.toml` + 15 `lib.rs` files + 17 `tests/*.rs` + 4 `examples/*.rs`) + `AGENTS.md` + `ai-docs/code-style.md` + `ai-docs/doc-convention.md` + `ai-docs/context.md` | ✅ implemented (0 new tests; placement-only lint-table lift + doc-coverage broadening — lifts the 7 per-crate doc-lint directives (`#![deny(missing_docs)]`, `#![deny(rustdoc::broken_intra_doc_links)]`, `#![warn(clippy::undocumented_unsafe_blocks)]`, plus 4 `clippy::missing_*_doc` / `doc_markdown` warns) from every `lib.rs` into root `Cargo.toml`'s `[workspace.lints.{rust,rustdoc,clippy}]` tables; each member crate inherits via the pre-existing `[lints] workspace = true` (15 Cargo.toml files; no per-crate `Cargo.toml` edits needed). AC6 live pre-check (primary: pedantic-suppression diff; cross: `cargo clippy --workspace --message-format=json | jq ... | grep` enumeration) confirmed all 4 `clippy::missing_*_doc` / `doc_markdown` warns are pedantic-implied → dropped from explicit declaration; only `undocumented_unsafe_blocks = "warn"` is independently kept (not in `clippy::pedantic`). Workspace lints have no per-target gating, so `missing_docs = "deny"` now also covers integration test crates + `examples/*.rs`; spec amended mid-Step-8 (Spec Amendment recipe) after Subtask 1 surfaced 17 test files + 4 examples needing one-line `//!` crate-docs; user approved Option 1 of 4. Design-review round 2 GO no notes against the amended (spec, design). `quartzite-runtime/tests/snapshot.rs` `//!` placement: BEFORE `#![cfg(feature = "serde")]` (design's "after" guidance did not satisfy rustc's `missing-docs` lint). Subtask 2 stripped the full 7-line block from 14 `lib.rs` files (renderer had the full 7 lines — live-grep authoritative, design's "6 lines" note was stale; `quartzite-style-dispatch/src/lib.rs` had only 3 lines, never carried the 4 pedantic-implied — stripped what it had); no AC9 renderer doc additions needed. Subtask 3 updated 6 instruction-file locations: AGENTS.md *Linter posture* + *Documentation* rows; `ai-docs/code-style.md` § Linter posture + § Documentation (line 135) + § Lints that mechanically enforce (lines 405–413, with 1 in-scope syntax-form cleanup); `ai-docs/doc-convention.md` § Lints that mechanically enforce (line 539+) restructured the 4 pedantic-implied bullets to attribute them to `clippy::pedantic` (the actual mechanism after Subtask 2's strip). AGENTS.md size grew 35,577 → 35,756 chars (above 35k early-warning, well under 40k hard cap — pre-existing condition; +179 chars from this commit). 3 commits on the branch: `e2ebce0` (workspace tables + 17 test files + 4 examples), `9b5b11e` (15 lib.rs strip), `06555c9` (instruction-file mirror). Self-review APPROVE Round 1 no findings. All 20 ACs PASS. Closes #460.) | — |
| [replace-serial-test-with-mutex-helper](done/2026-05-17-replace-serial-test-with-mutex-helper.spec.md) | **new** `quartzite-test-helpers` + `quartzite-core` `quartzite-runtime` `quartzite-style` `quartzite-style-dispatch` + workspace root (`Cargo.toml` × 5, `tests/signal_to_signal.rs`) | ✅ implemented (1 new smoke test; ~1448 pre-change tests preserved within ±1 — net +1 from the helper smoke; AC5 satisfied — replaces the `serial_test → scc → sdd` dev-dep chain with a 30-line workspace member crate `quartzite-test-helpers/` exporting `pub fn test_lock() -> parking_lot::MutexGuard<'static, ()>` over `static TEST_LOCK: parking_lot::Mutex<()>`. The static is declared once; each test binary that depends on `quartzite-test-helpers` links its own instance via Rust per-binary static linkage, preserving the per-binary serialisation semantics that bare `#[serial]` provided (no named groups exist in the codebase, so a single unnamed group suffices). 42 `#[serial]` / `#[serial_test::serial]` attributes across 9 source files rewritten to `let _lock = quartzite_test_helpers::test_lock();` as the first statement of each migrated test body (AC13 invariant). Drops `serial_test`, `scc`, `sdd` from `Cargo.lock` — unblocks the path-of-least-resistance fix for #427 (Miri Tree Borrows / `sdd/tag.rs:49` integer-to-pointer cast incompatible with TB). `quartzite-test-helpers/Cargo.toml` declares `parking_lot = "0.12"` as a regular (non-dev) dep; each of the 5 consumer `Cargo.toml` files swaps the prior `serial_test = "3"` `[dev-dependencies]` entry for `quartzite-test-helpers = { path = ... }`. Spec amended at end-of-design Round 1 (user redirect: helper placement flipped from per-crate-static option (a) to shared-crate option (b) to avoid duplicating the static + helper fn across 6 test-binary sites; new AC14 enforces single-`static TEST_LOCK` via `rg -U 'static\s+TEST_LOCK\s*:' --type rust` returning exactly 1 hit inside `quartzite-test-helpers/`). 7 commits on the branch: `d264573` (helper crate + bundled `learnings.md` 2026-05-17 copy-paste entry per AGENTS.md *Workflow*), `5d3cfd8` (subtask-1 inline-self-review fix: third-person summary line), `6248e19` (5-Cargo.toml dev-dep swap + `cargo update`), `5414a3f` (quartzite-core 10 sites), `da39b92` (quartzite-runtime 14 sites), `7681b87` (quartzite-style + quartzite-style-dispatch + workspace-root 18 sites), `4347e9f` (subtask-6 doc-comment scrub to satisfy AC2/AC3 literal greps). Two design-review rounds: Round 1 GO with 4 minor notes (smoke-test ±1; tasks 4/5 parallelism prose; spec count drift 41 vs live 42; `let _ = …` drop-order trap) — folded after user-driven spec amendment; Round 2 GO with 3 minor notes (tasks 4/5 independence prose; doctest `no_run` pick over `ignore`; `default-members` clarification) — all 3 folded before Step 8 start. AC15 (first post-merge master Miri run green — `sdd/tag.rs:49` TB warning no longer fires) deferred to post-merge observation. Closes #440.) | — |
| [migrate-std-locks-to-parking-lot](done/2026-05-17-migrate-std-locks-to-parking-lot.spec.md) | `quartzite-style` `quartzite-renderer` `quartzite-widgets` `quartzite-runtime` `quartzite-core` (+ `AGENTS.md` + `ai-docs/code-style.md` + root `Cargo.toml`) | ✅ implemented (0 new tests; mechanical refactor + lint-policy cleanup — replaces every remaining `std::sync::{Mutex,RwLock}` site across the workspace with `parking_lot::Mutex` / `parking_lot::RwLock`. 15 source files migrated (8 prod-side incl. `quartzite-core/src/id.rs` #[cfg(test)] mod, 7 integration `tests/` files), plus `quartzite-runtime/tests/factory.rs` + `quartzite-runtime/tests/snapshot.rs` consumer call sites pulled in by compiler enforcement (`parking_lot`'s `lock()` returns `MutexGuard` directly, not `LockResult<MutexGuard>`, so any surviving `.unwrap()` / `.expect()` / `.unwrap_or_else()` becomes a type error). Drops every `.unwrap_or_else(\|e\| e.into_inner())` poisoned-recovery closure (12 sites across 5 files); drops `.lock().unwrap()` in test code (52 sites) and `.lock().expect("…")` in `timer.rs` (7 sites — line 213's trailing `.unwrap()` survives because it's on the inner `Option<ThreadId>` payload, not the lock). Adds `parking_lot = "0.12"` to `quartzite-style` / `quartzite-renderer` / `quartzite-widgets` `[dependencies]` (`quartzite-core` already declares it gated by `std`; `quartzite-runtime` already declares it). Public-API break on `ObjectFactory::global()` (return type `Option<Arc<std::sync::RwLock<Self>>>` → `Option<Arc<parking_lot::RwLock<Self>>>`) authorised by AGENTS.md *API Stability* AXIOM (pre-publish, no compat shims). Deletes `poison_for_test` helper + `try_style_recovers_from_poisoned_mutex` test + module-level poison-tolerance doc comments in `quartzite-style/src/registry.rs` (parking_lot has no poison concept). Drops the `redundant_closure_for_method_calls = "allow"` workspace clippy allow + its justifying comment from root `Cargo.toml` (clippy stays green — the gating verification). Instruction-file pair: `AGENTS.md` § *Library safety idioms* + `ai-docs/code-style.md` § *Library safety idioms* both name `parking_lot::Mutex` / `parking_lot::RwLock` as workspace default; the `unwrap_or_else(\|e\| e.into_inner())` recovery idiom is retained only as an FFI-only footnote. AC1/AC2 use `rg -U` (multi-line) regex pair to defend against the line-based grep weakness that round-1/round-2 design-review surfaced (`per_thread_loops.rs` / `timer.rs` / `factory.rs` use multi-line `use std::{ sync::{ Arc, Mutex, ... } ... }` shape). 6-commit decomposition 1:1 with design tasks: 544118d (AGENTS.md + code-style.md), cde260f (registry.rs + style Cargo.toml), d7e17ba (factory.rs + snapshot consumers), 367b847 (7 prod-crate inline test mods + widgets/renderer Cargo.toml), dd0b922 (6 integration test files), 31f0ade (clippy-allow removal + final audit). AC14 (post-merge Miri green) verifies post-merge in the first master push CI run. Closes #442.) | — |
| [miri-deny-list](done/2026-05-17-miri-deny-list.spec.md) | (CI / repo config — `.github/workflows/miri.yml` + 3 renderer-touching test files newly tagged `#![cfg(not(miri))]` (`quartzite-widgets/tests/snapshots.rs`, `quartzite-widgets/tests/support_internals.rs`, `quartzite-style/tests/snapshots.rs`) + new `ai-docs/miri-policy.md` + `AGENTS.md` cross-reference row + `ai-docs/plans/INDEX.md` row) | ✅ implemented (0 new tests; CI + doc-tag + instruction-file only — flips `.github/workflows/miri.yml` from a `-p`-style allow-list (the v1 / v2 shape from #422 / #431) to a single workspace-wide `cargo miri test --workspace --exclude quartzite-renderer` deny-list invocation. The v2 7-step block (one per `-p`/`--test` allow-listed target) collapses to one step. `quartzite-renderer` is the only workflow-level exclusion (prod-dep FFI: winit/wgpu/vello), carrying a 5-line `# Why excluded:` block per AC2. `quartzite-macros` stays in `--workspace` (Shape B): empirically confirmed via local dry-run that `cargo-miri` skips the proc-macro lib's unittest binary at runtime with exit 0 and the message `Running unit tests of 'proc-macro' crates is not currently supported by Miri.` (same for doctests); `quartzite-macros/tests/via_facade.rs` is Miri-clean unchanged (spec hypothesis that it needed `#![cfg(not(miri))]` was falsified). 3 new per-file `#![cfg(not(miri))]` tags + ≥ 3-line top-of-file comment block per `ai-docs/miri-policy.md § Per-file fallback recipe — comment-block template` on the renderer-touching integration tests: widgets `snapshots.rs` (GPU init via `wgpu::Instance::default`), widgets `support_internals.rs` (FFI via `nv_flip_sys::flip_image_color3_new`), style `snapshots.rs` (GPU init). Zero new per-test `#[cfg_attr(miri, ignore = "<reason>")]` additions — empirical audit found no hostile-case in a mixed-file context; the `cfg_attr` mechanism stays documented as the default in `ai-docs/miri-policy.md`. New 192-line `ai-docs/miri-policy.md` documents the per-test default + six reason-prefix taxonomy + per-file fallback recipe + workflow-level exclusion contract + audit recipe + maintenance contract + two exemplar pointers (`no_style_dep.rs` / `timer.rs`, both unchanged per AC11). AGENTS.md `§ Build & Test` gains a single-line cross-reference blockquote (33,457 → 33,659 chars; 1,341 headroom under 35k early-warning per AC12). In-flow Step-9 finding: stale Linux-deps comment at `miri.yml:78-80` referenced the retired `--lib -p quartzite-widgets` v2 invocation; same class as the design Round-1 ITERATE blocker (lines 82-84) at a sibling location, fixed in commit 7f6cc22 as an in-scope v3 cleanup. Local dry-run (Subtask 3 + Subtask 5 re-run) exit 0: 1030 passed / 0 failed / 9 ignored across 48 test-target binaries; 1m33.94s and 1m35.59s wall on warm cache. Toolchain baseline: `rustc 1.97.0-nightly (f53b654a8 2026-04-30)`. AC9 (first post-merge master Miri run green) deferred to post-merge CI verification. 5 commits: 334c9f6 (miri-policy.md + AGENTS.md row), 4aabe0a (per-file tags), 59a00a2 (workflow inversion), 350e608 (INDEX active row), 7f6cc22 (stale-comment fix). Closes #436.) | — |
| [tighten-clippy-pedantic-nursery](done/2026-05-17-tighten-clippy-pedantic-nursery.spec.md) | all crates (root `Cargo.toml` + 13 leaf-crate `Cargo.toml` + new `clippy.toml`) + `AGENTS.md` + `ai-docs/code-style.md` | ✅ implemented (0 new tests; lint policy + workspace-wide refactor — enables `clippy::pedantic` + `clippy::nursery` (both `warn`, `priority = -1`) plus `clippy::large_stack_frames` + `clippy::large_stack_arrays` workspace-wide via `[workspace.lints.clippy]` in root `Cargo.toml`; opts every member crate in via `[lints] workspace = true` (14 files: 13 leaf crates + root `quartzite` package); materialises the two 512 KiB size thresholds in a new workspace-root `clippy.toml`. Design-phase audit ran provisional clippy with zero allows → 666 hits across 51 distinct lints; 0 hits for `large_stack_frames`/`large_stack_arrays` (forward-protection only at v1). 17-entry workspace-level allow-list (16 Bucket A + 1 `redundant_closure_for_method_calls` cross-referencing AGENTS.md *Library safety idioms* poisoned-mutex idiom), each with a one-line `#`-justifying comment. ~243 Bucket B+C+D source-fixes across 35 production files (mechanical: `use_self`, `default_constructed_unit_structs`, `cast_lossless` → `From::from`, `doc_markdown` backticks, `ref_as_ptr` → `ptr::from_ref`, `manual_let_else`, `uninlined_format_args`, etc.; judgement: 4 macro `codegen(ir: &IR)` signature switches eliminating by-value moves of the parsed IR, `bare` → `bare_generics_idents` rename for `similar_names`). 5 per-call `#[allow(..., reason = "…")]`s for cases where fixing the lint would be worse (RAII guard `_keep_alive_x` bindings, `EnvGuard::set` invariant tie, cohesive 117-line codegen emission, test-fixture field-name mirror). AGENTS.md *Code Style → Linter posture* row + `ai-docs/code-style.md § Linter posture` updated to mention `[workspace.lints.clippy]` + `clippy.toml`. Single-PR, big-bang tightening; 6 commits on the branch (eee0962, 3885b4a, ba79193, 5a3df48, df7f698, bc5cdc2). All 9 ACs PASS; `cargo build`, `cargo test --workspace` (87 tests), `cargo fmt -- --check`, `cargo clippy --workspace -- -D warnings`, doc gate (`RUSTDOCFLAGS="-D warnings -D missing-docs" cargo doc --no-deps --workspace --all-features`), and `cargo build -p quartzite --no-default-features --features libm` all green. AGENTS.md size grew 33,328 → 33,457 chars (well under 35k early-warning). Closes #423.) | — |
| [miri-subset-widening](done/2026-05-17-miri-subset-widening.spec.md) | (CI / repo config — `.github/workflows/miri.yml`) | ✅ implemented (0 new tests directly; widens Miri subset by ~160 tests post-merge — doc only, single workflow edit, additive 17 lines — adds 6 new `cargo miri test` step blocks to the Miri Tree Borrows job alongside the existing v1 `-p`-style invocation: `--lib -p quartzite-widgets` (~83 lib `#[test]` items), `--lib -p quartzite-style` (~41), `-p quartzite-macros --test {extend,meta_enum,object,object_impl}` allow-list (~25), `widgets --test no_style_dep` (1), `widgets --test re_exports` (5), `style --test third_party_paint` (5). The design-time AC2 re-audit (`grep -nE 'use quartzite_renderer\|extern crate quartzite_renderer\|mod support'`) confirmed all 3 newly-widened integration test files are `--test`-FFI-free (zero matches). `MIRIFLAGS` unchanged at `-Zmiri-tree-borrows -Zmiri-ignore-leaks -Zmiri-disable-isolation`; `continue-on-error: true` v1 policy preserved at job level; existing v1 step (`cargo miri test (FFI-free crate subset)`) preserved verbatim. `ai-docs/unsafe-index.md` § Notes bullet at line 42 (106 chars) is unchanged — the "FFI-free subset" framing remains accurate post-v2 since the widening adds further FFI-free targets within the same envelope. Step shape: 6 separate `- name: cargo miri test (…)` blocks per the multi-step convention used by `ci.yml`'s `gpu-tests` job (per-step exit codes prevent failure masking). M=3 single-terminal-group every-group handoff. Closes #431.) | — |
| [miri-master-push-job](done/2026-05-17-miri-master-push-job.spec.md) | (CI / repo config — new `.github/workflows/miri.yml` + `ai-docs/unsafe-index.md` cross-reference) | ✅ implemented (0 new tests; CI + doc only — new `.github/workflows/miri.yml` runs `cargo +nightly miri test` under Tree Borrows (`MIRIFLAGS=-Zmiri-tree-borrows`) on every push to master against 9 FFI-free workspace crates: `quartzite-core`, `quartzite-geometry`, `quartzite-paint-api`, `quartzite-events`, `quartzite-event-types`, `quartzite-paint`, `quartzite-style-types`, `quartzite-style-dispatch`, `quartzite-runtime`. Excludes 4 crates: `quartzite-renderer` (winit/wgpu/vello prod deps), `quartzite-widgets` + `quartzite-style` (transitive renderer dev-deps), `quartzite-macros` (proc-macro). Nightly pin `nightly-2026-05-01` with `components: miri, rust-src`. `continue-on-error: true` v1 policy (flip to hard failure tracked as deferred follow-up). Master-only-comment AXIOM honoured (names 10–30× latency cost + absence of equivalent PR-side path). `actionlint .github/workflows/miri.yml` exit 0. Distinct `cache-shared-key: ${{ runner.os }}-nightly-miri-v1-${{ env.ImageVersion }}` collides with none of the existing `*-stable-v2-*` / `*-cargo-v2-*` / `*-cargo-coverage-v2-*` / `*-stable-gpu-v2-*` / `*-stable-features-*-v2-*` keys. Spec amended Round-1 → Round-2 to substitute `quartzite-runtime` from excluded → included set after the FFI-free audit (`grep -nE 'winit|wgpu|vello'` returned zero hits across runtime's `Cargo.toml` + `src/`); the spec's earlier "(winit/wgpu/vello dependents)" exclusion list was a factual error per the spec's own binding rule. `ai-docs/unsafe-index.md § Notes` gains a 106-char cross-reference bullet (within design's ≤ 110 budget). 2 commits on the branch: 5e39454 (workflow) + 7e138f8 (unsafe-index cross-ref). Tree Borrows chosen over Stacked Borrows because TB is the leading-candidate model for Rust's eventual official aliasing rules; SB has documented false positives in `Cell`/`RefCell`/`Rc`/`Arc` patterns. AC8 (first-post-merge artifact) verifies post-merge in the first master push CI run.) | — |
| [unsafe-index](done/2026-05-16-unsafe-index.spec.md) | (instruction docs only — new `ai-docs/unsafe-index.md` + `.claude/skills/task/SKILL.md` + `.claude/skills/task/reference.md` + `.claude/agents/review-findings.md` + `.claude/agents/self-review.md`) | ✅ implemented (0 new tests; doc-only — creates `ai-docs/unsafe-index.md` mirroring `ai-docs/panic-index.md`'s structure: preamble + per-entry schema (Location / Why unsafe / Safety invariant / Why not safe Rust / Preferred fix) + `## Active entries` + `### <crate> — <symbol>` H3 entries + `## Notes`. Initial population covers the 2 production unsafe blocks at `quartzite-renderer/src/wrapped_handler.rs:39` (raw-pointer Cell deref in `ActiveLoopGuard::drop`) and `quartzite-renderer/src/window_registry.rs:160` (raw-pointer `&*ptr` of `active_loop` in `try_create_window`). The `:160` entry's `Safety invariant` field cross-references the canonical in-source bodies (`# Safety invariants` doc block at `window_registry.rs:66-84` + inline `// SAFETY:` comment at `window_registry.rs:155-159`) rather than duplicating them — three-sources-of-truth drift prevention. Sync gate wired into the same 4 touchpoints that already enforce panic-index sync: `.claude/skills/task/SKILL.md` Step 9 verify-list mention + `## Decisions log` instruction, `.claude/skills/task/reference.md` new § *Step 9 — unsafe-index sync (detail)* + verify-list enumeration item 8 + Gate-checklist Step-9 row clause, `.claude/agents/review-findings.md` "Unsafe-index sync" bullet (parallel to existing panic-index bullet; severity `major`), `.claude/agents/self-review.md` "Unsafe-index sync" bullet (parallel; REJECT `major`). Spec amended Round-1→Round-2 to substitute `window_registry.rs:160` for the originally-named `wrapped_handler.rs:255` (which is inside `#[cfg(test)] mod tests` at line 222 of wrapped_handler.rs; out-of-scope under spec's own production-only rule). 5 commits on the branch (one per subtask): 98e5cc5, 31a44ee, 42925ac (Group A — unsafe-index.md + task SKILL/reference), c1e745e, cd7dcd1 (Group B — review-findings + self-review). Post-edit sizes: unsafe-index.md=5,589; task/SKILL.md=21,590; task/reference.md=30,100; review-findings.md=19,827; self-review.md=22,803 — all under 35k early-warning. AGENTS.md unchanged (33,328; spec § Scope item 5 conditioned the Agent-Docs row on existing panic-index row presence — no row exists today, so no symmetric edit performed; symmetry follow-up routed to deferred inbox).) | — |
| [agents-md-extraction](done/2026-05-16-agents-md-extraction.spec.md) | (instruction docs only — `AGENTS.md` + new `ai-docs/agent-docs-index.md` + new `ai-docs/api-naming.md`) | ✅ implemented (0 new tests; doc-only — proactive extraction to clear the 35k char-cap early-warning band Sub-check 9 fires on. § Agent Docs verbose row bodies (25 rows) moved to new `ai-docs/agent-docs-index.md` under `## Agent doc rows` (slug `#agent-doc-rows`); AGENTS.md keeps the table row enumeration with ≤ 80-char one-line stubs (per-row max 74, average 52). § API Naming entire body (the `_unchecked` AXIOM + per-shape table + 4 bullet rules) moved verbatim to new `ai-docs/api-naming.md` under `## The _unchecked AXIOM` (slug `#the-_unchecked-axiom`, letter-leading per GitHub slugger); AGENTS.md keeps the `## API Naming` heading + one-line stub-link (AC4 preserved). Two new `§ Agent Docs` rows for the new reference pages. Two-commit monotonic-decreasing branch: Group A commit `0e5b703` (AGENTS.md 39,960 → 35,608), Group B commit `2758548` (35,608 → 33,328). Post-extraction sizes: AGENTS.md=33,328 (≤ 34,000 AC5 with 672-char headroom, well below the 35k early-warning band); agent-docs-index.md=7,036; api-naming.md=2,825. `/ai-audit` Sub-check 9 demonstrator post-extraction emits zero findings — the pre-extraction `AGENTS.md: 39,960 chars — early warning` finding is gone. Content fidelity verified by MD5: the `_unchecked` AXIOM block + 4 bullet rules are byte-identical pre/post extraction; only deltas are permitted light restructure (trailing-period normalisation in row bodies; new `## Naming rules` subheading separating AXIOM from bullets in api-naming.md). All 4 downstream consumer references (`self-review.md:71`, `review-findings.md:52`, `spec-writer.md:136`, `ai-audit/SKILL.md:150`) cite section headings only — both `## API Naming` and `## Agent Docs` headings preserved, references remain valid.) | — |
| [ai-audit-charcap-axiom](done/2026-05-16-ai-audit-charcap-axiom.spec.md) | (Claude Code tooling — `ai-docs/agent-writing-style.md` + `.claude/skills/ai-audit/SKILL.md`) | ✅ implemented (0 new tests; instruction-file-only — codifies the 40,000-char file-size AXIOM as **Pattern 8** under `## Patterns` in `ai-docs/agent-writing-style.md`, with the 3-band threshold table (`< 35k` OK / `35k–39,999` early warning / `≥ 40k` STOP), explicit 9-entry covered-file enumeration, PR #324 extraction-model citation, per-commit invariant note, and `#383` forward reference for the planned mechanical pre-commit / CI gate. `.claude/skills/ai-audit/SKILL.md` Checklist M extended with **Sub-check 9** (file-size AXIOM conformance with verbatim copy-pasteable `wc -c` recipe + matching severity bands) and **Sub-check 10** (style-guide audit coverage map — ATX `## ` parser strictness rules + inline coverage map for all 7 live headings + `audit coverage gap: § <heading>` finding format for future style-guide additions). AC5 demonstrator confirmed end-to-end: `/ai-audit` Sub-check 9 surfaces AGENTS.md = 39,960 chars as the only `minor` finding, Sub-check 10 emits zero `nit` findings against the current 7 headings. AGENTS.md unchanged at every commit boundary (per-commit AC6 verified). Post-edit sizes: agent-writing-style.md = 13,209; ai-audit/SKILL.md = 27,082 — both well under 35k early-warning.) | — |
| [task-step-8-group-handoff](done/2026-05-16-task-step-8-group-handoff.spec.md) | (Claude Code tooling — `.claude/skills/task/SKILL.md` + `.claude/skills/task/reference.md` + `.claude/agents/design.md` + `.claude/agents/design-review.md` + `.claude/skills/context-reset/SKILL.md` + `AGENTS.md` + `ai-docs/agent-writing-style.md` + `ai-docs/learnings.md`) | ✅ implemented (0 new tests; instruction-file-only — replaces the `N=3 of M≥5` runtime handoff gate with an **every-group** contract: `/task` Step 8 spawns `/context-reset` at the start of every design-defined group, including the first and including single-subtask designs; orchestrator never executes subtask code in its own context during Step 8. `design.md` § *Rules → handoff-grouping* and `design-review.md` Step 3 checklist row updated in lockstep so `## Handoff plan` is mandatory for every M ≥ 1 (the prior M ≤ 4 OPTIONAL / auto-PASS carve-outs are removed); design.md gains a synthetic M = 1 example next to the existing M = 5. `context-reset/SKILL.md` frontmatter `when_to_use`, `allowed-tools` (DK-2: cargo + git family minus `git push *`; includes `git diff *`), § *Handoff protocol* "When triggered" line, § *Rules* "Maximum 3 handoffs per task" → "Maximum 3 design-defined groups per task", and a new AC8 per-group write-contract paragraph all updated. AGENTS.md Propagation Rule widened to include `.claude/skills/context-reset/SKILL.md` in the Task/Design sync group + new dedicated row keyed on context-reset; agent-writing-style.md propagation row extracted into `ai-docs/agent-writing-style.md § Propagation rule for new patterns` (with § Enforcement cohesion fix from circular → intra-file reference) to stay under AGENTS.md's 40,000-char hard cap. In-flow learning entry recorded PR #374 context-rot incident as the second failure mode the redesign addresses (`Escalated? no`, Boundary rule 2 Exception applies). M = 9 → 3 groups × 3 demonstrator. Post-edit sizes: AGENTS.md=39,960; task/SKILL.md=21,455; reference.md=28,867; design.md=5,216; design-review.md=4,662; context-reset/SKILL.md=7,823 — all under 35k early-warning except AGENTS.md (under 40k hard cap, 40 chars headroom).) | — |
| [draw-widget-type-system-redesign](done/2026-05-16-draw-widget-type-system-redesign.spec.md) | `quartzite-macros` `quartzite-widgets` `quartzite-style` `quartzite-style-dispatch` | ✅ implemented (43 new tests — `WidgetView<'a>` borrowed `#[non_exhaustive]` enum + `WidgetChildren<'a>` in `quartzite-widgets`; `#[widget_view(variant)]` + `#[widget_children(slice\|optional)]` proc-macro attributes on `Extend` derive; `Paint<W: AsWidget + ?Sized>` trait publicly exported from `quartzite-style`; `DefaultStyle::draw_widget` body replaced with `match widget.widget_view()` + six `impl Paint<W> for DefaultStyle` blocks; `children_of` downcast chain in `quartzite-style-dispatch` deleted, replaced with `AsWidget::children()`; third-party dispatch integration test covering `WidgetView::Other` open-set path; zero `.downcast_ref::<` in dispatch hotpath; all AC1–AC11 PASS) | — |
| [design-handoff-groups](done/2026-05-16-design-handoff-groups.spec.md) | (Claude Code tooling — `.claude/agents/design.md` + `.claude/agents/design-review.md` + `.claude/skills/task/SKILL.md` + `.claude/skills/task/reference.md`) | ✅ implemented (0 new tests; instruction-file-only — pre-computes the N=3-of-M≥5 handoff-gate trigger boundary at design-time so `/task` Step 8 reads it instead of re-deriving per turn. `design.md` gains a Rule entry enumerating all 4 wording sub-points (M≥5 trigger / ≤3 group cap / `/context-reset` destination / 1..=3 terminal sizing) + new `## Handoff plan` artifact-format section with an inline synthetic M=5 example (Group A: 1–3, handoff prose, Group B: 4–5). `design-review.md` gains one Workflow-Step-3 checklist row with three severities (missing/non-terminal≠3/terminal-outside-1..=3 = `major`; cosmetic = `minor`; M≤4 auto-pass). `task/SKILL.md` Step 8 sub-step 5 + `task/reference.md` § *N=3 of M≥5 handoff gate (rationale)* both cite the design's `## Handoff plan` as source-of-truth; runtime counter becomes a cross-check (Design Amendment recipe triggered on disagreement). Propagation Rule Task/Design sync group satisfied (canonical keyword `Handoff plan` present in all 4 files). This task's own design is the M≤4 demonstrator (M=4, no `## Handoff plan` section — absence-paragraph explains why). Single commit `535187e`, +18/-1 across 4 files. Post-edit sizes: design.md=4983, design-review.md=4702, task/SKILL.md=20975, task/reference.md=27984 — all under 35k early-warning.) | — |
| [ai-audit-checklist-m](done/2026-05-16-ai-audit-checklist-m.spec.md) | (Claude Code tooling — `.claude/skills/ai-audit/SKILL.md` + `ai-docs/agent-writing-style.md`) | ✅ implemented (0 new tests; instruction-file-only — `/ai-audit` Phase 2 gains Checklist M with 8 sub-checks (Patterns 1–7 + Anti-patterns table) auditing every fail-loud block in AGENTS.md + every SKILL.md + every agent.md + `ai-docs/{code-style,doc-convention,agent-writing-style,corrections-log}.md` against `agent-writing-style.md`. Severities: Pattern 1 / 4 / 7 / Anti-patterns = `major`; Pattern 2 / 3 = `minor`; Pattern 5 / 6 = `nit`. Pattern 7 sub-check drives off live grep (not the style guide's static table) so it auto-handles new callout-carrying skills. Pattern 6 heuristic tightened to require BOTH a Pattern 2 fail-loud verb AND a stronger contrast marker (`instead` / `wrong` / `correct` / `forbidden`) — drops the noisy `not` / `NOT` triggers. Reciprocal `## Enforcement` section added to `agent-writing-style.md` linking to the Checklist M heading (slug `m-agent-writing-stylemd-conformance` verified at commit-time). AGENTS.md UNCHANGED — Q1 disposition: AC6 satisfied by PR-body record line rather than a new Propagation Rule row (existing line-212 row covers forward direction; AC4 supplies reverse-direction discoverability). AC5 demonstrator surfaced real findings (Variant-B body drift in `/bugfix` + `/interview`; 4 paragraphs with >1 fail-loud verb across AGENTS.md / `/self-review` / code-style). False-positive self-references (audit recipe and style guide enumerating their own subject matter) recorded as a future `/improve` candidate for per-sub-check self-reference exception.) | — |
| [self-improve-step6-contract](done/2026-05-16-self-improve-step6-contract.spec.md) | (Claude Code tooling — `.claude/agents/self-improve.md`) | ✅ implemented (0 new tests; instruction-file-only — single-file edit to `.claude/agents/self-improve.md` rewriting the Step 6 (eval) contract. Old directive *"Run the scenario via an `Agent` subagent in a clean context"* removed; replaced with a pause-and-surface protocol that names the `Agent` primitive as **"structurally unfulfillable"** from inside the `self-improve` agent class (cites PR #362 Commit C + PR #364 P5 entries as authority). New Step 6 carries: (a) primitive-absence statement; (b) pause-and-surface protocol (introspect → assemble handoff block → yield; explicit list of forbidden same-context substitutions); (c) Propagation-Rule asymmetry note re `.claude/agents/learnings-escalation-audit.md` (sister file has no Step 6 eval phase → no mirror); (d) fenced reproducer-prompt template with named placeholders; (e) worked example anchoring the skeleton; (f) preserved `Eval: PASS ✅` / `Eval: FAIL ❌` strings with explicit "parent thread emits the final report" note. File size 7,233 → 10,787 chars (well under 35k early-warning). AC7 deferred to post-merge behavioural gate (next `/improve` natural invocation).) | — |
| [ci-failed-skills](done/2026-05-15-ci-failed-skills.spec.md) | (Claude Code tooling — `.claude/skills/pr-ci-failed/SKILL.md` + `.claude/skills/master-ci-failed/SKILL.md` + `AGENTS.md` + `.claude/skills/pr-merged/scripts/cleanup-progress.sh` + `.gitignore`) | ✅ implemented (0 new tests; instruction-file + script-only — two new code-side orchestrator skills mirroring `/pr-commented`'s shape: `/pr-ci-failed` handles CI failure on a PR branch (in-place fix + push to existing PR; downstream of `/task` Step 12, parallel with `/pr-commented`); `/master-ci-failed` handles CI failure on master (post-merge red build; fix lands as a new feature branch + new PR via `gh pr create`; master is never directly modified; downstream of `/pr-merged` for manual invocation in v1, auto-invoke deferred to a follow-up). Both skills enforce the AGENTS.md `## Workflow` axiom *"CI-fix commits get self-review too"* (loop cap 3). Both carry Variant-A Compaction recovery callout per `ai-docs/agent-writing-style.md § Pattern 7` with skill-specific probe paths (`/pr-ci-failed` uses the `grep -l "Tracked in:.*#${PR_NUM}\b" …` recipe; `/master-ci-failed` uses `ls ai-docs/master-ci/*.progress.md`). `learnings.md` boundary preserved (NEVER edited; CI logs threat model identical to PR comments). `cleanup-progress.sh` extended with a secondary `**Tracked in run:** <run-id>` probe so `/pr-merged` can clean up `/master-ci-failed`-created per-run progress files. AGENTS.md `## Agent Docs` gains two new rows; size 35,529 → 37,405 chars (past 35k early-warning, well under 40k hard cap; Path A budget ratified by design-review).) | — |
| [propagate-callout-to-style-guide](done/2026-05-15-propagate-callout-to-style-guide.spec.md) | (instruction docs only — `ai-docs/agent-writing-style.md` + `AGENTS.md` + `ai-docs/learnings.md`) | ✅ implemented (0 new tests; doc-only — new Pattern 7 *Compaction recovery callout* in `agent-writing-style.md` documenting the three per-skill variants (A: probe-driven path discovery; B: fixed-glob single-artefact; C: parent-routing) that PR #349 applied to 6 SKILL.md files but never recorded in the style guide; AGENTS.md `## Propagation Rule` grep recipe extended to scan `ai-docs/agent-writing-style.md` at both Procedure step 1 and AXIOM-table sites; new dedicated Propagation Rule row for `ai-docs/agent-writing-style.md` placed immediately before the `Any other instruction file` catch-all; in-flow `learnings.md` entry records the propagation miss with `Escalated? no` per Boundary rule 2 in-flow exception clause (3); unblocks #357 `/pr-failed` skill which would otherwise re-derive the variant taxonomy from the archival design doc.) | — |
| [increase-project-coverage](done/2026-05-15-increase-project-coverage.spec.md) | `quartzite-core` `quartzite-geometry` `quartzite-macros` `quartzite-paint-api` `quartzite-renderer` `quartzite-runtime` | ✅ implemented (70 new unit tests + 3 new public API methods; lifted project coverage from 90.42% → 94.92% lines / 93.18% regions (llvmpipe, with GPU tests); AC4 `LocalBrushKind<'a>` exhaustive-match restructure in `vello_painter.rs`; `Font::with_italic/underline/strikethrough` consuming-builder setters; `key_event_from_parts` inner-fn extraction in `event_convert.rs`; follow-up issue filed for 95% stretch goal) | — |
| [ci-macos-clippy-cache-fix](done/2026-05-15-ci-macos-clippy-cache-fix.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only — `ImageVersion`-aware cache keys appended to all 8 `cache-shared-key` values across `ci.yml`, `coverage.yml`, `docs.yml`; "Export ImageVersion" step sources `~/.bashrc` on macOS and writes to `$GITHUB_ENV` before toolchain install; `Verify cargo identity` diagnostic step added to all 9 cargo-running jobs. Fixes intermittent `error: unexpected argument 'clippy' found` caused by stale macOS runner image cache restoring a `rustup-init` shim from an older image version.) | — |
| [sonnet-skill-reentry-protocol](done/2026-05-14-sonnet-skill-reentry-protocol.spec.md) | Claude Code tooling (`.claude/skills/{task,interview,bugfix,project-review,pr-commented,context-reset,verify-change,pr-merged}/SKILL.md` + new `.claude/skills/task/reference.md` + `.claude/agents/{review-findings,self-review,spec-writer}.md` + `AGENTS.md` + `ai-docs/templates/progress-format.md`) | ✅ implemented (0 new tests; instruction-file + sibling-reference only — per-skill "Compaction recovery check" callout on 6 code-side orchestrators using three variants (A: glob-based for `/task`+`/project-review`+`/pr-commented`; B: fixed-path for `/bugfix`+`/interview`; C: parent-routing for `/context-reset`); canonical `## Compaction recovery (re-entry)` h2 anchor in `/context-reset` SKILL.md is the singular cross-link destination. Five new schema fields on `.progress.md` (`current_step`, `decisions_log`, `last_passed_gate`, `parent_skill`, `entry_args`) — `/bugfix` extends its trace file with the same fields inline; `/interview` is exempt (in-flight spec + `.state.md` is the durable state); `/verify-change` and `/pr-merged` are 1-line waivered. `/task` SKILL.md reordered workflow-on-top and extracted reference material to `reference.md` sibling: 29,996 → 19,861 chars (AC2). `/task` `⚡ First` preamble grows the lost-arguments routing clause; `⚡ Second` and `⚡ Third` carry non-empty-`$ARGUMENTS` guard sentences. Encodes the **Full-read-on-re-entry invariant**: re-read durable state end-to-end, then re-enter from the top of the skill body (preambles + Step 1) — never skip-to-`current_step`. AC9 deferred to post-merge manual Sonnet `/task` spot-check.) | — |
| [macos-cargo-path-pin](done/2026-05-14-macos-cargo-path-pin.spec.md) | CI / repo config (`.github/workflows/{ci,base_benchmarks,coverage,docs,fork_pr_benchmarks_run}.yml`) | ✅ implemented (0 new tests; CI config only — `dtolnay/rust-toolchain` → `actions-rust-lang/setup-rust-toolchain@v1` across all 5 workflow files (11 install sites); fixes macOS `cargo` → `rustup-init` shadowing that became constant on PR #339. Explicit `Swatinem/rust-cache@v2` (6 sites in `ci.yml`) and `actions/cache@v5` (1 each in `coverage.yml` + `docs.yml`) dropped — the new action's bundled cache absorbs them via per-job `cache-shared-key:`; bare shared-keys on `coverage.yml` / `docs.yml` (no `hashFiles('**/Cargo.lock')`) keep the bundled cache's `cacheKey`/`restoreKey` split equivalent to today's `actions/cache@v5` `key:`/`restore-keys:` partial-prefix semantics. `RUSTFLAGS=-D warnings` action-default accepted (extends to `build`/`test`/`bench`/`doc`/`llvm-cov`); 30-LOC in-PR fix budget for latent-warning fallout (AC11). `coverage.yml` gains explicit `components: llvm-tools-preview`; all other `components:` selections preserved.) | — |
| [renderer-style-dispatch](done/2026-05-13-renderer-style-dispatch.spec.md) | `quartzite-style-dispatch` `quartzite-style` | ✅ implemented (15 new tests — 12 unit + 3 doc-tests; `dispatch_paint` free function + `WidgetResolver` trait in new `quartzite-style-dispatch` bridge crate; depth-first tree walk with save/translate/restore; no-op when no style installed; `tracing::warn!` on resolver miss; closure blanket impl; `test-support` Cargo feature on `quartzite-style` exposes `StyleRegistry::clear_for_test()` to integration tests) | — |
| [shrink-context-md](done/2026-05-13-shrink-context-md.spec.md) | (docs only) | ✅ implemented (0 new tests; `ai-docs/context.md` 37,139 → 29,979 chars (~19% reduction; 21 chars under 30k target). KDD table partitioned 22 KEEP-INLINE + 35 EXTRACT; 35 extracted rows moved to new `ai-docs/key-decisions.md` (13,840 chars) with verbatim-Question H2 anchors; 30 Maintenance-plans entries compressed to one-liners with `[spec](plans/done/<file>)` links; `quartzite-renderer`/`quartzite-style-types`/`quartzite-style` crate-layout rows deduped against KDD; `### Maintenance plans (cross-cutting)` heading preserved verbatim so `INDEX.md:115` anchor still resolves; AGENTS.md § Agent Docs gains one row for the new reference page) | — |
| [cleanup-progress-issue-derive](done/2026-05-13-cleanup-progress-issue-derive.spec.md) | (Claude Code tooling — `.claude/skills/pr-merged/scripts/cleanup-progress.sh`) | ✅ implemented (0 new tests — bash script change; live-verified AC1–AC7 against PR #324 / #321 / #172. Fixed latent bug since PR #300: script derived spec-lookup key from the merged PR number, but `/task` + `/interview` write the **issue** number in `**Tracked in:**`. Now a `gh pr view` + `grep -oiE '(Close[sd]?\|Fix(es\|ed)?\|Resolve[sd]?) #[0-9]+'` step extracts the issue number from the merged PR body's `Closes/Fixes/Resolves #N` line, then a tightened `grep -lE "^\*\*Tracked in:\*\* #<ISSUE_NUM>\b"` (anchored to prevent prose false-matches) looks up the spec. Both miss cases emit one-line stderr warnings instead of silent no-ops. Docstring rewritten. `shellcheck` clean.) | — |
| [shrink-agents-md](done/2026-05-13-shrink-agents-md.spec.md) | (instruction docs only) | ✅ implemented (0 new tests; `AGENTS.md` 40,572 → 31,466 chars (~22% reduction); AXIOM-vs-redundant-prose dedup across `## Workflow` / `## Dependency Versions` / `## Propagation Rule`; narrative extraction into two new reference pages `ai-docs/workflow.md` (PR review comment resolution GraphQL recipe) and `ai-docs/corrections-log.md` (Boundary rule 1 / 2 Exception bodies + Entry-format field glossary); all 8 AXIOMs preserved verbatim; Propagation sync-group lookup table preserved in AGENTS.md; `.claude/skills/task/SKILL.md:269` + `.claude/skills/pr-commented/SKILL.md:183` updated to point at the new workflow.md anchor) | — |
| [default-style-snapshot-tests](done/2026-05-13-default-style-snapshot-tests.spec.md) | `quartzite-style` | ✅ implemented (7 GPU snapshot tests — AC1–AC13; `quartzite-style/tests/snapshots.rs` + `tests/support/mod.rs` mirroring widgets helper contract; 7 golden PNGs in `shared/`; `harness_or_skip` lifted into both support modules; snapshot-helper sync group added to AGENTS.md; `scripts/update-snapshots.sh` `--crate` flag added) | — |
| [default-style-content](done/2026-05-13-default-style-content.spec.md) | `quartzite-style` | ✅ implemented (10 new tests — AC1 `Send+Sync` trait-object; AC2–AC5 per-widget RecordingPainter assertions; AC6 unknown-widget no-op; AC7 checked-vs-idle button colour; AC8 disabled half-alpha; AC10 `StyleRegistry` round-trip; `DefaultStyle` zero-sized struct + `draw_widget` downcast router + `draw_button`/`draw_label`/`draw_text_edit`/`draw_scroll_area` private methods; `brush`/`disabled`/`maybe_disabled` helpers) | — |
| [renderer-painter-impls](done/2026-05-12-renderer-painter-impls.spec.md) | `quartzite-renderer` `quartzite-widgets` | ✅ implemented (17 new tests — 3 `VelloPainter` unit tests, 2 `FontCache` tests, 2 `RenderHarnessBuilder` tests, 10 GPU snapshot tests AC1–AC12; `VelloPainter` full 11-method `Painter` impl with parley+skrifa text; `FontCache` struct; `RenderHarnessBuilder` with `scale_factor`; `draw_text`/`draw_text_in` via parley layout + skrifa glyph metrics + vello glyph rendering; bug fix: `emit_layout_glyphs` no longer double-counts centering offset from `positioned_glyphs()`) | — |
| [multi-window-support](done/2026-05-11-multi-window-support.spec.md) | `quartzite-renderer` | ✅ implemented (19 new tests — 7 `WrappedHandler` dispatch unit tests, 4 `event_convert` unit tests, 2 `WindowRegistry` unit tests, 1 `WindowId` unit test, 5 integration tests; `WindowedAppHandler` trait + `WindowedApplicationBuilder` + `WindowRegistry` + multi-window event fan-out wired into `WrappedHandler`; `try_create_window` / `windows()` API; `quit_on_last_window_closed` builder flag; `CloseRequested` teardown + last-window-quit policy; per-window `WidgetRoot` dispatch) | — |
| [triage-bridge](done/2026-05-11-triage-bridge.spec.md) | Claude Code tooling (`.claude/skills/triage/SKILL.md` + `.claude/agents/triage-runner.md` + `tests/fixtures/process-improvements/`) | ✅ implemented (0 new tests; instruction-file + fixtures only — C (final) of process-improvements: extends `/triage` with md ↔ gh issues bridge. `triage-runner.md` Phase 4 `--json` projection widened `number,title` → `number,state,title` (single bulk-call invariant preserved); map shape upgraded to `{number → {state, title}}` with derived `{title → #N}` view from same pass. New Phase 4.5 (Bridge sweep) between Phase 4 and Phase 5 iterates `Tracked` cells across 10 sources (8 thematic cell-4 + `_inbox.md` cell-4 ONLY when `#N` not `—` + `widget-backlog.md` `Notes` cell tracked-prefix); classifies 3 conflict types (stale tracked: closed `#N`; status mismatch: widget-backlog `✅` vs OPEN; untracked-candidate count); idempotency short-circuit on `(closed)` substring; per-conflict prompt with `update md` / `update issue` / `keep both`; `update issue` surfaces diff preview + yes/no gate before any `gh issue close/reopen`; concurrent-edit guard inherits B's content-snapshot rule. Phase 8 summary gains a Bridge sub-section (after status table, before issues-created). `triage/SKILL.md` Bridge placeholder replaced with real prose; frontmatter `allowed-tools` extended with `Bash(gh issue close *)` + `Bash(gh issue reopen *)`. 1 new fixture `divergence-cases.md` (synthetic `#9001` OPEN + `#9002` CLOSED + embedded mock map); 2 stale-tense fixes (Task 6 propagation); cell-4-`Tracked` invariant preserved; `widget-backlog.md` 3-column schema unchanged; AC1/2/5 mechanical PASS, AC3 fixture PASS, AC4/6/7 manual reviewer-run per spec) | — |
| [triage-skill](done/2026-05-11-triage-skill.spec.md) | Claude Code tooling (`.claude/skills/triage/SKILL.md` + `.claude/agents/triage-runner.md` + `AGENTS.md` + `.claude/skills/improve/SKILL.md` + `tests/fixtures/process-improvements/`) | ✅ implemented (0 new tests; instruction-file + fixtures only — B of process-improvements: new `/triage` skill (`disable-model-invocation: true`, 9 allow-listed tools) + new `triage-runner` opus subagent with 9-phase workflow (branch check → threshold gate → identify candidates → bulk `gh issue list` dedupe → draft → present batch + collect approvals **(deferred creates)** → drain `_inbox.md` per-entry sort/promote/drop/keep **(deferred promote-creates)** → Phase 7.5 combined `gh issue create` pass over union of approvals **(single bulk per spec)** → update `deferred-items.md` + summary); pagination watchdog at ≥ 0.9× limit; content-snapshot concurrent-edit guard (NOT mtime); mutation scope strictly `ai-docs/deferred/**` + `gh issue create/edit`; AGENTS.md *Agent Docs* row + 3-row Triage group in *Propagation Rule* + *Sync groups* prose bullet; one-line cross-ref in `improve/SKILL.md`; 3 fixtures (`widget-backlog-promotion.md`, `triage-decline.md`, `triage-inbox-3rows.md`); cell-4-`Tracked` invariant preserved across all 10 `ai-docs/deferred/*.md` files; widget-backlog 3-column schema unchanged; AC1/4/5/7 mechanical PASS, AC2/3/6/8 manual reviewer-run per spec) | — |
| [task-step12-inbox-backfill](done/2026-05-10-task-step12-inbox-backfill.spec.md) | Claude Code tooling (`.claude/skills/task/SKILL.md` + `AGENTS.md` + `ai-docs/deferred/_inbox.md` + `tests/fixtures/process-improvements/`) | ✅ implemented (0 new tests; instruction-file + fixtures only — A2 of process-improvements: creates `ai-docs/deferred/_inbox.md` with 4-column `\| Item \| Source \| Section \| Tracked \|` schema; extends `/task` Step 12 with new sub-step 4 *Inbox propagation* that parses just-finalised spec/design *Out of scope* / *Deferred* / *Open questions* sections via 6-shape parser (NONE / TABLE / PIPEBULLET3 / PIPEBULLET2 / BOLDBULLET / PLAINBULLET) with warn-and-skip fallback; one-shot deduped backfill walked all 111 `done/*` files in chronological order, appended **294 rows** drawn from 92 emitting sections × ~3.2 items per section (1 WARN: `recursive-inline-annotations.design.md :: Open questions` numbered-list body), file-level dedupe against the 14-file H set (12 specs + 2 designs already harvested by commits a8f23d5/0304e8e/8bd8c26); 9 synthetic fixtures at `tests/fixtures/process-improvements/`; AGENTS.md `; introduced in Issue A2` suffix dropped on line 243; cell-4-`Tracked` invariant preserved across all 9 + 1 `ai-docs/deferred/*.md` files) | — |
| [next-deferred-discoverability](done/2026-05-10-next-deferred-discoverability.spec.md) | Claude Code tooling (`.claude/skills/next/SKILL.md` + `AGENTS.md`) | ✅ implemented (0 new tests; instruction-file-only — A1 of process-improvements: extends `/next` to read 8 thematic deferred files + `widget-backlog.md` via 9 `!`-blocks; surfaces untracked rows in new *Candidates needing /triage* output section; adds AGENTS.md AXIOM restricting `_inbox.md` writes to `/task` Step 12 + `/triage`; adds *Agent Docs* row for `_inbox.md`; AC1/AC2 deferred to manual `/next` reviewer-run per spec) | — |
| [docs-cleanup-197](done/2026-05-10-docs-cleanup-197.spec.md) | all crates (Cargo.toml metadata + `src/**/*.rs` doc comments + `quartzite-macros/src/{object,extend}/codegen.rs`) + `ai-docs/doc-convention.md` | ✅ implemented (0 new tests; metadata + doc-comment + convention text + macro-codegen-doc only — Part A: `all-features = true` propagated to 11 leaf-crate `[package.metadata.docs.rs]` blocks; Part B: cross-crate intra-doc-link audit re-verified all targets are direct deps; Part C: bare-backtick → intra-doc-link audit + conversion across `src/**/*.rs`; Part D: 3 new bullets + 3 before/after pairs in `ai-docs/doc-convention.md` § *Linking and code references*; Part E: `quartzite-macros` codegen audit — converted bare `` `Auto` `` / `` `Queued` `` to umbrella-pathed intra-doc links in macro-emitted `#[doc=…]` attributes, verified via `cargo expand` on `Counter` test type; 11 forced-bare qualified-path sites in `quartzite-core` / `quartzite-macros` left bare per dep-graph direction) | — |
| [object-property-serialization-layer](done/2026-05-10-object-property-serialization-layer.spec.md) | `quartzite-core` `quartzite-runtime` `quartzite` | ✅ implemented (34 new tests — 8 core error/type unit tests, 5 object-layer unit tests, 8 tree-layer unit tests incl. WeakObjectRef remap, 13 integration tests covering JSON + bincode + CustomValue; `serde` Cargo feature on quartzite-core/runtime/facade; `Value`/`WeakObjectRef` Serialize/Deserialize; typetag supertrait on CustomValue; `ObjectSnapshot`, `TreeSnapshot`, `ObjectNode`, `SerializeError`, `DeserializeError`; `capture_object`/`restore_object`/`capture_tree`/`restore_tree`; `quartzite::snapshot` facade module) | — |
| [gpu-snapshot-tests-ci](done/2026-05-10-gpu-snapshot-tests-ci.spec.md) | `quartzite-renderer` `quartzite-widgets` CI / repo config | ✅ implemented (15 new tests — 4 harness incl. GPU smoke, 8 helper-internals, 5 widget snapshots; 5 vulkan goldens committed; new `gpu-tests` matrix CI job with Win/Mac `continue-on-error: true` until per-backend goldens are bootstrapped; `xvfb_smoke` Linux integration test; `actions/upload-artifact@v7` for `*.actual.png`/`*.diff.png` on failure; `scripts/update-snapshots.sh`; CONTRIBUTING.md `## GPU snapshot tests` section; wgpu pinned 29 → 28 to match vello 0.8) | — |
| [ci-skip-rust-matrix](done/2026-05-09-ci-skip-rust-matrix.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only — `dorny/paths-filter@v4` `changes` job gates Rust matrix on Rust-affecting paths; aggregators reshaped to treat `skipped` as `success`; new `docs-pass` aggregator) | — |
| [interview-spec-writer-subagent](done/2026-05-09-interview-spec-writer-subagent.spec.md) | Claude Code tooling (`.claude/agents/`, `.claude/skills/interview/`, `AGENTS.md`) | ✅ implemented (0 new tests; instruction-file-only — extracts `/interview` spec-drafting into `spec-writer` opus subagent with structured YAML output; AC4–AC7 live tests deferred to post-merge per Verification protocol) | — |
| [ci-rust-cache-migration](done/2026-05-08-ci-rust-cache-migration.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only — `actions/cache@v5` → `Swatinem/rust-cache@v2` in 5 compile jobs; `SCCACHE_CACHE_SIZE: "2G"` added per-job; `shared-key` + `save-if: master only` tuning) | — |
| [ci-sccache](done/2026-05-08-ci-sccache.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only — sccache layer added to 5 merge-gate compile jobs in ci.yml) | — |
| [widgets](done/2026-05-01-widgets.spec.md) | `quartzite-widgets` | ✅ implemented (64 unit + 82 doc tests) | — |
| [project-docs](done/2026-05-08-project-docs.spec.md) | `quartzite` (facade) + repo-level docs + CI | ✅ implemented (0 new tests; README description block + comprehensive `lib.rs` rustdoc + `CONTRIBUTING.md` + auto-generated `ROADMAP.md` + CI sync-gate) | — |
| [generic-simple-tags](done/2026-05-07-generic-simple-tags.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (0 new tests; annotation-only) | — |
| [coverage-ci](done/2026-05-07-coverage-ci.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only) | — |
| [criterion-benchmarks](done/2026-05-07-criterion-benchmarks.spec.md) | `quartzite-core` `quartzite-runtime` CI | ✅ implemented (0 new tests; 10 benches, 3 CI workflows) | — |
| [cargo-doc-pages](done/2026-05-07-cargo-doc-pages.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only) | — |
| [macro-object-bench](done/2026-05-07-macro-object-bench.spec.md) | `quartzite` (facade) | ✅ implemented (0 new tests; 6 benches via criterion + macro-derived fixture) | — |
| [graphics-stack](done/2026-05-03-graphics-stack.spec.md) | `quartzite-paint-api` `quartzite-paint` `quartzite-renderer` | ✅ implemented (39 new tests) | — |
| [code-style-extraction](done/2026-05-07-code-style-extraction.spec.md) | (docs only) | ✅ implemented (0 new tests; docs only) | — |
| [generic-fn-split](done/2026-05-07-generic-fn-split.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (0 new tests; refactoring) | — |
| [per-thread-event-loops](done/2026-05-06-per-thread-event-loops.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` | ✅ implemented (7 new tests) | — |
| [tracing-spans](done/2026-05-06-tracing-spans.spec.md) | `quartzite-core` `quartzite-runtime` `quartzite` | ✅ implemented (0 new tests) | — |
| [object-tree-query](done/2026-05-06-object-tree-query.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` | ✅ implemented (28 new tests) | — |
| [signal-to-signal](done/2026-05-06-signal-to-signal.spec.md) | `quartzite-core` `quartzite-macros` `quartzite` | ✅ implemented (23 new tests) | — |
| [thiserror-migration](done/2026-05-05-thiserror-migration.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (0 new tests) | — |
| [tracing-itertools](done/2026-05-05-tracing-itertools.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (0 new tests) | — |
| [log-facade](done/2026-05-05-log-facade.spec.md) | `quartzite-core` `quartzite-runtime` `quartzite` | ✅ implemented (0 new tests) | — |
| [paint-brush-gradient-variants](done/2026-05-14-paint-brush-gradient-variants.spec.md) | `quartzite-paint-api` `quartzite-paint` `quartzite-renderer` `quartzite-style` `quartzite-widgets` | ✅ implemented (4 new GPU snapshot tests; `BrushKind::LinearGradient`, `RadialGradient`, `Custom(peniko::Gradient)` variants; `Brush::linear_gradient` / `radial_gradient` / `custom_gradient` constructors; `Brush`/`BrushKind` lose `Copy`; `VelloPainter::brush_to_peniko` handles all live variants; peniko gradient re-exports in `quartzite-paint`; `quartzite-style::default_style::brush_color` covers gradient variants) | — |
| [button-hover-pressed-focused-states](done/2026-05-15-button-hover-pressed-focused-states.spec.md) | `quartzite-paint-api` `quartzite-widgets` `quartzite-style` | ✅ implemented (26 new tests — 5 `Color::blend` unit+doctest; 3 `WidgetBase` default assertions; 6 accessor tests; 4 event-handler default tests; 7 recording-painter tests (AC3–AC6, AC11); 3 snapshot goldens in `shared/`; `Color::blend` `const fn #[inline]`; three `pub bool` fields on `WidgetBase`; six `WidgetExt` accessors; four updated event-handler defaults; `DefaultStyle::draw_button` with hover/pressed/focused rendering) | — |
| [container-lineedit-rendering](done/2026-05-15-container-lineedit-rendering.spec.md) | `quartzite-style` | ✅ implemented (8 new tests — AC1/AC2 `Container` fill+outline assertions; AC3–AC8 `LineEdit` idle/text/placeholder/read-only assertions; `#[path]`-extracted test module to `default_style_tests.rs`; `draw_container` + `draw_line_edit` private methods; `quartzite-core` dev-dep for `ObjectId` in tests) | — |
| [paint-style](done/2026-05-09-paint-style.spec.md) | `quartzite-paint-api` `quartzite-paint` `quartzite-geometry` `quartzite-widgets` `quartzite-style-types` (new) `quartzite-style` (new) | ✅ implemented (38 new tests; full Painter trait + paint-side Font/Image/Path; new `quartzite-style-types` leaf + `quartzite-style` downstream crates with `Box::leak`-backed `StyleRegistry`; `Alignment` moved to `quartzite-geometry`; `style ↔ widgets` cycle broken by leaf-crate split, enforced by `cargo tree` integration test) | — |
| [textedit-caret-selection](done/2026-05-23-textedit-caret-selection.spec.md) | `quartzite-paint-api` `quartzite-style` `quartzite-widgets` `quartzite-renderer` | ✅ implemented (14 ACs; 2 new cursor traits + 2 new Painter methods; StyleClock blink-phase seam; DefaultStyle converted from unit struct to clock-bearing struct; TextEdit caret/selection state fields + slots + signal; paint_caret + paint_selection helpers; 8 GPU snapshot goldens; blink-timer integration tests; parley-backed VelloPainter cursors. Closes #317.) | — |
| [lineedit-caret-selection](done/2026-05-24-lineedit-caret-selection.spec.md) | `quartzite-style` `quartzite-widgets` | ✅ implemented (15 ACs; reuse-only — all seams from #317 consumed unchanged; LineEdit gains caret/selection_anchor/selection_changed/selection_range + set_caret slot + set_selection_anchor method; Paint<LineEdit> extracted to default_style/line_edit.rs with paint_caret_line_edit (vertically centred) + paint_selection_line_edit (single-rect, no text_visual_lines); 10 GPU snapshot goldens; 13 symbolic-AC tests + 11 widget-side unit tests. Closes #405.) | — |

## Completed plans

| Plan | Crate(s) | Status | Blocked by |
|------|----------|--------|------------|
| [core-types](done/2026-05-01-core-types.spec.md) | `quartzite-core` | ✅ implemented (45 tests) | — |
| [github-workflow](done/2026-05-01-github-workflow.spec.md) | CI / repo config | ✅ live | — |
| [multi-platform-ci](done/2026-05-07-multi-platform-ci.spec.md) | CI / repo config | ✅ implemented (0 new tests; CI config only) | — |
| [macros](done/2026-05-01-macros.spec.md) | `quartzite-macros` | ✅ implemented (47 tests) | — |
| [runtime](done/2026-05-01-runtime.spec.md) | `quartzite-runtime` | ✅ implemented (176 tests) | — |
| [auto-connection](done/2026-05-01-auto-connection.spec.md) | `quartzite-core` (extension) | ✅ implemented (6 tests) | — |
| [geometry-events](done/2026-05-01-geometry-events.spec.md) | `quartzite-geometry` `quartzite-events` | ✅ implemented (26 unit + 91 doc tests) | — |
| [code-quality-cleanup](done/2026-05-02-code-quality-cleanup.spec.md) | `quartzite-macros` `quartzite-runtime` `quartzite-core` | ✅ implemented (0 new tests) | — |
| [docs-and-facade](done/2026-05-02-docs-and-facade.spec.md) | all crates + `quartzite` | ✅ implemented (1 new test) | — |
| [public-api-docs](done/2026-05-02-public-api-docs.spec.md) | all crates | ✅ implemented (47 new doctests) | — |
| [lookup-perf](done/2026-05-02-lookup-perf.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` | ✅ implemented (21 new tests) | — |
| [inline-simple-fns](done/2026-05-02-inline-simple-fns.spec.md) | all crates | ✅ implemented (8 new tests) | — |
| [examples-crate](done/2026-05-02-examples-crate.spec.md) | `quartzite-examples` `quartzite` | ✅ implemented (0 new tests; 4 runnable examples) | — |
| [signals-blocked](done/2026-05-02-signals-blocked.spec.md) | `quartzite-core` `quartzite-macros` | ✅ implemented (13 new tests) | — |
| [receiver-guard-auto](done/2026-05-03-receiver-guard-auto.spec.md) | `quartzite-core` `quartzite-macros` | ✅ implemented (4 new tests) | — |
| [connect-queued-codegen](done/2026-05-03-connect-queued-codegen.spec.md) | `quartzite-macros` | ✅ implemented (3 new tests) | — |
| [enumflags2-property-flags](done/2026-05-03-enumflags2-property-flags.spec.md) | `quartzite-core` `quartzite-macros` | ✅ implemented (6 new tests) | — |
| [signal-emit-checked](done/2026-05-03-signal-emit-checked.spec.md) | `quartzite-core` `quartzite-macros` | ✅ implemented (3 new tests) | — |
| [objectbase-debug-rename-factory](done/2026-05-03-objectbase-debug-rename-factory.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (16 new tests) | — |
| [macro-codegen-improvements](done/2026-05-03-macro-codegen-improvements.spec.md) | `quartzite-macros` | ✅ implemented (30 new tests) | — |
| [object-part-redesign](done/2026-05-03-object-part-redesign.spec.md) | `quartzite-macros` `quartzite` | ✅ implemented (27 new tests) | — |
| [doc-convention](done/2026-05-05-doc-convention.spec.md) | all crates | ✅ implemented (workspace-wide doc convention; 23+ new doctests; 645 tests total) | — |
| [parent-children-accessors](done/2026-05-05-parent-children-accessors.spec.md) | `quartzite-core` `quartzite-runtime` | ✅ implemented (1 integration test covering AC1–AC9, 1 unit test) | — |
| [timer-object](done/2026-05-05-timer-object.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` | ✅ implemented (36 new tests) | — |
| [signal-emit-rename](done/2026-05-05-signal-emit-rename.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` | ✅ implemented (3 renamed tests; 0 new) | — |
| [signal-emit-macro](done/2026-05-06-emit-macro.spec.md) | `quartzite-core` `quartzite-macros` `quartzite-runtime` `quartzite` | ✅ implemented (3 new tests) | — |
| [event-types-crate](done/2026-05-06-event-types-crate.spec.md) | `quartzite-event-types` `quartzite-events` `quartzite-runtime` | ✅ implemented (4 new tests) | — |
| [recursive-inline-annotations](done/2026-05-07-recursive-inline-annotations.spec.md) | `quartzite-core` `quartzite-geometry` `quartzite-runtime` | ✅ implemented (0 new tests; annotation-only) | — |
| [all-features-clippy-debt](done/2026-05-30-all-features-clippy-debt.spec.md) | `quartzite-core` `quartzite-runtime` CI | ✅ implemented (0 new tests; lint cleanup — 35 `--all-features` clippy sites across `quartzite-core` (28) + `quartzite-runtime` (7) + Feature-matrix clippy CI gate (no `--all-targets`); pre-existing `#[object_impl]` macro-doc-deny class under `--all-targets --all-features` deferred to #587. Closes #586.) | — |
| [macro-doc-deny-debt](done/2026-05-30-macro-doc-deny-debt.spec.md) | `quartzite` examples/tests/benches `quartzite-macros` `quartzite-style` CI | ✅ implemented (0 new tests; resolved 51 `#[object_impl]` macro-doc-deny hard errors under `--all-targets --all-features` — genuine `///` docs on 15 `examples/` sites + per-block `undocumented = "allow"` opt-out on 36 macro-internal fixtures; extended the Feature-matrix clippy CI step to `--all-targets`. Closes #587, closes the deferred #586 gap.) | — |

## Deferred plans

| Plan | Crate(s) | Status | Blocked by |
|------|----------|--------|------------|

Tracked future work without dedicated specs (cross-cutting items only — not plans). INDEX.md-only footnote; not surfaced in `ROADMAP.md`.

- **#35** dynamic_properties — runtime read/write of non-schema properties
- **#39** signals_blocked serde — ✅ implemented in PR #537
- **#48** BlockingQueued connection type — ready (per-thread loops ✅ implemented)
- **#52** object mobility / thread migration with stale `thread_id` invalidation
- **#53** multi-window support — ✅ implemented (see Active plans row)
- **#56** property system extensions — computed properties / bindings / custom getter/setter closures
- **#58** Python interop crate (`quartzite-python` via PyO3)

## Dependency order

```
core-types ✅
├── geometry-events ✅
│   └── graphics-stack             ✅ implemented (quartzite-paint-api + quartzite-paint stub + quartzite-renderer scaffold)
│       ├── quartzite-paint-api    ✅ (thin no_std crate; 11-method Painter trait + Color/Pen/Brush/Font/Image/Path/PaintError)
│       ├── quartzite-paint        ✅ (re-export shell over paint-api + Alignment from geometry; full vocabulary completed in #47)
│       └── quartzite-renderer     ✅ full impl (#53 multi-window + #289/#277 real VelloPainter — all 11 Painter methods live; text via parley+skrifa)
├── macros ✅
├── runtime ✅
│   ├── auto-connection ✅
│   ├── widgets (#46)              ✅ implemented (refactored in #47 to re-export Alignment / Font / Palette from upstream)
│   └── paint-style (#47)          ✅ implemented (full Painter trait + paint-side Font/Image/Path; quartzite-style-types leaf + quartzite-style downstream)
│       └── default-style-content (#290) ✅ implemented (DefaultStyle concrete impl: Button/Label/TextEdit/ScrollArea draw_widget routing)
│           ├── default-style-snapshot-tests (#297) ✅ implemented (7 GPU snapshot goldens via RenderHarness; snapshot-helper sync group)
│           │   └── paint-brush-gradient-variants (#281) ✅ implemented (LinearGradient/RadialGradient/Custom BrushKind variants; brush_to_peniko; 4 GPU snapshot tests)
│           ├── button-hover-pressed-focused-states (#316) ✅ implemented (hover/pressed/focused visual states; Color::blend; 3 new snapshot goldens)
│           ├── container-lineedit-rendering (#318) ✅ implemented (8 new tests; `draw_container` + `draw_line_edit`; `#[path]` test-file extraction)
│           ├── hover-focused-label-textedit-scroll (#403) ✅ implemented (28 new tests; state_group helper extraction; state-aware Paint impls for Label/TextEdit/ScrollArea; FocusRing alpha-exempt per design-system rule; 12 new snapshot goldens)
│           └── lineedit-hover-pressed-focused (#406, folds in #407) ✅ implemented (10 new recording-painter tests + 9 new snapshot goldens; brings Paint<LineEdit> to parity with the post-#403 widgets, closing the state-aware-paint matrix across all 5 interactive widgets; folds in #407 disabled-axis parity — disabled-LineEdit now visibly halves alpha on Base fill + Text outline + Text glyphs)
└── github-workflow ✅
    └── multi-platform-ci ✅        (Windows/macOS runners — build/test/clippy on all 3 OSes)
```

Serialization-layer track (#107) ✅ implemented — unblocks #39.

Coverage track (#341) ✅ implemented — project coverage lifted to 94.92% lines / 93.18% regions; 95% stretch deferred to follow-up issue.

Maintenance plans (cross-cutting, all ✅): see [`../context.md` § Maintenance plans](../context.md#maintenance-plans-cross-cutting) for the canonical list. These touched multiple crates and are not part of the dependency tree.

## Suggested next steps

1. **Process-improvements meta-plan COMPLETE.** A1 (#202), A2 (#203), B (#204), and C (#205) have all merged. The meta-plan file moved to `done/` in C's Step 12 commit. The deferred store migrated to canonical **JSONL** in #596 (`ai-docs/deferred/*.jsonl`, one JSON object per line); run `/triage` against current data to drain `_inbox.jsonl` and to flag the canonical `#60` stale-tracked refs in `ci-docs-workflow.jsonl`. Subsequent suggested next steps now revert to the framework / v1-UI-designer track:
1. **Input event-dispatch loop (follow-up to #395)** — the paint-free `hit_test` traversal primitive landed in `quartzite-hit-test` (#395). The natural next step is the event-dispatch pass that routes a `MouseEvent` to the hit widget's `on_mouse_press`/`on_mouse_release` (needs an event-routing design + mutable tree access). New issue when the input-dispatch pass starts (see the spec's § Deferred + the `#395`-tagged `_inbox.jsonl` row on `ScrollArea` clip-rect-aware hit testing, now satisfied by AC6's clip gate).
2. **`signals_blocked` persistence (#39)** — now unblocked by the serialization layer (#107 ✅); small targeted change to carry `signals_blocked` through `ObjectSnapshot` / restore.
2. **Multi-window support (#53)** — ✅ implemented. `WindowedAppHandler` + `WindowRegistry` + `try_create_window` + per-window `WidgetRoot` dispatch landed.
2. **Concrete `Style` implementation in `quartzite-style`** — ✅ implemented (#290). `DefaultStyle` ships `draw_widget` covering Button/Label/TextEdit/ScrollArea/Container/LineEdit with palette-driven flat rendering; downcast chain replaced with Hybrid `Paint<W>` dispatch (#373 ✅).
2. **`DefaultStyle` GPU snapshot tests** — ✅ implemented (#297). 7 pixel-level goldens in `quartzite-style/tests/snapshots/shared/`; snapshot-helper sync group established between `quartzite-widgets/tests/support/mod.rs` and `quartzite-style/tests/support/mod.rs`.
3. **Bootstrap Windows/macOS snapshot goldens (#192 follow-up)** — `gpu-snapshot-tests-ci` shipped Linux/vulkan goldens only; Win/Mac matrix lanes are `continue-on-error: true` until contributors with those platforms run `scripts/update-snapshots.sh` and commit the per-backend PNGs. Drop `continue-on-error` once both lanes are green.
3. **Gradient brush support (#281)** — ✅ implemented. `BrushKind::LinearGradient` / `RadialGradient` / `Custom(peniko::Gradient)` + `VelloPainter::brush_to_peniko` + 4 GPU snapshot tests.
3. **`DefaultStyle` Container + LineEdit arms (#318)** — ✅ implemented. `draw_container` (Window fill + WindowText outline) and `draw_line_edit` (Base fill, Text outline, single-line text with placeholder/read-only support); test module extracted to `default_style_tests.rs` via `#[path]`.
3. **`DefaultStyle` read-only overlay fix (#458)** — ✅ implemented. Overlay changed from invisible `disabled(Window)` to `WindowText.with_alpha(0.10)`; read-only text dims to `Text.with_alpha(0.65)`. Both `TextEdit` and `LineEdit` patched; golden regenerated.
3. **`DefaultStyle` hover/pressed/focused for Label/TextEdit/ScrollArea (#403)** — ✅ implemented. Generalises Button #316 state-rendering pattern across the three widgets via the post-#402 two-axis palette API. Private `state_group` helper powers all four state-aware impls; FocusRing pen exempt from `maybe_disabled` (full alpha always per design-system additive-overlay rule). 12 new snapshot goldens (9 light + 3 dark-focused). LineEdit equivalent landed in #406 (folds in #407) — closes the state-aware-paint matrix across all 5 interactive widgets.
3. **`DefaultStyle` LineEdit hover/pressed/focused + disabled-axis parity with TextEdit (#406, folds in #407)** — ✅ implemented. Reuses the shared `state_group` selector to bring `impl Paint<LineEdit> for DefaultStyle` to parity with the post-#403 widgets; #407 fold-in makes disabled-LineEdit visibly halve alpha on `Base` fill + `Text` outline + `Text` glyphs (pre-spec disabled-LineEdit was visually identical to enabled-LineEdit). 10 new recording-painter tests + 9 new snapshot goldens (7 light + 2 dark) including `line_edit_disabled.png` as the visible #407 anchor.
4. **Expand** `quartzite` facade prelude as new crates are implemented
4. Any future PR adding public items must satisfy the workspace doc convention at [`ai-docs/doc-convention.md`](../doc-convention.md): `#![deny(missing_docs)]` + `# Examples` + `# Parameters` (when ≥1 non-receiver arg) + `# Errors`/`# Panics`/`# Safety` when applicable; section ordering enforced by reviewer checklist; clippy `missing_errors_doc`/`missing_panics_doc`/`missing_safety_doc`/`doc_markdown` enabled across all crates
5. Match-based lookups are in place for properties/signals/methods/enums; enum lookup (`#[object_impl]` generates noop) could be wired up to `#[meta_enum]`-annotated enums when widgets land
6. `#[inline]` rule (recursive — see [`ai-docs/code-style.md` → `#[inline]` and the `_Simple._` doc tag](../code-style.md#inline-and-the-_simple_-doc-tag)) is enforced by AGENTS.md and review agents; new simple fns must carry the marker matching their shape — `#[inline]` on concrete fns, `_Simple._` doc tag on generic fns and on trait method declarations whose every conforming impl is required to be simple
7. Single-dep ergonomics are **already in place**: `quartzite-macros` uses `proc-macro-crate` to emit `::quartzite::core` paths when the user depends only on `quartzite`. Verified by `quartzite-macros/tests/via_facade.rs` and `quartzite/tests/single_dep.rs`.
