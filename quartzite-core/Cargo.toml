[package]
name = "quartzite-core"
version.workspace = true
edition.workspace = true

[features]
default = ["std"]
std = []

[dependencies]

[dev-dependencies]
rstest = "0.26.1"
pretty_assertions = "1"
